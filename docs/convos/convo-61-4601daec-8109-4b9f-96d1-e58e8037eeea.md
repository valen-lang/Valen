<!-- session: 4601daec-8109-4b9f-96d1-e58e8037eeea; exported-bytes: 1648686; transcript: /Users/verdagon/.claude/projects/-Volumes-V-Vale2/4601daec-8109-4b9f-96d1-e58e8037eeea.jsonl; opened: convo-58-953796ce-7895-4778-a383-def726cfde8e.md; continues: convo-58-953796ce-7895-4778-a383-def726cfde8e.md -->
# Claude Conversation Log

Session ID: 4601daec-8109-4b9f-96d1-e58e8037eeea
Date: 2026-08-01 22:44:28

---

## 👤 User

<command-message>continue-session</command-message>
<command-name>/continue-session</command-name>
<command-args>953796ce</command-args>

---

## 🤖 Claude

How many conversations back should I read?

(`/continue-session 953796ce reading 1` = just the predecessor, `reading 2` adds its predecessor, and so on; `reading 0` records the link without reading anything.)

---

## 🤖 Claude

Watcher armed. Still waiting on your answer: how many conversations back should I read?

---

## 👤 User

<command-message>continue-session</command-message>
<command-name>/continue-session</command-name>
<command-args>953796ce reading 2</command-args>

---

## 🤖 Claude

Linked `convo-61-4601daec-8109-4b9f-96d1-e58e8037eeea.md` → predecessor `convo-58-953796ce-7895-4778-a383-def726cfde8e.md`.

---

## 🤖 Claude

convo-58's head stamp has no `continues:` — only `opened:`. So the chain ends at depth 1; there's no second ancestor to walk to. Reading convo-58 in full now.

---

## 🤖 Claude

`vcoord-handoff.md` is the one — reading it in full.

---

## 🤖 Claude

Caught up. Here's where things stand.

## What was linked

`convo-61-4601daec…md` (this session) → `convo-58-953796ce…md`.

**`reading 2` could only get 1.** convo-58's head stamp carries `opened:` (convo-38, convo-56) but no `continues:`, so the chain has no second ancestor to walk to. I didn't pad the count from `opened:`. If you want convo-56 or convo-38 read too, say so — that's a different request than a continuation walk.

## Read

- `docs/convos/convo-58-953796ce…md` — 5,067 lines, in full
- `vcoord-handoff.md` — 1,958 lines, in full (the only handoff convo-58 names)

## Where the predecessor left off

**The through-line: argument types reached the call-site solve for the first time.** You wired the sends; every reverse direction in the solver had never been exercised, so each met its first real input as a shape it had no arm for. The answer was consistently **reject the candidate, don't tolerate the shape** — the overload resolver solves everything it finds by name, so a mismatch means that candidate loses.

**Suite 582/166/8 → 616/133/8**, two commits ratcheted to `experimental` (`6c859ed12`, `1e73e8d51`), fourteen changes, none reverted, no step introduced a failure.

Landed along the way: the send's three parts scoped to one solve (the `preliminary_sends_rune_to_type` clone); `KindIsNotBorrowRef` / `KindIsNotTemplateApplication` / the `WeakRef`+`OwnRef` wrap arms; `dot_borrow` dissolved into its two callers (19 tests); the per-param rule fold added to the dispatcher (`header_rules` renamed by you, two tripwires left on the lambda paths); `ssa_len` / `rsa_pop` / `lock_weak` / `as_subtype` macro bodies; drop generation collapsing five kinds into one arm (7 green); and the `TypingPassDefiningError` propagation that made the whole diagnosis legible instead of a 485KB `{:?}` dump.

**Next on the board**, per the ladder: real compile errors reaching `expect_compiler_outputs` (largest row, triage individually), then `UpcastTE::new` — one `unimplemented!` its own `VCOORD` answers with the `replace_value_type_in_ref` recipe you've now used twice, and where both of Vale4's interop tests sit. Behind that, export/extern, which is waiting on your `is_primitive` naming call rather than on work.

## Two stale spots in the handoff

Both contradicted by convo-58's own results, so worth knowing before you trust them:

- **PICK UP HERE, the Vale4 paragraph** (~line 130) still says `opt_with_undroppable_contents` "now fails on the unconsumed sends." It doesn't — both their tests moved past drop generation to `UpcastTE::new` and a `post_parser_error_humanizer` arm.
- **"Argument types do not enter the call-site value solve"** (~line 1064) says `assemble_initial_sends_from_args`'s result "is never consumed anywhere," which the block at line 105 directly refutes.

Ready when you are.

---

## 👤 User

gimme the next simplest-looking failure thats blocking them, and its simplest failing test?

---

## 🤖 Claude

Let me measure rather than quote the handoff — it's a session old.

---

## 🤖 Claude

Both of Vale4's tests confirmed. Let me look at the two sites.

---

## 🤖 Claude

## Site: `ast/expressions.rs:1137` — `UpcastTE::new`

**9 tests**, one of them Vale4's `opt_with_undroppable_contents`. Measured just now; suite is 616/133/8, tree has no source changes since `1e73e8d51`.

```rust
pub fn new(
    inner_expr: ExpressionTE<'s, 't>,
    target_super_kind: ISuperKindTT<'s, 't>,
    impl_name: IdT<'s, 't>,
) -> UpcastTE<'s, 't> {
    // VCOORD: preserve the inner wrap and swap the innermost citizen to target_super_kind.
    // perhaps: replace_value_type_in_ref(interner, inner_expr.result(), target_super_kind.into())
    unimplemented!("UpcastTE onion result")
}
```

**Every piece the VCOORD names already exists.** `replace_value_type_in_ref` (`templata_compiler.rs:101`) recurses through all four wraps and bottoms out at `_ => new_value_type`, which is exactly the owned case; `From<ISuperKindTT> for KindT` is at `types.rs:535`, so `.into()` resolves. The one thing the signature is missing is the interner — and there's **exactly one caller**, `convert_helper.rs:177`, which already reads `self.typing_interner.alloc(UpcastTE::new(…))`, so the interner is in scope at the only site that has to change.

The nine: `upcast`, `tests_upcasting_from_a_struct_to_an_interface`, `tests_upcasting_has_the_right_stuff`, `tests_calling_a_virtual_function`, `tests_making_a_variable_with_a_pattern`, `local_set_upcasts`, `upcast_generic`, `downcast_with_as`, `opt_with_undroppable_contents`.

## Simplest failing test

**`compiler_virtual_tests::upcast`** — six lines, no assertions beyond compiling:

```vale
interface IShip {}
struct Raza { fuel int; }
impl IShip for Raza;

exported func main() {
  ship IShip = Raza(42);
}
```

**`tests_upcasting_from_a_struct_to_an_interface` is the acceptance test**, and it's a good one: it asserts `upcast.result` is a **bare** `KindT::Interface(MyInterface)` and `upcast.inner_expr.result()` a **bare** `KindT::Struct(MyStruct)` — which is precisely what the helper's `_ => new_value_type` arm produces for an owned upcast. So the fix has a pinned shape rather than a guess. Drive with `upcast`, confirm with that one.

## Two things worth knowing before you scope it

**The borrowed and owned cases are one line, not two.** `local_set_upcasts` and `upcast_generic` will arrive wrapped where `upcast` arrives bare; the helper's recursion covers both, which is the whole reason the VCOORD points at it.

**The sibling stays untouched.** `InterfaceToInterfaceUpcastTE::new` carries the identical VCOORD twelve lines up and has **zero callers** — grep for `UpcastTE::new` returns one hit total, and it's the plain one. Don't fill it on the strength of symmetry.

## The other Vale4 test is a different, smaller thing

`opt_with_undroppable_mutable_ref_contents` sits at `postparsing/post_parser_error_humanizer.rs:158` — a bare `panic!("implement: humanize_rune StructNameRune")`, one of ~12 unfilled arms in that match. **One test, and filling it won't green it**: it's a humanizer arm, so it fires only because the program already reached a real compile error. The arm makes that error printable and hands you the diagnostic to read. Cheaper than the upcast and worth doing at some point, but it buys a message rather than a pass — and the spelling should come off the ISNZ prefix table, same as the `dis:` / `case:` arms last session.

---

## 👤 User

investigate the failure please

---

## 🤖 Claude

Reading the caller settled part of it. Let me check the two arms that reach it, then probe.

---

## 🤖 Claude

Nine hits, and the distribution is the finding. Let me tie each to its test and check the borrowed case.

---

## 🤖 Claude

Probe run and removed; tree is back to doc-only changes, suite back to 616/133/8.

## What arrives at `UpcastTE::new`

All nine hits, shape-summarized:

| source | target | count |
|---|---|---|
| bare `Struct(Raza)` | bare `Interface(IShip)` | 2 |
| bare `Struct(Toyota)` | bare `Interface(Car)` | 2 |
| bare `Struct(MyStruct)` | bare `Interface(MyInterface)` | 1 |
| bare `Struct(MySome<int>)` | bare `Interface(MyOption<int>)` | 1 |
| bare `Struct(XNone<int>)` | bare `Interface(IXOption<int>)` | 1 |
| bare `Struct(Some<Spaceship>)` | bare `Interface(Opt<Spaceship>)` | 1 |
| bare `Placeholder(T)` | bare `Interface(IShip)` | 1 ← `upcast_generic` |

**Not one wrapped source.** Every arrival is bare on both sides.

## What that means for the recipe

`replace_value_type_in_ref` hits its `_ => new_value_type` arm on a bare source, so for all nine tests today the VCOORD's recipe is **observationally identical to `result = target_super_kind.into()`**. The wrap-preserving recursion — the entire reason the helper is named — is dead with respect to the current corpus.

Two things make that a fact about the corpus rather than about the code:

**The target side can never be wrapped, by construction.** `convert_via_upcast:151` gates on `ISuperKindTT::try_from(target_kind)`, which admits only `Interface` and `KindPlaceholder`. Anything wrapped returns `CouldntConvertT` before reaching `new`. So `target_super_kind.into()` is always a bare citizen — that's a guarantee, not an observation.

**The source side genuinely can be wrapped.** `convert()`'s `(BorrowRef, BorrowRef)` arm at :86-99 peels only the *kind arguments* it hands to `convert_via_upcast`; `source_expr` passes through untouched, so `inner_expr.result()` is the full onion while `target_super_kind` is already peeled. That asymmetry is exactly what the helper exists for. It just isn't exercised.

## Why the borrowed path is absent — it's blocked, not missing

The corpus does contain the program that would exercise it: **`tests_calling_a_virtual_function_through_a_borrow_ref`**. It dies earlier, at `edge_compiler.rs:163` (one of that site's 4 tests), so it never reaches the upcast. Same shape as `dot_borrow` last session — all the traffic on one arm, the others unexercised because their tests stop upstream.

So the wrapped half will arrive the moment `edge_compiler.rs:163` clears, and **nothing today can verify it**.

## The verification you do get

`tests_upcasting_from_a_struct_to_an_interface` asserts `upcast.result` is a bare `KindT::Interface(MyInterface)` and `inner_expr.result()` a bare `KindT::Struct(MyStruct)`. That pins the bare path exactly — it's a real acceptance test, not a compile-only one. The wrapped path has no assertion anywhere.

## Mechanics

One caller, `convert_helper.rs:177`, already reads `self.typing_interner.alloc(UpcastTE::new(…))` — so adding the interner parameter the helper needs is a same-line change at the only site. `From<ISuperKindTT> for KindT` exists at `types.rs:535`.

## One adjacent thing the probe ruled out of scope

The no-borrow arm at `convert.rs:118` guards only on `BorrowRef`, so a `ShareRef` / `OwnRef` / `WeakRef` source reaches `convert_via_upcast` **unpeeled** and fails `ISubKindTT::try_from` → `CouldntConvertT`. That's defect 4, it sits upstream of this site, and filling `UpcastTE::new` neither fixes nor disturbs it. No corpus test reaches it today.

## The one call I'd want from you

**Write it as `replace_value_type_in_ref(…)` or as the bare `target_super_kind.into()`?** The helper is what the VCOORD names, it's correct, and it's right the day `edge_compiler:163` clears — but it takes an interner parameter and a recursion that no test will exercise, so it lands as unverified code. The one-liner is verified by the corpus and wrong later. I'd take the helper and say in the comment that only the bare arm is exercised today, so the next person knows the wrapped half is untested rather than assuming it's proven.

---

## 👤 User

you are very helpful, but also very verbose, and i just cant read all the things you say. please explain to me, in very simple terms, from the top, what the failing test is trying to ensure and why its failing and what the cause is

---

## 🤖 Claude

## What the test wants

```vale
interface IShip {}
struct Raza { fuel int; }
impl IShip for Raza;

exported func main() {
  ship IShip = Raza(42);
}
```

You make a `Raza` and put it in a variable declared as `IShip`. That's an **upcast** — treating a specific type as the general one it implements. The test just asks that this compiles.

## Why it fails

The compiler crashes with `not implemented: UpcastTE onion result`.

## The cause

When the compiler builds an upcast, it makes an `Upcast` node. Every node has to record what type it produces. That one line was never written — it's a stub that panics instead.

```rust
pub fn new(inner_expr, target_super_kind, impl_name) -> UpcastTE {
    unimplemented!("UpcastTE onion result")
}
```

It's a leftover from the onion migration: nodes used to get their type computed elsewhere, and when that moved into each node's constructor, this one got a `panic!` as a placeholder.

## What it should say

The answer it needs is the interface — `IShip`. That's already sitting in the `target_super_kind` argument.

The only wrinkle: if the thing being upcast is a *borrow* (`&Raza` → `&IShip`), the `&` has to survive. There's an existing helper for that, and the comment above the stub already names it.

---

## 👤 User

that was better. lets make it even better than that though:
- your "why it fails" is disorienting, because it doesnt give me any lead-in.
- whenever you show me a snippet, give me the full filepath and line.

---

## 🤖 Claude

## What the test wants

`FrontendRust/src/typing/test/compiler_virtual_tests.rs:118` — `fn upcast`, whose program is at lines 126–134:

```vale
interface IShip {}
struct Raza { fuel int; }
impl IShip for Raza;

exported func main() {
  ship IShip = Raza(42);
}
```

You make a `Raza` and put it in a variable declared as `IShip`. That's an **upcast** — treating a specific type as the general one it implements. The test just asks that this compiles.

## Why it fails

The compiler gets almost all the way there. It parses the program, finds the `impl` that makes a `Raza` count as an `IShip`, decides an upcast is needed, and starts building the `Upcast` node for it — that's `FrontendRust/src/typing/convert_helper.rs:177`.

Building that node is where it dies. Every node has to record what type it produces, and for this one that step was never written:

```rust
// FrontendRust/src/typing/ast/expressions.rs:1129-1138
impl<'s, 't> UpcastTE<'s, 't> where 's: 't, {
    pub fn new(
        inner_expr: ExpressionTE<'s, 't>,
        target_super_kind: ISuperKindTT<'s, 't>,
        impl_name: IdT<'s, 't>,
    ) -> UpcastTE<'s, 't> {
        // VCOORD: preserve the inner wrap and swap the innermost citizen to target_super_kind.
        // perhaps: replace_value_type_in_ref(interner, inner_expr.result(), target_super_kind.into())
        unimplemented!("UpcastTE onion result")   // ← line 1137, the panic
    }
}
```

## The cause

A leftover from the onion migration. Node types used to be computed elsewhere; when that job moved into each node's own constructor, this one got a `panic!` as a placeholder and never got filled in.

## What it should say

The answer it needs is the interface — `IShip` — which is already sitting in the `target_super_kind` argument.

The only wrinkle: if the thing being upcast is a *borrow* (`&Raza` → `&IShip`), the `&` has to survive. The comment on line 1136 already names the helper for that: `replace_value_type_in_ref`, at `FrontendRust/src/typing/templata_compiler.rs:101`.

---

## 👤 User

that was better. lets make it even better than that though:
- your "why it fails" is disorienting, because it doesnt give me any lead-in. is this happening during the compilation of main's definition? is it looking for an impl? looking for an upcast function?
- whenever you show me a snippet, give me the full filepath and line.

---

## 🤖 Claude

## What the test wants

**`FrontendRust/src/typing/test/compiler_virtual_tests.rs:118`** — `compiler_virtual_tests::upcast`:

```vale
interface IShip {}
struct Raza { fuel int; }
impl IShip for Raza;

exported func main() {
  ship IShip = Raza(42);
}
```

You make a `Raza` and put it in a variable declared as `IShip`. That's an **upcast** — treating a specific type as the general one it implements. The test just asks that this compiles.

## Why it fails

It happens while compiling **`main`'s body**, on the `ship IShip = Raza(42)` line.

The right-hand side `Raza(42)` has type `Struct(Raza)`. The variable is declared `Interface(IShip)`. They don't match, so the compiler calls `convert()` to bridge them — **`FrontendRust/src/typing/convert_helper.rs:60`**.

`convert()` sees no borrow on either side, so it takes the plain-upcast arm at **`convert_helper.rs:118`** and hands off to `convert_via_upcast` at **`convert_helper.rs:139`**.

That function then **finds the impl successfully** — `is_parent` at **`convert_helper.rs:164`** looks up `impl IShip for Raza` and returns it. Nothing is missing and nothing is unresolved.

Having found it, at **`convert_helper.rs:177`** it goes to build the `Upcast` node — and *that* is where it dies.

## The cause

**`FrontendRust/src/typing/ast/expressions.rs:1129-1138`**:

```rust
impl<'s, 't> UpcastTE<'s, 't> where 's: 't, {
    pub fn new(
        inner_expr: ExpressionTE<'s, 't>,
        target_super_kind: ISuperKindTT<'s, 't>,
        impl_name: IdT<'s, 't>,
    ) -> UpcastTE<'s, 't> {
        // VCOORD: preserve the inner wrap and swap the innermost citizen to target_super_kind.
        // perhaps: replace_value_type_in_ref(interner, inner_expr.result(), target_super_kind.into())
        unimplemented!("UpcastTE onion result")   // ← line 1137
    }
}
```

Every expression node has to record what type it produces. This one's line was never written.

It's leftover from the onion migration: node types used to be computed elsewhere, and when that job moved into each node's own `new()`, this one got a `panic!` as a placeholder.

## What it should say

The answer is the interface, `IShip` — already sitting in the `target_super_kind` argument.

The one wrinkle: if the upcast thing is a *borrow* (`&Raza` → `&IShip`), the `&` has to survive. The helper for that is **`FrontendRust/src/typing/templata_compiler.rs:101`**, `replace_value_type_in_ref`, which the comment on line 1136 already names.

---

## 👤 User

can you get me the full call stack trace for the place(s) that are problematic? the places with the symptoms/errors

---

## 🤖 Claude

All nine backtraces are **byte-identical** above the test frame. Here's the one, from `compiler_virtual_tests::upcast`:

```
thread 'typing::test::compiler_virtual_tests::upcast' panicked at
  src/typing/ast/expressions.rs:1137:9:
not implemented: UpcastTE onion result

 2: <UpcastTE>::new
      at ./src/typing/ast/expressions.rs:1137:9
 3: <Compiler>::convert_via_upcast
      at ./src/typing/convert_helper.rs:177:64
 4: <Compiler>::convert
      at ./src/typing/convert_helper.rs:121:22
 5: <Compiler>::infer_and_translate_pattern::<...evaluate_expression::{closure#2}>
      at ./src/typing/expression/pattern_compiler.rs:134:22
 6: <Compiler>::evaluate_expression
      at ./src/typing/expression/expression_compiler.rs:465:38
 7: <Compiler>::evaluate_expression
      at ./src/typing/expression/expression_compiler.rs:492:30
 8: <Compiler>::evaluate_block_statements_block
      at ./src/typing/expression/block_compiler.rs:49:18
 9: <Compiler>::evaluate_block_statements
      at ./src/typing/expression/expression_compiler.rs:2019:14
10: <Compiler>::evaluate_function_body
      at ./src/typing/function/function_body_compiler.rs:170:18
11: <Compiler>::declare_and_evaluate_function_body
      at ./src/typing/function/function_body_compiler.rs:81:51
12: <Compiler>::finish_function_maybe_deferred
      at ./src/typing/function/function_compiler_core.rs:276:18
13: <Compiler>::evaluate
      at ./src/typing/compiler.rs:1254:30
14: <TypingPassCompilation>::get_compiler_outputs
      at ./src/typing/compilation.rs:118:18
15: <TypingPassCompilation>::expect_compiler_outputs
      at ./src/typing/compilation.rs:129:14
16: frontend_rust::typing::test::compiler_virtual_tests::upcast
      at ./src/typing/test/compiler_virtual_tests.rs:140:13
```

Reading it bottom-up: compile the program → compile `main`'s body → walk its statements → hit the `let` → bind the pattern → the value's type doesn't match the declared type, so `convert` → it's an upcast, so `convert_via_upcast` → build the node → stub.

## The finding

Frame 5 is the same in all nine: **`infer_and_translate_pattern`**. Every upcast in the whole corpus arrives at a `let` binding.

That's worth knowing because `convert()` has six callers — let bindings, call arguments, `return`, if/else branches, `set` destinations, function results. Only the `let` one ever produces an upcast today. Even `local_set_upcasts` and `tests_calling_a_virtual_function`, whose names suggest a `set` and a call, arrive through the `let`.

Want the trace for the other Vale4 blocker (`post_parser_error_humanizer.rs:158`)? It's a different shape — that one comes through an error path, not the expression walk.

---

## 👤 User

give me that stack trace again but full filepath+line for each

---

## 🤖 Claude

Paths in the raw backtrace are relative to `FrontendRust/`; expanded:

```
thread 'typing::test::compiler_virtual_tests::upcast' panicked at
  /Volumes/V/Vale2/FrontendRust/src/typing/ast/expressions.rs:1137:9
not implemented: UpcastTE onion result

 2: <UpcastTE>::new
      /Volumes/V/Vale2/FrontendRust/src/typing/ast/expressions.rs:1137:9
 3: <Compiler>::convert_via_upcast
      /Volumes/V/Vale2/FrontendRust/src/typing/convert_helper.rs:177:64
 4: <Compiler>::convert
      /Volumes/V/Vale2/FrontendRust/src/typing/convert_helper.rs:121:22
 5: <Compiler>::infer_and_translate_pattern::<...evaluate_expression::{closure#2}>
      /Volumes/V/Vale2/FrontendRust/src/typing/expression/pattern_compiler.rs:134:22
 6: <Compiler>::evaluate_expression
      /Volumes/V/Vale2/FrontendRust/src/typing/expression/expression_compiler.rs:465:38
 7: <Compiler>::evaluate_expression
      /Volumes/V/Vale2/FrontendRust/src/typing/expression/expression_compiler.rs:492:30
 8: <Compiler>::evaluate_block_statements_block
      /Volumes/V/Vale2/FrontendRust/src/typing/expression/block_compiler.rs:49:18
 9: <Compiler>::evaluate_block_statements
      /Volumes/V/Vale2/FrontendRust/src/typing/expression/expression_compiler.rs:2019:14
10: <Compiler>::evaluate_function_body
      /Volumes/V/Vale2/FrontendRust/src/typing/function/function_body_compiler.rs:170:18
11: <Compiler>::declare_and_evaluate_function_body
      /Volumes/V/Vale2/FrontendRust/src/typing/function/function_body_compiler.rs:81:51
12: <Compiler>::finish_function_maybe_deferred
      /Volumes/V/Vale2/FrontendRust/src/typing/function/function_compiler_core.rs:276:18
13: <Compiler>::evaluate
      /Volumes/V/Vale2/FrontendRust/src/typing/compiler.rs:1254:30
14: <TypingPassCompilation>::get_compiler_outputs
      /Volumes/V/Vale2/FrontendRust/src/typing/compilation.rs:118:18
15: <TypingPassCompilation>::expect_compiler_outputs
      /Volumes/V/Vale2/FrontendRust/src/typing/compilation.rs:129:14
16: frontend_rust::typing::test::compiler_virtual_tests::upcast
      /Volumes/V/Vale2/FrontendRust/src/typing/test/compiler_virtual_tests.rs:140:13
```

---

## 👤 User

give me that stack trace again but full filepath+line for each, and reversed and indented so its like a call tree all the way from compiler_virtual_tests::upcast

---

## 🤖 Claude

Each `file:line` is the **call site inside that function** — where it calls the one indented below it.

```
compiler_virtual_tests::upcast
  /Volumes/V/Vale2/FrontendRust/src/typing/test/compiler_virtual_tests.rs:140:13
  │
  └─ TypingPassCompilation::expect_compiler_outputs
       /Volumes/V/Vale2/FrontendRust/src/typing/compilation.rs:129:14
     │
     └─ TypingPassCompilation::get_compiler_outputs
          /Volumes/V/Vale2/FrontendRust/src/typing/compilation.rs:118:18
        │
        └─ Compiler::evaluate
             /Volumes/V/Vale2/FrontendRust/src/typing/compiler.rs:1254:30
           │
           └─ Compiler::finish_function_maybe_deferred
                /Volumes/V/Vale2/FrontendRust/src/typing/function/function_compiler_core.rs:276:18
              │
              └─ Compiler::declare_and_evaluate_function_body
                   /Volumes/V/Vale2/FrontendRust/src/typing/function/function_body_compiler.rs:81:51
                 │
                 └─ Compiler::evaluate_function_body
                      /Volumes/V/Vale2/FrontendRust/src/typing/function/function_body_compiler.rs:170:18
                    │
                    └─ Compiler::evaluate_block_statements
                         /Volumes/V/Vale2/FrontendRust/src/typing/expression/expression_compiler.rs:2019:14
                       │
                       └─ Compiler::evaluate_block_statements_block
                            /Volumes/V/Vale2/FrontendRust/src/typing/expression/block_compiler.rs:49:18
                          │
                          └─ Compiler::evaluate_expression
                               /Volumes/V/Vale2/FrontendRust/src/typing/expression/expression_compiler.rs:492:30
                             │
                             └─ Compiler::evaluate_expression
                                  /Volumes/V/Vale2/FrontendRust/src/typing/expression/expression_compiler.rs:465:38
                                │
                                └─ Compiler::infer_and_translate_pattern
                                     /Volumes/V/Vale2/FrontendRust/src/typing/expression/pattern_compiler.rs:134:22
                                   │
                                   └─ Compiler::convert
                                        /Volumes/V/Vale2/FrontendRust/src/typing/convert_helper.rs:121:22
                                      │
                                      └─ Compiler::convert_via_upcast
                                           /Volumes/V/Vale2/FrontendRust/src/typing/convert_helper.rs:177:64
                                         │
                                         └─ UpcastTE::new
                                              /Volumes/V/Vale2/FrontendRust/src/typing/ast/expressions.rs:1137:9
                                              ►► PANIC: not implemented: UpcastTE onion result
```

---

