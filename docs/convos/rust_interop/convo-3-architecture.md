# Claude Conversation Log

Session ID: 1fef7402-0d47-4325-8680-9bacb1f4d5a2
Date: 2026-07-01 00:47:48

---

## 👤 User

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

---

## 👤 User

<command-name>/effort</command-name>
            <command-message>effort</command-message>
            <command-args>max</command-args>

---

## 👤 User

<local-command-stdout>Set effort level to max (this session only): Maximum capability with deepest reasoning. May use excessive tokens resulting in long response times or overthinking. Use sparingly for the hardest tasks.</local-command-stdout>

---

## 👤 User

please read vale-rust-interop-architecture.md in full, no skipping any parts

---

## 🤖 Claude

Read the full document — all 3,032 lines from top to bottom, no skips.

Quick structural summary of what's in there:

- **§0–2**: Doc meta, goals/constraints (memory-safe systems lang, groups + linear types, slab-based comptime, deep Rust interop), and the "interleaved monomorphization" invariant driving the whole design.
- **§3–5**: Two-binary split (`valec` standalone ~40-100MB vs `valec-rs` ~2GB bundling forked rustc), the 4-patch rustc fork, and the C++ Backend in borrowed-mode FFI.
- **§6–10**: Stub rlib model, local `.vale-cache` (no distributed sidecar), HinputsT, export/visibility, and `ValeOpaqueType<const T: u128>` type representation.
- **§11–17**: Groups + `dangle`, honest Send/Sync auto-derive (HBAB — divergent from Sky's "lie globally"), comptime, closures + async (linear default, migratory opt-in, `into_cancellable`), drop-as-function via `__vale_drop<T>`, `panic=abort`, and tokio interop.
- **§18–24**: Build orchestration via `vale.toml` + generated `.vale-build/`, per_instance_mir + Approach A, pipeline ordering, source-only library distribution, incremental compilation, diagnostics, and annotation files for Rust deps.
- **§25–29b**: Risks (A/B/C categories), cross-cutting invariants (MINCZ, GCMLZ, ELASZ, HBAB, DRAFD, CMWAR, CIDD, NNGZ, etc.), compatibility promises, ~3-5-year phased implementation, open questions, and no_std posture (deferred to v2+).
- **§30 + Appendices**: Glossary, fork patch reference, codegen backend method reference, and toylang lessons inherited (F.1–F.22).

Version 0.1.0, pre-implementation draft. Ready for whatever's next.

---

## 👤 User

please read docs/historical/vale-rust-interop-architecture-convo-2.md in full

---

## 🤖 Claude

Read the full document — all 3,074 lines from top to bottom, no skips.

Structural recap of what's in the transcript:

**Session bootstrap (lines 1-460).** Prior-session recap of the ~3,617-line convo-1 log; `/effort max` set; `/guardian-tl` skill loaded (three roles, mailbox mechanics, ordination); ordained on port 7882; watcher armed as `Vale4-tl`; briefed that Harmonious/toylang folks would review the Vale design via mailbox.

**Q1 — `__mode_is_rustc()` retirement (lines 460-730).** Harmonious-tl flagged that §3.3's `comptime if __mode_is_rustc()` framing was overreach; you confirmed we only have `#[cfg(rust_interop)]` (item-level, parse-time binary expressions Rust-style) + `comptime if __deterministic()` (body-level unsafe gating). Five doc edits landed; response sent.

**Q2 — String allocator-genericity + Vec/HashMap wrapping (lines 730-1160).** Long thread: I got the "Rust's `String<A>` landed" claim wrong (WebSearch conflated PR discussion with merge); Harmonious-tl called it out via `gh pr view`. Real state: PR #149328 open since Nov 2025, unlanded; `Vec<T,A>`/`HashMap<K,V,S,A>`/`Box<T,A>` have had allocator params on nightly for years via `allocator_api`. Landed on **option D**: comptime-conditional backing for `String<A>` — wraps `rust.std.string.String` when A=GlobalAlloc, wraps `Vec<u8, A>` otherwise. Locks comptime-selection-of-types as a Vale language feature. Vec/HashMap/Box wrap Rust std directly. Four doc edits (§3.3, §12.1, §13.4, §29.5).

**Q3 — `dangle` mechanics + group borrowing (lines 1160-1270).** You pointed at `/Volumes/V/VerdagonSite/src/grimoire/group-borrowing.vmd` (Nick Smith's model). Confirmed: `dangle` is user-annotated (not inferred), typing pass has mostly stripped older region machinery, group-borrowing is completely unimplemented and won't ground-break for months. Minimal §11 intro reframing + §11.10 rewrite — didn't rewrite §11.1-§11.7 because the region framing is provisional pending group-borrowing landing.

**Q4 — valec ↔ valec-rs cache switching (lines 1270-1330).** You picked **(b)**: same target/, switching thrashes. Framed as "graduate from valec to valec-rs once you want Rust; one-way transition; cache thrash paid once." Doc edits to §3.4 + §11 intro.

**Q5 — per-view refs / §12.5 rework (lines 1330-2896).** The longest and most substantive thread. Traversed:
- Initial finding that Sky's lie-globally + §29.A.skyref are both unimplemented in Sky's actual codebase (Harmonious-tl grepped).
- Your seamless-fearless-concurrency framing (three view cases; case 3 = imm-region refs where Vale knows more than Rust).
- Long spitball about read/imm/normal groups; you collapsed imm=read to just two tiers.
- Send/Sync deep-dive: whether Vale needs either as language concepts; corrections on Mutex elision (Rust does elide via `get_mut(&mut self)`, so Vale generalizes via group analysis); Cell/RefCell stay in Vale but inaccessible through read groups.
- Rust → Vale import semantics for references (anonymous groups; you tightened my mapping several times).
- The key architectural insight: Vale read groups **prevent** unsync interior mutation at the access level, so cross-thread sharing is universally safe for read-group refs — Rust needs Sync because `&T` permits interior mutation; Vale doesn't.
- ValeImm wrappers narrowed: per-type opaque (`ValeImm_Spaceship_Ref<'parallel>`, not generic-over-view), lifetime-bounded to pure/parallel scope, always Send+Sync structurally.
- You confirmed **auto-derive Send at typing pass, Rust-style**; five doc edits landed (§12 framing, §12.1, §12.4, §12.5, §26 HBAB + glossary); Q45 B's default-!Send revised to default-Send-if-all-fields-Send.

**Final exchange (lines 2896-3073).** Watcher killed; no more doc editing; you asked for a thorough explanation of what Sync is in Rust, why Rust needs it, and what read groups subsume. I laid out (α)/(β)/(γ) positions on Sync at boundary — recommended (β) auto-derive parallel to Send for Rust-ecosystem ergonomics — and confirmed `&mut T` import: anonymous normal group with `mut x` effect (matching your earlier bullet). Ended with the Sync-position call pending your decision, no doc edits pending.

Ready for whatever's next.

---

## 👤 User

can you please make sure the architecture doc is up to date with this new stuff we discussed in that convo? please be thorough

---

## 🤖 Claude

I'll do a targeted verification pass on the sections that convo-2 touched, identify gaps against what was actually agreed vs what's in the doc, and surface a plan before editing.

---

## 🤖 Claude

Let me batch-read a few more sections to complete my audit before surfacing a plan.

---

## 🤖 Claude

Audit done. Here's where the doc stands vs the convo, and the one open call I need from you before I edit.

## What's already in the doc (verified)

All 5 batches of edits from the convo landed:
- **§3.3** — `#[cfg(rust_interop)]` as parse-time binary expressions; dual `#[cfg]`-gated definitions for body variants; no `__mode_is_rustc`.
- **§3.4** — graduation framing (valec → valec-rs is one-way; cache thrash paid once).
- **§11 intro** — group-borrowing locked direction + unimplemented status.
- **§11.10** — user-explicit `dangle` annotation, checked not asserted.
- **§12.1** — auto-derive Send at typing pass; String comptime-conditional backing; allocator-generic wraps rust std.
- **§12.4** — HBAB rationale.
- **§12.5** — per-type `ValeImm_T_Ref<'parallel>` wrappers, narrowed to pure/parallel-block scope, lifetime-bounded.
- **§13.4** — comptime selection among pre-existing types allowed.
- **§26.20 HBAB invariant** — updated.
- **§28 Phase 1/7** — `__mode_is_rustc` references dropped.
- **§29.5** — String comptime-conditional entry.
- **§29.8** — `__mode_is_rustc` dropped from open questions.
- **Glossary** — `ValeImm` wrappers correctly described.

## What's NOT in the doc (agreed in convo but not yet edited — you said "no more editing the doc tonight")

1. **Rust → Vale reference import semantics** (new subsection in §11):
   - Rust `&T` (any T) → Vale anonymous read group, universal lift, no type check
   - Rust `&mut T` → Vale anonymous normal group with `mut x` effect, single-mut enforced
   - Lifetime = Rust borrow's lifetime
   - Multiple incoming refs = independent anonymous groups
   - `'static` refs → unbounded group

2. **Read-group access-level rule** (belongs in §11 or §12):
   - Read group = "no unsynchronized mutation through this reference"
   - Cell.set / RefCell.borrow_mut inaccessible through read-group refs
   - Mutex.lock / atomic ops / channel send/recv accessible (escape hatch)
   - This is Vale's alternative to Sync — Vale-internal cross-thread safety falls out of the group annotation

3. **Cell/RefCell in stdlib**: they stay in Vale but are blocked through read groups. Not currently documented anywhere.

4. **Mutex elision via group analysis at call sites** (Rust-precedented via `get_mut(&mut self)`):
   - Vale's `Mutex<T>` wraps `rust.std.sync.Mutex<T>` (consistent with Vec/HashMap pattern)
   - Codegen at Vale call sites picks `mutex.get_mut().unwrap()` when group analysis proves only-immediately-accessible; else `mutex.lock().unwrap()`
   - Cross-boundary to Rust always emits real lock (Rust lacks group context)
   - This IS Vale generalizing Rust's existing `get_mut` pattern, not a novel elision

## The one open decision I need before editing

Sync at the boundary. The convo ended with you asking "why do we need Sync at all in Vale?" and me presenting three options. Current doc §12.1 line 1224 reflects **(β)** — auto-derive Sync at typing pass parallel to Send — but you never confirmed.

Three positions:

- **(α) No Sync claims from Vale at boundary.** Vale types default `!Sync` to Rust. Rust callers wanting to share Vale refs cross-thread MUST go through a Vale pure/parallel block to get a `ValeImm_T_Ref<'parallel>` wrapper. Maximum honesty; forces the ergonomic detour for cross-thread share.

- **(β) Auto-derive Sync at typing pass, parallel to Send** (current doc state). Vale-source has no Sync surface; stub_gen emits `unsafe impl Sync for T` when field walk concludes Sync. Rust callers get plain `&T` that's cross-thread-shareable in safe cases; wrapper is only needed when Vale's stronger-than-Sync temporary-immutability proof matters. **My recommendation** — matches Rust ecosystem ergonomics; the auto-derive doesn't fight your "Sync is a group property Vale-internally" framing because it's a *boundary-emission* concern, not a Vale-source surface.

- **(γ) Explicit `unsafe impl Sync` opt-in only.** No auto-derive. Users write `unsafe impl Sync for MyType` when they want it. Weird given Sync isn't otherwise a Vale-source concept.

Which position? (β) means I just add framing that clarifies "Vale source has no Sync surface; stub_gen auto-derives from fields for boundary purposes; read groups do the equivalent work Vale-internally." (α) means a substantial §12.1 + §12.5 rewrite. (γ) means adding an opt-in surface.

---

## 👤 User

put in the doc that we havent really decided between α and β

---

## 🤖 Claude

Landing the α/β note plus the other pending edits from the convo (Rust → Vale imports, read-group access rule, Cell/RefCell in stdlib, Mutex elision, HBAB invariant update). Let me batch the exact-string reads I need, then execute.

---

## 🤖 Claude

Now executing the edits. 8 total, all in different unique locations so they can run in parallel.

---

## 🤖 Claude

All edits landed. Let me sanity-check for stragglers — any lingering "auto-derive Sync" claims elsewhere in the doc, and a quick line-count / structure verification.

---

## 🤖 Claude

Found stragglers in §12.4 and §12.5 that still assert Sync auto-derive as fact. Fixing them for consistency with the α/β openness.

---

## 🤖 Claude

Verifying end state — grep for any remaining unqualified Sync-auto-derive claims and check the section count.

---

## 🤖 Claude

Done. Doc is at 3,118 lines (was 3,032; +86). Structural additions all landed:

- **§11.11** new — Rust → Vale reference imports (universal `&T` → anon read group; `&mut T` → anon normal group + mut effect; lifetime = Rust borrow scope; `'static` unbounded; round-trip loses precision but stays sound)
- **§12.6** new — Read groups as Vale-internal alternative to Sync (access-level rule; Cell/RefCell blocked; Mutex/atomics/channels are the escape hatch; Arc/Mutex/Arc<Mutex<T>> composition)
- **§12.7** new — Mutex elision via group analysis (Vale generalizes Rust's `get_mut(&mut self)` pattern via group-borrowing; wraps `rust.std.sync.Mutex<T>` per stdlib pattern; boundary always emits real lock)
- **§29.9** new — Sync at the boundary α vs β open question with full framing, consideration axes, and resolution criteria

Reframes for α/β Sync openness landed in:
- **§11** intro (added §11.11 to conditional-on-group-borrowing list)
- **§12 opener** paragraph (Sync [OPEN] flag with cross-refs)
- **§12.1 heading** ("Auto-derived Send at the typing pass (Sync undecided per §29.9)")
- **§12.1 body** Sync line (α/β presented; §12.6 cross-ref for the Vale-internal picture)
- **§12.4** ("Why honest") — Send stays honest-auto-derive; Sync openness flagged; user-override for Sync qualified as β-only
- **§12.5** ValeImm section — three places softened to reflect that the wrapper's role depends on α/β
- **§26.20 HBAB body** — Sync [OPEN] noted with the "honest under either position" argument
- **§26 summary table HBAB row** — one-liner updated

Verified no stragglers assert Sync auto-derive as unconditional fact — all remaining "auto-derive Sync" mentions are now inside explicit (β)-branch descriptions or in the α/β open question.

---

## 👤 User

now, i'd like to toss around with you an alternative approach, where there's only one kind of group, and when there's no mutation effects on a group it's basically like a "read" group (and can be shared among multiple threads), theres no "normal" group per se. here's their writeup below, please summarize it back to me to see if we're on the same page:

# Deep-immutable references and `Sync`-as-permission

*A design note for a Rust-like language*

## The one-sentence version

Split Rust's `&T` into two references — a **deeply immutable, unconditionally sendable** reference and today's shared reference — and then, in the strong form of the design, delete the shared-but-unsynchronized escape hatch entirely. What remains is a language with exactly two memory regimes (frozen and synchronized), where `Sync` stops being a sharing-permission auto-trait and becomes the permission token required to have interior mutability at all.

This note develops that from the observation that Rust's `&T` quietly does two unrelated jobs, and shows where each design decision leads.

---

## 1. The problem: `&T` conflates "immutable" and "shared"

In today's Rust, `&T` means *shared*, not *immutable*. The two came apart the moment interior mutability existed: through a `&Cell<T>`, `&RefCell<T>`, `&Mutex<T>`, or `&AtomicUsize`, you can mutate. The project acknowledged this by renaming `&T` from "immutable reference" to "shared reference" in the Reference and Nomicon. `Sync` exists precisely to police this: `T: Sync ⟺ &T: Send`, and the *only* reason `&T: Send` isn't automatic is that shared references can reach interior-mutable state that isn't safe to touch from two threads.

The root primitive is `UnsafeCell<T>`, which carries the sole `!Sync` opt-out (`impl<T: ?Sized> !Sync for UnsafeCell<T>`). Everything else that's `!Sync` — `Cell`, `RefCell`, `OnceCell`, non-atomic `Rc`'s refcount — is `!Sync` *because* it's built on `UnsafeCell`. (Raw pointers and `Rc`/`NonNull` are separately `!Sync` too, but `UnsafeCell` is the interior-mutability root.)

## 2. The proposal: a deeply immutable reference `&imm T`

Introduce a second reference form. Call it `&imm T`. Its defining rule:

> **`&imm` never coerces to `&` (shared).**

That single non-coercion is the whole mechanism. Everything else follows from it.

### Why it's sound, and why it's *unconditionally* sendable

Because `&imm` can't downgrade to `&shared`, it cannot call any `&self` method whose receiver is `&T`. That's exactly the API surface of interior mutability: `Cell::get`, `Cell::set`, `RefCell::borrow`, `Rc::clone`, `Rc::strong_count`, `Mutex::lock` — all take `&self`. Through `&imm` you can only *project to fields* (`&imm T → &imm Field`) and read the frozen bytes.

Formally: **through `&imm T`, the only reachable state is the `Freeze` substructure of `T`** — the bytes that contain no `UnsafeCell`. Everything behind an `UnsafeCell` is unreachable, because every path to it goes through a `&self` API or a raw pointer, and `&imm` grants neither.

Those reachable bytes cannot be written by anyone for the borrow's duration:

- Writing them would require a unique `&mut`/`inout`, which the outstanding `&imm` borrow excludes.
- There is no interior mutability *in them*, by construction (they're the frozen part).

Race-free reads, by definition. Therefore:

```
&T      : Send   ⟺   T: Sync       // today: retains the &self interior-mut API
&imm T  : Send   always            // proposed: that API surface is gone
```

The `Sync` bound on `&T: Send` was only ever protecting against the interior-mutability API. Drop the API and the bound has nothing left to guard, so it drops too — for *all* `T`, including `!Sync` and even `!Send` types.

### The worked example: `&imm Rc<Ship>` across threads

`Rc::clone(&self)` increments a non-atomic count living in the `RcBox`. That non-atomic increment is the entire reason `Rc: !Sync`. Under `&imm`:

- `.clone()` needs `&Rc<Ship>`; `&imm Rc<Ship>` won't coerce to it, so `clone` is simply unnameable. You cannot touch the count.
- You *can* project `&imm Rc<Ship> → &imm Ship` and read the ship.
- Meanwhile the owning thread may still `clone`/`drop`, which writes *only* the count. Count and payload are disjoint, non-overlapping fields of the `RcBox`, so "owner clones / borrower reads the hull" is two accesses to different memory locations — not a data race even in principle.
- The borrowed handle keeps `strong ≥ 1`, so the box is never freed under the reader.

So the thing that looked like it needed a special "forbid clone" rule needs no special rule at all. It falls out of the non-coercion.

### The load-bearing lemma

`&imm` and `&shared` can coexist pointing at the same object *only because* their byte-sets are disjoint: frozen bytes (readable via `&imm`) and `UnsafeCell` bytes (mutable via `&shared`) never overlap within a type, and enum discriminants can't change under shared access. That disjointness is what a formalization must actually prove — it's the property that lets the owning thread keep mutating the refcount while the reader reads the payload.

### Prior art worth reading

- **Pony's reference capabilities** (`iso`, `val`, `ref`, `box`, `tag`, `trn`). Rust's `&T` ≈ Pony's `box` (locally readable, not sendable). The capability you're building is Pony's **`val`**: deeply immutable and therefore sendable. Pony makes `val` work by enforcing deep immutability through the whole type system — that's the cost of admission.
- **`core::marker::Freeze`.** Rust already has this internal marker ("no `UnsafeCell` in the representation"), used to promote values into read-only static memory. It's unstable/internal and not a public bound. Note `Rc<T>` *is* `Freeze` (its bytes are just a pointer) yet still `!Sync` — the clean demonstration that shallow "no interior mutability in my bytes" ≠ "safe to share." `&imm` needs *deep/transitive* immutability, not just shallow `Freeze`. The predicate exists; what's missing is the reference qualifier that consumes it.

## 3. The strong form: make `imm` the default and delete unsynchronized interior mutability

Now the more radical move the design invites: make immutable-when-shared the *default* and remove the unsynchronized escape hatch. The key realization that makes this coherent rather than crippling:

> The line to cut is not "interior mutability: yes/no." It is **synchronized vs. unsynchronized** interior mutability. Only the unsynchronized half must go.

### What dies and what survives — and why the split is principled

- **Casualties:** `Cell`, `RefCell`, `OnceCell`, non-atomic `Rc`. Exactly the **`!Sync`** interior-mutable types.
- **Survivors:** `Mutex`, `RwLock`, `Atomic*`, channels, `Arc`. Exactly the **`Sync`** interior-mutable types.

That is not an ad-hoc exception list. It's the definition of `Sync` doing its job — `Sync` always meant "this type's interior mutability is thread-safe." So the rule collapses to one line:

> **Interior mutability is permitted iff the type is `Sync`.**

`Mutex`/`RwLock`/atomics self-serialize, so sharing `&imm Mutex<T>` across threads is fine: both threads call `.lock()`, mutual exclusion does its job, no race. These are the primitives that *don't* have to go. So the resulting language keeps all of "fearless concurrency" — `Arc<Mutex<T>>`, `RwLock`, atomics, channels — intact. It is **not** message-passing-only Erlang-in-Rust-syntax.

`UnsafeCell` itself isn't deleted; it still exists inside the compiler/stdlib to *implement* the blessed primitives. What changes is that it stops being a public composable primitive any library can wrap to mint a *new* `!Sync` cell. You may use the blessed synchronized primitives; you may not hand-roll a `Cell`, and you can't build a novel lock-free structure in safe code — that power centralizes into std. (Whether that's acceptable depends on how much you value "std is just a library you could rewrite.")

### What happens to `Sync`

It doesn't cease to exist — it gets **promoted**. Today `Sync` is an auto-trait describing *who may be shared*. In this world it becomes the *permission token required to have interior mutability at all*. Same predicate, different job — it moves up the stack.

And `&imm T: Send` loses its **data-race** failure mode entirely, because every mutable byte reachable through `&imm` now lives behind something that synchronizes itself. What remains is pure **thread-affinity**: types that are `!Send` because a resource is pinned to its thread (a lock guard that must unlock on the locking thread, an FFI handle, a `!Send` foreign object). That hazard is real and orthogonal — it's what `Send` already tracks. So the honest statement isn't "`Sync` becomes always-true"; it's that **`Sync` converges toward `Send`**. Once unsynchronized interior mutability is gone, the only reason left to restrict sharing is the same reason you'd restrict moving. The split narrows to almost nothing but doesn't fully close.

### The two-regime model (the elegant core)

Every byte of every type now lives in exactly one of two regimes, each with its own soundness argument:

| Regime | Contents | Why sharing is sound |
|---|---|---|
| **Frozen** | No interior mutability | No writer can exist while an `&imm` is outstanding — writing needs unique `&mut`/`inout`, excluded by the borrow. *Soundness by absence of writers.* |
| **Synchronized** | Inside a blessed `Sync` primitive | The primitive serializes access at runtime. *Soundness by mutual exclusion.* |

There is no third regime, because the third regime — unsynchronized interior mutability — is precisely what was deleted. Today's Rust has all three, and `Sync` exists to police the boundary between regimes 2 and 3. Delete regime 3 and that boundary becomes trivial.

## 4. The irreducible costs

These are the things that genuinely can't be rescued, stated plainly:

1. **Zero-cost single-threaded mutation is gone.** `Cell<u32>` as a free counter, `RefCell` for a single-threaded graph — these must now use a `Sync` primitive, so you pay synchronization even with one thread. Uncontended locks and relaxed atomics are cheap, but not *zero*, and `.set()` becomes a lock/guard or an atomic-with-ordering. For hot interpreter-style code that leaned on `Cell`, this is a real speed and ergonomics regression.

2. **`Rc`/`Arc` collapses to just `Arc`.** The whole point of `Rc` is skipping atomic refcounting when single-threaded. Under "interior mutability requires `Sync`," the refcount cell must be `Sync`, hence atomic, hence only `Arc` survives — unless you push refcounting into the compiler (Swift-style ARC, which then wants to prove single-threadedness to elide atomics — hard) or adopt a tracing GC. Mild upside: it removes a footgun. "Why won't my `Rc` cross the thread boundary" stops existing, because there's no non-thread-safe shared pointer to reach for.

3. **`RefCell`-style runtime-borrow graphs move to arenas + indices.** Soft cost: the ecosystem already drifts this way for performance (`slotmap`, `petgraph`, ECS all say "don't use `Rc<RefCell>`, use a flat arena and integer handles"). This is a casualty the community half-volunteers for.

### Replacement patterns

| Today | In this language |
|---|---|
| `Rc<RefCell<T>>` graphs/caches/observers | Arena + integer indices (`slotmap`/ECS style) |
| `Arc<Mutex<T>>` shared state | **Unchanged** — survives intact |
| `Cell<T>` cheap counter | `Atomic*` or a lock (pay a small cost) |
| `Rc<T>` shared ownership | `Arc<T>`, or compiler-inserted refcount/GC |

## 5. Related design in the wild

- **Hylo (formerly Val)** — "mutable value semantics." No references in the surface language at all; values are passed with conventions (`let`, `inout`, `sink`, `set`), mutation happens only through unique `inout` access, sharing is by-value with copy-on-write underneath. That's very close to "immutable-when-shared, mutable-when-unique, no interior-mutability hatch," adopted specifically to avoid borrow-checker-style aliasing reasoning. Research-stage; verify current conventions if you go deep.
- **Pony** — `val` is your deeply-immutable-sendable reference; shared-mutable state lives in actors reached by messages. Pays for dropping shared mutation with an actor model.

The contrast to hold in mind: Hylo/Pony bet on value semantics / actors. Rust bets on `&mut` + `Arc<Mutex>`. The design in this note is a *third* point: keep `Arc<Mutex>` and synchronized concurrency, but make immutable references deeply immutable and freely sendable, and repurpose `Sync` from a sharing-permission into an interior-mutability-permission.

## 6. Implementation cost (if retrofitting onto a Rust-like base)

`&imm` is a third reference *category*, and a category must be threaded through the entire language:

- A separate `Deref` story: `Deref::deref(&self) -> &Target` yields a *shared* ref, so you need an `&imm`-deref producing `&imm Target` — builtin for `&`/`Box`/`Rc`, a trait for user types.
- Method-receiver resolution and autoref.
- Closure capture modes (a new capture kind).
- Variance and a full set of subtyping/coercion rules whose *central* rule is the **absence** of `&imm → &`.
- Pattern-match field projection must produce `&imm` of fields.

That's a Pony-sized addition. You'd be retrofitting Pony's `val` onto a language whose entire library is written against the `&`/`&mut` binary. Which is the real reason to prefer designing it in from the start rather than bolting it on.

## 7. Recommendation for a new language

- If the language is **value-semantics or actor-oriented**: the strong form (imm-default, `Sync`-as-permission, two regimes) is coherent, elegant, and has working precedent. Take it.
- If the language wants Rust's **shared-state concurrency identity** intact: the two-reference form (`&imm` *alongside* `&shared`) buys unconditionally-sendable immutable references without giving up cheap single-threaded mutation — a strictly additive extension.
- Either way, **design the `&imm` deref/projection/coercion rules in from day one.** The central invariant is trivial to state (`&imm` never becomes `&shared`) and expensive to retrofit.

The whole idea reduces to one hinge: *`Sync` is only load-bearing because `&T` retains the interior-mutability API. Give immutable sharing its own reference that drops that API, and `Sync` turns into either "no bound at all" (weak form) or "the permission to have interior mutability" (strong form).*

---

## 🤖 Claude

Here's my summary of the writeup, structured around what I take to be its central moves. Let me know if I've misread any of it.

## The core observation

Rust's `&T` quietly does two unrelated jobs: it's *shared* (multiple can coexist) AND it *permits interior mutation* (through Cell/RefCell/Mutex/Atomic — anything with a `&self`-taking mutating method). The two came apart the moment interior mutability existed; the language even renamed it from "immutable reference" to "shared reference" in the docs. `Sync` exists purely to police the second job: `T: Sync ⟺ &T: Send`. If `&T` didn't have the interior-mutability escape hatch, `Sync` would have nothing to guard.

The root primitive is `UnsafeCell<T>` — the sole `!Sync` opt-out. Everything else that's `!Sync` (Cell, RefCell, OnceCell, Rc's refcount, etc.) is `!Sync` transitively because it's built on UnsafeCell.

## The proposal: introduce `&imm T`

A second reference form alongside `&T`. **The whole mechanism is one rule: `&imm` never coerces to `&`.** Everything else falls out.

Consequence chain:
- Because `&imm` can't downgrade to `&`, it can't call any `&self` method whose receiver is `&T`.
- That `&self` API surface is precisely the interior-mutability API (`Cell::get/set`, `RefCell::borrow*`, `Rc::clone`, `Mutex::lock`, atomic ops).
- Through `&imm T`, you can only project fields (`&imm T → &imm Field`) and read the frozen bytes.
- Formally: the only reachable state through `&imm T` is the **`Freeze` substructure** of T — the bytes containing no UnsafeCell.
- Those bytes can't be written by anyone during the borrow: writing needs unique `&mut`/`inout` (excluded by the outstanding `&imm`), and there's no interior mutability in them by construction.
- **Therefore `&imm T: Send` unconditionally**, for all T — including `!Sync` and even `!Send` T. `Sync` was only ever guarding the interior-mut API; drop the API, drop the bound.

## The worked example (`&imm Rc<Ship>`)

- `Rc::clone` needs `&Rc<Ship>`; `&imm Rc<Ship>` won't coerce, so clone is unnameable → refcount untouchable through the imm handle.
- You *can* project `&imm Rc<Ship> → &imm Ship` and read the payload.
- Owning thread meanwhile freely clones/drops (writing only the count).
- Count and payload are disjoint fields — not a race even in principle.
- Refcount stays ≥ 1 while the handle exists → box never freed under the reader.

The load-bearing lemma: `&imm` and `&shared` coexist safely at the same object only because their byte-sets are disjoint — frozen bytes and UnsafeCell bytes never overlap within a type, and enum discriminants can't shift under shared access. Any formalization has to prove that disjointness.

## The strong form: `imm` as default, delete unsynchronized interior mutability

The key realization: the line to cut isn't "interior mutability yes/no." It's **synchronized vs unsynchronized** interior mutability. Only the unsynchronized half must go.

- **Killed:** Cell, RefCell, OnceCell, non-atomic Rc — exactly the `!Sync` interior-mutable types.
- **Survives:** Mutex, RwLock, Atomic*, channels, Arc — exactly the `Sync` interior-mutable types.

Rule collapses to one line: **"Interior mutability is permitted iff the type is Sync."** UnsafeCell doesn't disappear from the language — it stays inside compiler/stdlib to implement blessed primitives — but stops being a public composable primitive. Users can no longer hand-roll new `Cell`-shaped types or novel lock-free structures in safe code; that power centralizes into std.

Fearless concurrency (`Arc<Mutex<T>>`, RwLock, atomics, channels) survives intact — it's the *non*-synchronized interior mutability that goes. Not Erlang-in-Rust-syntax.

## What happens to `Sync`

Doesn't disappear — **gets promoted**. Same predicate, new job: from "who may be shared" to "the permission token required to have interior mutability at all." Moves up the stack.

`&imm T: Send` loses its **data-race** failure mode entirely (every mutable byte reachable through `&imm` now lives behind a self-synchronizing primitive). What remains is pure **thread-affinity** — types that are `!Send` because a resource is pinned to its thread (a lock guard whose unlock must happen on the locking thread, an FFI handle, etc.). That hazard is what `Send` already tracks and is orthogonal to the data-race question. Honest framing: **`Sync` converges toward `Send`** — the split narrows to almost nothing but doesn't fully close.

## The two-regime model

Every byte of every type lives in exactly one regime:

- **Frozen** — no interior mutability. Sharing sound because no writer can exist while `&imm` is outstanding.
- **Synchronized** — inside a blessed Sync primitive. Sharing sound because the primitive serializes access at runtime.

No third regime, because unsynchronized interior mutability was deleted. Rust today has all three; `Sync` exists to police the 2↔3 boundary; delete regime 3 and that boundary becomes trivial.

## Costs (called out honestly)

1. **Zero-cost single-threaded mutation is gone.** `Cell` as free counter, `RefCell` for a single-threaded graph — must use a Sync primitive; pay synchronization even single-threaded. Uncontended lock / relaxed atomic is cheap but not zero.
2. **Rc collapses into Arc.** Non-atomic refcount requires a `!Sync` cell to hold it; deleted. Unless compiler-inserted refcount (Swift-ARC style, then hard problem of proving single-threadedness to elide atomics) or tracing GC. Mild upside: no "why won't my Rc cross the thread boundary" footgun.
3. **`Rc<RefCell>` graphs move to arenas + indices** (slotmap/petgraph/ECS pattern) — ecosystem already drifting this way.

## Prior art

- **Pony's `val`** — deeply immutable, sendable reference capability. Pays for it via reference capabilities throughout the type system.
- **Hylo (Val)** — mutable value semantics; no references in surface language; conventions (`let`, `inout`, `sink`, `set`).
- **Rust's `core::marker::Freeze`** — internal marker, "no UnsafeCell in bytes." Rc IS Freeze (its bytes are a pointer) yet `!Sync` — proves shallow Freeze isn't enough. `&imm` needs *deep/transitive* immutability. Predicate exists; the reference qualifier that consumes it doesn't.

Note: Hylo/Pony bet on value semantics / actors; Rust bets on `&mut` + `Arc<Mutex>`; this design is a **third point** — keeps `Arc<Mutex>`-shaped concurrency, but makes immutable refs deeply immutable + always sendable and repurposes `Sync` as an interior-mut permission token.

## Implementation cost (if retrofitting)

`&imm` is a third reference category. Must thread through: a separate Deref story, method-receiver resolution / autoref, closure capture modes (new capture kind), variance + full subtyping/coercion rules whose central rule is the *absence* of `&imm → &`, pattern-match field projection producing `&imm`. Pony-sized addition. Real reason to design in from the start.

## Recommendation

- Value-semantics / actor-oriented language: take the strong form.
- Rust-shaped shared-state concurrency identity intact: two-reference form (`&imm` alongside `&`) is a strictly additive extension.
- Either way: design deref / projection / coercion rules for `&imm` from day one.

**The one-line hinge:** `Sync` is only load-bearing because `&T` retains the interior-mutability API. Give immutable sharing its own reference that drops that API, and `Sync` becomes either "no bound at all" (weak form) or "the permission to have interior mutability" (strong form).

---

## How this maps to your one-group Vale idea (my read — check me)

The connection I take you to be drawing: instead of Vale annotating groups as `read` vs `normal` up-front, have **one kind of group**, with the group's shareability derived from **whether any mutation effects occur through it** during a scope. Groups whose scope has no mut effects behave like read groups (shareable across threads); groups with mut effects behave like normal groups. The read-ness is *analyzed*, not *annotated* — analogous to how `&imm T` in the writeup gets its "no writers can exist during the borrow" guarantee from analysis (unique `&mut`/`inout` is excluded) rather than from a type-level Sync claim.

And then to fully close the loop with the strong form: Vale's stdlib restricts interior mutability to `Sync` primitives (Mutex, atomics, channels — no Cell/RefCell as separate cases, since Mutex-with-elision-via-group-analysis already covers the single-thread case per §12.7). Two regimes: frozen (no writers because analysis says so) and synchronized (Mutex/atomic serializes).

Am I reading your intent right?

---

## 👤 User

later, i want to talk with you about an alternative approach. but before we do that, here's the group borrowing article, for reference:

---
title: Group Borrowing: Zero-Cost Memory Safety with Fewer Restrictions
author: Evan Ovadia, with help from Nick Smith
date: Aug 28, 2025
realm: blog
path: blog/group-borrowing
layout: annotated
namespace: c-blog m-annotated
---


If you've read my blog before, you know that memory safety is a huge unsolved problem, and that there's a vast unexplored space between the various memory safety models. The discerning eye can infer that we're [starting to see the lines blur](https://verdagon.dev/grimoire/grimoire#the-list) between these seemingly unrelated memory safety approaches.


This is _ridiculously exciting_ to me, because today's popular memory-safe languages are very limited: they're fast, or they're flexible, but not both. Finding new blends is an incredibly challenging and worthy endeavor... one which has claimed the sanity of many explorers.


A few of us have been approaching the problem by starting with reference counting or garbage collection (or generational references!) and allowing functions to "borrow" those objects with much less--or zero--overhead. [# For example, [blending GC with arenas](https://lobste.rs/s/p4co53/simple_semi_space_collector#c_2mwkx4), [blending RC with region borrowing](https://github.com/verdagon/RegionsRCCellularAutomataBenchmark), or Vale's approach of [blending generational references with region borrowing](https://verdagon.dev/blog/generational-references), and so on.]

In my biased [# My language Vale uses one of these approaches, so I'm of course biased to see its benefits more strongly than others!] opinion, these approaches have some strong benefits. But they're not a panacea, and the world needs more approaches here.


And luckily, my good friend [Nick Smith](https://github.com/nmsmith) (from the [Mojo](https://www.modular.com/mojo) community [# Disclaimer: I work on the Mojo team at Modular! But I'll keep this post more about Nick's discovery in general, rather than how it would do in any specific language.]) has been exploring exactly that for the past few years.

I think he's found a way to add mutable aliasing directly into a borrow checker _without_ building on a foundation of reference counting, garbage collection, or generational references. In other words, an approach for zero-overhead mutable aliasing, which is a _big deal_. [# My long-time readers will recognize my cognitive dissonance here because I think such a thing is [a myth](https://verdagon.dev/blog/myth-zero-overhead-memory-safety).

Nick's approach makes me question that, though. At the very least, we're _much closer_ to achieving the myth, if he hasn't solved it completely already.]


After reading his [original explanation here](https://gist.github.com/nmsmith/cdaa94aa74e8e0611221e65db8e41f7b), I knew that it should definitely be more widely known. He's graciously allowed me to take a shot at explaining it, so here we are!


I'll try to explain the approach as simply as possible, but if you have any questions, Nick can be found in the [Mojo server](https://discord.com/invite/modular) (username nick.sm), or feel free to ask me in the [r/vale subreddit](https://www.reddit.com/r/vale/) or the [Vale discord](https://discord.gg/SNB8yGH)'s #languages channel. And if you find this interesting, consider [sponsoring Nick](https://github.com/sponsors/nmsmith)!


Also, this article is gloriously long and has a lot of context, so I'll let you know when to skip ahead.


<slice />


# Foundation: Builds on C++'s "Single Ownership"

*TL;DR:* Nick's approach is based on single ownership, like C++ and Rust. Every value is "owned" by a containing object, array, stack frame, or global.

If you know how C++ and Rust work already, [skip ahead](#recap-of-rust-borrowing)!


If you don't know, or just like reading, I'll explain what single ownership is.


For example if we have this C++ program:

```c++
#include <vector>
struct Engine { int fuel; };
struct Spaceship { unique_ptr<Engine> engine; };
void foo(vector<Spaceship>* ships) { ... }
void main() {
    vector<Spaceship> ships;
    ...
    foo(&ships);
}
```

...we can say this:

 * `main`'s stack frame "owns" `vector<Spaceship> ships;`
 * The `vector<Spaceship> ships;` owns each `Spaceship`.
 * Each `Spaceship` owns its `Engine` (via the unique_ptr).
 * Each `Engine` owns its `int fuel;`
 * `foo` does *not* own the vector<Spaceship>, it just has a raw pointer.
 * `main`'s stack frame owns `foo`'s stack frame.
 * `main` is the only thing without an owner.

If you've coded in C++ or Rust, you're probably familiar with this mindset.


If you've coded in C, you might think like this too, even though C doesn't explicitly track single ownership. If you trace an object's journey all the way from its `malloc()` call to its `free()` call, all of the variables/fields that the pointer passes through are dealing with the "owning pointer", so to speak. It's almost like how detectives track the "chain of custody" for evidence. In other words, who is responsible for it at any given moment.


Heck, even Java and C# programmers sometimes think in terms of single ownership. If you're supposed to call an object's "dispose"/"cleanup"/"destroy"/"unregister"/etc. method at some point, you can trace that object's journey all the way from `new` to that (conceptually destructive) method call, and those are the variables/fields that are handling its "owning reference", so to speak.


Single ownership, as explained so far, is the foundation for a lot of languages:

 * If you add regular (unrestricted) pointers and references, you get [C++](https://en.wikipedia.org/wiki/C%2B%2B).
 * If you add generational references and region borrowing, you get [Vale](https://verdagon.dev/blog/generational-references).
 * If you add "aliasable-xor-mutable" references, you get [Rust](https://www.rust-lang.org/).
 * If you add "group borrowing", you get [Nick's system](https://gist.github.com/nmsmith/cdaa94aa74e8e0611221e65db8e41f7b).

Nick's system is the main topic of this article, but for some context, and to know why Nick's system stands out, let's take a quick detour to recap how Rust's borrow checking works.


<slice />


# Recap of Rust Borrowing

To truly appreciate Nick's approach, it's helpful to know the limitations of Rust's borrow checker.

*TL;DR:* Rust's borrow checker has the "aliasing xor mutable" rule which makes it conservative. This means it rejects a lot of valid programs and useful patterns [# Like observers, intrusive data structures, back-references and graphs (like doubly-linked lists), delegates, etc.] and it causes accidental complexity for some use cases. [# Like mobile/web apps, games using EC, or stateful servers... generally, things that inherently require a lot of state.] If Nick's approach can solve even some of these pain points, that's a pretty exciting step forward.

If you're already familiar with Rust's limitations, [skip ahead to Nick's approach](#nicks-borrowing-system)!


If not, here's a _very_ simplified explanation of Rust's borrow checking, and I'll overview the limitations in the next section.

I'll assume some knowledge of modern C++, but if you're primarily a C programmer, check out [this post](https://verdagon.dev/blog/single-ownership-without-borrow-checking-rc-gc) instead.


There are two kinds of references: *readwrite*, and *readonly*. These are often called "mutable" and "immutable" (or more accurately "unique" and "shared") but for now, think of them as readwrite and readonly.


There are a few ways to get a readwrite reference:

 * If you own an object, you can get a single temporary "readwrite" reference for a certain scope (function body, loop body, then/else body, etc). *During this scope, you can't access the original object directly, you must use that reference.*
 * If you have a readwrite reference to an object (a struct, collection, etc.), you can get a readwrite reference to one of its fields (or elements) for a certain scope. During this scope, the original reference can't be used.


Using these is pretty restrictive. Because of that first rule:

 * You can never have two usable readwrite references to the same object in the same scope.
 * You can never have two usable readwrite references to an object and its field at the same time. In other words, while you have a readwrite reference to a `Spaceship`'s `Reactor`, you can't read or write that `Spaceship`.


Now, let's introduce "readonly" references. They operate by different rules:

 * If you have a readwrite reference, you can get any number of temporary "readonly" references for a certain scope. *During this scope, the readwrite reference is inaccessible.*
 * In a scope, if you have an readonly reference to an object (a struct, collection, etc.), you can get a readonly reference to one of its fields (or elements) for that scope.


Rust adds some quality-of-life improvements to make this a little easier. For example, you can get a bunch of immutable references directly from an owned object. It's actually not that bad if you're writing a program that inherently agrees with the rules, like compilers, games using ECS, stateless web servers, or generally anything that transforms input data to output data.


<slice />


## Context and Comments on Borrow Checking Limitations

One can't improve on a paradigm unless they know its limitations. So let's talk about borrow checking's limitations!


Because of those "inaccessible" rules, *we can never have a readwrite reference and a readonly reference to an object at the same time.* This restriction is known as "aliasability xor mutability".

In theory this doesn't sound like a problem, but in practice it means you can't implement a lot of useful patterns like observers, intrusive data structures, back-references, graphs (like doubly-linked lists), delegates, etc. and it causes accidental complexity for use cases like mobile/web apps, games using EC, or stateful servers... generally, things that inherently require a lot of state.

And besides, borrow checking is generally worth it, because it means we get memory safety without run-time overhead.


Well, mostly.

Like I explain in [this post](https://verdagon.dev/blog/myth-zero-overhead-memory-safety), it's not really free; even if you avoid `Rc`/`RefCell`/etc., borrow checking can often incur hidden costs, like extra bounds checking or potentially expensive cloning and hashing.

The borrow checker has long been known to reject programs that are actually safe, causing you to add and change code to satisfy its constraints. When this happens, one might just shrug and say "the borrow checker is conservative," but in reality, the borrow checker is imposing accidental complexity.


And besides, we know that mutable aliasing doesn't conflict with zero-cost memory safety, as we learned from the [Arrrlang](/blog/myth-zero-overhead-memory-safety) thought experiment. The only question is... can we get the best of both worlds?


Perhaps! It's not certain, even with Nick's approach. But with enough innovation in this space, I think we can get there.

<slice />


## An example

(Or [skip ahead to Nick's approach](#nicks-borrowing-system) if you understood the above!)


Here's an example ([source](https://play.rust-lang.org/?version=nightly&mode=debug&edition=2021&gist=bed04d72beb6462bc3d147ae3ebaedb5)):

```rs
struct Entity {
    hp: u64,
    energy: u64,
}
impl Entity { ... }
fn attack(a: &mut Entity, d: &mut Entity) { ... }
fn main() {
    let mut entities = vec![
        Entity { hp: 10, energy: 10 },
        Entity { hp: 12, energy: 7 }
    ];
    attack(&mut entities[0], &mut entities[1]);
}
```

Rust rejects this, giving this output:

```
error[E0499]: cannot borrow `entities` as mutable more than once at a time
  --> src/main.rs:35:35
   |
35 |     attack(&mut entities[0], &mut entities[1]);
   |     ------      --------          ^^^^^^^^ second mutable borrow occurs here
   |     |           |
   |     |           first mutable borrow occurs here
   |     first borrow later used by call
   |
   = help: use `.split_at_mut(position)` to obtain two mutable non-overlapping sub-slices
```

Alas, .split_at_mut isn't always great in practice (reasons vary) [# For example, if you need N references instead of just 2, or they don't need to be / shouldn't be distinct, or you want to still hold a reference while also holding those N references, etc.] and besides, we sometimes _want_ to have two `&mut` referring to the same object.


The more universal workaround is to use IDs and a central collection, like this ([source](https://play.rust-lang.org/?version=nightly&mode=debug&edition=2021&gist=fbf3a758a43818621a9dba537d264afc), uses [slotmap](https://docs.rs/slotmap/latest/slotmap/index.html)):

```rs
fn attack(
    entities: &mut SlotMap<DefaultKey, Entity>,
    attacker_id: DefaultKey,
    defender_id: DefaultKey
) -> Result<(), String> {
    let a = entities
        .get(attacker_id)
        .ok_or_else(|| "Attacker not found in entities map".to_string())?;
    let d = entities
        .get(defender_id)
        .ok_or_else(|| "Defender not found in entities map".to_string())?;

    let a_energy_cost = a.calculate_attack_cost(d);
    let d_energy_cost = d.calculate_defend_cost(a);
    let damage = a.calculate_damage(d);

    let a_mut = entities
        .get_mut(attacker_id)
        .ok_or_else(|| "Attacker not found in entities map".to_string())?;
    a_mut.use_energy(a_energy_cost);

    let d_mut = entities
        .get_mut(defender_id)
        .ok_or_else(|| "Defender not found in entities map".to_string())?;
    d_mut.use_energy(d_energy_cost);
    d_mut.damage(damage);

    Ok(())
}
```

This is using the slotmap crate (similar to generational_arena), though you often see this pattern with HashMap instead (or one could also use raw indices into a Vec, though that risks use-after-release problems).


If you want it to be more efficient, you might be tempted to get two mutable references up-front:

```rs
fn attack(
    entities: &mut SlotMap<DefaultKey, Entity>,
    attacker_id: DefaultKey,
    defender_id: DefaultKey
) -> Result<(), String> {
    let a = entities
        .get_mut(attacker_id)
        .ok_or_else(|| "Attacker not found in entities map".to_string())?;
    let d = entities
        .get_mut(defender_id)
        .ok_or_else(|| "Defender not found in entities map".to_string())?;
    let a_energy_cost = a.calculate_attack_cost(d);
    let d_energy_cost = d.calculate_defend_cost(a);
    let damage = a.calculate_damage(d);
    a.use_energy(a_energy_cost);
    d.use_energy(d_energy_cost);
    d.damage(damage);
    Ok(())
}
```


But alas, rustc complains:

```
error[E0499]: cannot borrow `*entities` as mutable more than once at a time
  --> src/main.rs:34:13
   |
31 |     let a = entities
   |             -------- first mutable borrow occurs here
...
34 |     let d = entities
   |             ^^^^^^^^ second mutable borrow occurs here
...
37 |     let a_energy_cost = a.calculate_attack_cost(d);
   |                         - first borrow later used here
```

...because we're mutably borrowing `entities` twice: once in `a`'s `get_mut` call, and once in `d`'s `get_mut` call, and their usages overlap.


Or, said differently, it's worried that `a` and `d` might be pointing to the same `Entity`, thus violating aliasability-xor-mutability.


But why is a compiler telling me that an `Entity` can't attack itself? That's odd, because in this game, that's totally allowed. Even pokémon can [hurt themselves in their confusion](https://knowyourmeme.com/memes/it-hurt-itself-in-its-confusion).


One might say, "because that's a memory safety risk!" But that's not necessarily true. From what I can tell, that code would be just fine, and not risk memory safety. And in fact, Nick's system handles it just fine.


So let's take a look at Nick's system!

<slice />


# Nick's Borrowing System

As I explain [Nick's system](https://gist.github.com/nmsmith/cdaa94aa74e8e0611221e65db8e41f7b), please keep in mind:

 * I'm taking some terminology liberties: the proposal calls them "regions", but here I'm describing them as "groups", mainly because I know that "regions" tends to get misinterpreted as "arenas". These are not arenas. [# I know this from experience. I regret naming Vale's regions "regions"!]
 * This proposal is from about a year ago, and Nick's been working on an even better iteration since then that's not ready yet. Subscribe to the [RSS feed](https://verdagon.dev/rss.xml) because I'll be posting about Nick's next proposal when it comes!
 * Nick's proposal is for Mojo, so the code examples are modified Mojo syntax.


Our goal is to write something like the Rust `attack` function from the last section:

```rs
fn attack(
    entities: &mut SlotMap<DefaultKey, Entity>,
    attacker_id: DefaultKey,
    defender_id: DefaultKey
) -> Result<(), String> {
    let a = entities
        .get(attacker_id)
        .ok_or_else(|| "Attacker not found in entities map".to_string())?;
    let d = entities
        .get(defender_id)
        .ok_or_else(|| "Defender not found in entities map".to_string())?;

    let a_energy_cost = a.calculate_attack_cost(d);
    let d_energy_cost = d.calculate_defend_cost(a);
    let damage = a.calculate_damage(d);

    let a_mut = entities
        .get_mut(attacker_id)
        .ok_or_else(|| "Attacker not found in entities map".to_string())?;
    a_mut.use_energy(a_energy_cost);

    let d_mut = entities
        .get_mut(defender_id)
        .ok_or_else(|| "Defender not found in entities map".to_string())?;
    d_mut.use_energy(d_energy_cost);
    d_mut.damage(damage);

    Ok(())
}
```


But we're going to write it with memory-safe mutable aliasing, so it's simpler and shorter!


A sneak peek of what it would look like:

```rs
fn attack[mut r: group Entity](
    ref[r] a: Entity,
    ref[r] d: Entity):
  a_power = a.calculate_attack_power()
  a_energy_cost = a.calculate_attack_cost(d)
  d_armor = d.calculate_defense()
  d_energy_cost = d.calculate_defend_cost(a)
  a.use_energy(a_energy_cost)
  d.use_energy(d_energy_cost)
  d.damage(a_power - d_armor)
```


<ignore>
Some initial notes:

 * `mut r: group Entity` means "a group of `Entity`s named `r` whose contents we might be modifying".
 * `ref[r] a: Entity` means "reference named `a` to a `Entity` in group `r`"
 * `a` and `d` can  refer to the same mutable variable (i.e. mutably alias each other), and the compiler knows that.
 * The above is memory safe with Nick's approach.
</ignore>



I'll explain Nick's system in four steps:

 * Basic mutable aliasing
 * References to an object vs its contents
 * Child groups, which blur that distinction a bit
 * Group annotations, which help inter-function reasoning a bit


We'll start simple, and build up gradually.

<slice />


## Basic Mutable Aliasing

Let's start by completely forgetting the difference between readonly and readwrite references. Let's say that all references are readwrite.


Now, take this simple Mojo program that has two readwrite aliases to the same list:

```rs
fn example():
    my_list = [1, 2, 3, 4]
    ref list_ref_a = my_list
    ref list_ref_b = my_list
    list_ref_a.append(5)
    list_ref_b.append(6)
```

Here's the equivalent Rust code:

```rs
fn example() {
    let mut my_list: Vec<i64> = vec![1, 2, 3, 4];
    let list_ref_a = &mut my_list;
    let list_ref_b = &mut my_list;
    list_ref_a.push(5);
    list_ref_b.push(6);
}
```

The Rust compiler rejects it because we're violating aliasability-xor-mutability, specifically in that we have two active readwrite references:

```
error[E0499]: cannot borrow `my_list` as mutable more than once at a time
 --> src/lib.rs:4:22
  |
3 |   let list_ref_a = &mut my_list;
  |                   ------------ first mutable borrow occurs here
4 |   let list_ref_b = &mut my_list;
  |                   ^^^^^^^^^^^^ second mutable borrow occurs here
5 |
6 |   list_ref_a.push(5);
  |   ---------- first borrow later used here
```


But... we humans can easily conclude this is safe. After the evaluation of `list_ref_a.push(5)`, `my_list` is still there, and it's still in a valid state. So there is no risk of memory errors when evaluating the second call to `push`.

In any language, when we hand a function a non-owning reference to an object, that function can't destroy the object, [# If a language supports temporarily destroying a live object's field, [like Mojo](https://docs.modular.com/mojo/manual/lifecycle/death/#field-lifetimes), Nick's model supports that as well. It tracks that "some object in this group is partially destroyed" and temporarily disables other potential references to that object while that's true.] nor change its type. The same is true here.

Therefore, the caller should be able to have (and keep using) other references to that object, and it's totally fine.


Nick's approach handles this by thinking about "a reference to an object" as different from "a reference to its _contents_". We can have multiple references to an object, but references into an object's _contents_ will require some special logic.

I'll explain that more in the next section.


<slice />


## When to Invalidate References to Contents

So how do we handle a caller's references to the _contents_ of the object? What kind of special logic does that require?


In the below example, the compiler should reject `print(element_ref)` because `append` might have modified the `List`.

```
fn example():
    my_list = [1, 2, 3, 4]
    ref list_ref = my_list
    ref el_ref = my_list[0]
    list_ref.append(5)
    print(el_ref)
```

It would be amazing if a memory safety approach knew that the previous example was fine and this one isn't.


In other words, the approach should know that when we hand `append` a reference to `List`, it shouldn't invalidate the other reference `list_ref`, but it should invalidate any references to its contents (like `el_ref`).


I like how Nick put it in his proposal:

> A dynamic container is a container that stores a dynamically-changing number of items, and/or items whose type changes dynamically. The two archetypal dynamic containers are resizable arrays (i.e. Mojo's List type), and tagged unions (i.e. Mojo's Variant type).

> Pointers to the items of a dynamic container need to be treated carefully, because if the container is mutated, those items may no longer reside at their original locations. There are several reasons why the items might have gone missing: the items were deleted. (e.g. a List was cleared.), the items were moved somewhere else. (e.g. a List's buffer was reallocated.), or the item has changed type. (e.g. a Variant was reassigned to a different payload type.)

> In all cases, the right action to take is to invalidate the pointer


If I had to boil it down to one sentence, I'd say: When you might have used a reference to mutate an object, _don't invalidate any other references to the object, but do invalidate any references to its *contents.*_


Following this general rule, a lot of programs are revealed to be safe.


And this isn't that crazy; if you've used C++ a lot, this likely agrees with your intuition.


Note that we'll relax this rule later, and replace it with a more accurate one. But for now, it's a useful stepping stone.


<slice />


### A More Complex Example

Above, I gave a sneak peek at an `attack` function.

Let's look at it again:

```rs
fn attack[mut r: group Entity](
    ref[r] a: Entity,
    ref[r] d: Entity):
  damage = a.calculate_damage(d)
  a_energy_cost = a.calculate_attack_cost(d)
  d_energy_cost = d.calculate_defend_cost(a)
  a.use_energy(a_energy_cost)
  d.use_energy(d_energy_cost)
  d.damage(damage)
```


For now:

 * Ignore the `[mut r: group Entity]`, we'll get to that later.
 * Know that none of these methods delete any `Entity`s. [# More precisely, these methods are only able to access the entities in group `r` by going through the variables `a` and `d`. In other words, there are no "back channels" for gaining access to the entities. This is important for memory safety and also for optimizations' correctness.]
 * Know that the `damage` call modifies `d`, and the `use_energy` calls modify `a` and `d`. Nothing else modifies anything.

(I'll explain both of those points more later.)


Note how this function isn't holding any references to `Entity`s' contents... only to whole `Entity`s.


All these methods don't delete any `Entity`s, so this `attack` function is completely memory safe. In fact, even though the `use_energy` and `damage` methods modify `Entity`s, every line in `attack` is still memory-safe. [# I'd like to remind everyone that this is all theoretical. Let me know if you have any improvements or comments on the approach!]


Let's look at this alternate example now to see it catching an actual memory safety risk.

`Entity` looks like this now:

```rs
struct Entity:
    var hp: Int
    var rings: ArrayList[Ring]
    ...
```

`attack` now holds a reference to an `Entity`'s contents, like so:

```rs
fn attack[mut r: group Entity](
    ref[r] a: Entity,
    ref[r] d: Entity):
  ref ring_ref = d.rings[0] # Ref to contents

  damage = a.calculate_damage(d)
  a_energy_cost = a.calculate_attack_cost(d)
  d_energy_cost = d.calculate_defend_cost(a)
  a.use_energy(a_energy_cost)
  d.use_energy(d_energy_cost)
  ...
```


The compiler views the program like this:

<img src="/images/EntityWithHPAndRings3.png" width="100%"/>

The compiler knows that:

 * There is a `r` group (in blue).
 * There is a `r.rings.items[0]` group (in green).
 * The `r.rings.items[0]` group (green) is a child of group `r` (blue).


Now let's see what happens when we modify `d` with a call to `damage` and then try to use that `ring_ref`:


```rs
  ref ring_ref = d.rings[0] # Ref to contents

  damage = a.calculate_damage(d)
  a_energy_cost = a.calculate_attack_cost(d)
  d_energy_cost = d.calculate_defend_cost(a)
  a.use_energy(a_energy_cost)
  d.use_energy(d_energy_cost)

  d.damage(damage)
  print(ring_ref) # Invalid, should show error
```


The compiler shows an error, because one of the functions (like `damage`) might have deleted that first ring, so the compiler _should_ invalidate any references to the contents of all `Entity`s in the group.


We're really just following the rule from before: When you might have used a reference to mutate an object, _don't invalidate any other references to the object, but do invalidate any references to its *contents.*_


<slice />


## Child groups

That's a useful rule, and it can get us pretty far. But let's make it even more specific, so we can prove more programs memory-safe.


For example, look at this snippet:

```rs
  ref hp_ref = d.hp # Ref to contents

  damage = a.calculate_damage(d)
  a_energy_cost = a.calculate_attack_cost(d)
  d_energy_cost = d.calculate_defend_cost(a)
  a.use_energy(a_energy_cost)
  d.use_energy(d_energy_cost)
  d.damage(damage)

  print(hp_ref) # Valid!
```


The previous (invalid) program had a `ring_ref` referring to an element in a ring array.

This new (correct) program has an `hp_ref` that's pointing to a mere integer instead.

This is actually safe, and the compiler should correctly accept this. After all, since none of these methods can delete an `Entity`, then they can't delete its contained `hp` integer.


Good news, Nick's approach takes that into account!


But wait, how? Wouldn't that violate our rule? We might have used a reference (`damage` may have used `d`) to mutate an object (the `Entity` that `d` is pointing to). So why didn't we invalidate all references to the `Entity`'s contents, like that `hp_ref`?


So, at long last, let's relax our rule, and replace it with something more precise.


*Old rule:*  When you might have used a reference to mutate an object, don't invalidate any other references to the object's group, but do invalidate any references to its *contents*.


*Better rule:* When you might have used a reference to mutate an object, don't invalidate any other references to the object's group, but do invalidate any references to *anything in its contents that might have been destroyed*.


Or, to have more precise terms:

*Even better rule:* When you might have used a reference to mutate an object, don't invalidate any other references to the object's group, but do invalidate any references to *its "child groups"*.


So what's a "child group", and how is it different from the "contents" from the old rule?



If Entity was defined like this:

```rs
struct Entity:
    var hp: Int
    var rings: ArrayList[Ring]
    var armor: Box[IArmor]            # An owning pointer to heap (C++ "unique_ptr")
    var hand: Variant[Shield, Sword]  # A tagged union (Rust "enum")

struct Ring:
    var power: int

struct Shield:
    var durability: int

struct Sword:
    var sharpness: int

struct SteelArmor:
    var hardness: int
```


Then these things would be part of an `Entity`'s group:

 * `hp: Int`
 * `rings: ArrayList[Ring]`
 * `armor: Box[IArmor]` [# An owning pointer to heap, `unique_ptr` in C++ speak.]
 * `hand: Variant[Shield, Sword]` [# A tagged union, "enum" in Rust speak.]


However, these would be in `Entity`'s child groups:

 * The `Ring`s _inside_ that `rings` list.
 * The `IArmor` object that `armor` _points to._
 * The `Shield` or `Sword` _inside_ the `hand` variant.


For example, if we had this code:

```rs
fn attack[mut r: group Entity](
    ref[r] a: Entity,
    ref[r] d: Entity):

  ref hp_ref = d.hp
  ref rings_list_ref = d.rings
  ref ring_ref = d.rings[rand() % len(d.rings)]
  ref armor_ref = d.armor[]  # Dereferences armor pointer

  match ref d.hand:
    case Shield as ref s:
      ...
```


Then these are the groups the compiler knows about:

<img src="/images/EntityPossibleGroups.png" width="100%"/>


Some observations:

 * `d`, `hp_ref`, and `rings_list_ref` all point to the `r` group (in blue).
 * `ring_ref` points to the `r.rings.items[*]` group (in green). That group represents _all_ the rings, because the compiler doesn't know the index `rand() % len(d.rings)`. This is different than the `r.rings[0]` from before.
 * `armor_ref` points to the `r.armor[]` group (in red).
 * `s` points to the `r.hand.Shield` group (in yellow). [# This doesn't have an "(owns)" arrow because in Mojo (which Nick's proposal was for), a `Variant` is a tagged union, which holds its data inside itself, rather than pointing to its data on the heap.]


As a user, you can use this rule-of-thumb: _any element_ of a `Variant` or a collection (`List`, `String`, `Dict`, etc) or `Box` will be in a child group. 


! If you want to go deeper, the real rule might be something like: "a Variant's element _or anything owned by a pointer_ will be in a child group." After all, `String`/`List`/`Dict`/`Box` own things with a pointer under the hood.


That all sounds abstract, so I'll state it in more familiar terms: *if an object (even indirectly) owns something that could be independently destroyed, it must be in a child group.*


Now, let's see what happens to the groups when we add a `damage` call in. Remember: `Entity.damage` mutates the entity, so it has the potential to destroy the rings, armor, shields and/or swords that the entity is holding:


```rs
fn attack[mut r: group Entity](
    ref[r] a: Entity,
    ref[r] d: Entity):

  ref hp_ref = d.hp                              # Group r
  ref rings_list_ref = d.rings                   # Group r
  ref ring_ref = d.rings[rand() % len(d.rings)]  # Group r.rings.items[*]
  ref armor_ref = d.armor[]                      # Group r.armor[]

  match ref d.hand:
    case Shield as ref s:                        # Group r.hand.Shield
      ...
      d.damage(10)  # Invalidates refs to r's child groups
                    # Group r.rings.items[*] is invalidated
                    # Group r.armor[] is invalidated
                    # Group r.hand.Shield is invalidated

      print(hp_ref)               # Okay
      print(len(rings_list_ref))  # Okay
      print(ring_ref.power)       # Error, used invalidated group
      print(s.durability)         # Error, used invalidated group
      print(armor_ref)            # Error, used invalidated group
```


Let's look at it piece-by-piece.

<slice />


### print(hp_ref)

The `hp: Int` isn't in a Variant or a collection, so it's pointing into the `r` group (not a child group), so the compiler can let us use our reference after the `damage` method.

Or using our more familiar terms: the integer can't be independently destroyed before or after the `Entity` (its memory is inside the `Entity` after all), so it's not in a child group, so the compiler can let us use our reference after the `damage` method.

<slice />


### print(ring_ref)

Now consider `ring_ref` which points to an item in `d.rings`.

```rs
  ref ring_ref = d.rings[rand() % len(d.rings)]  # Group r.rings.items[*]
  ...
      ...
      d.damage(10)  # Invalidates refs to r's child groups
                    # Group r.rings.items[*] is invalidated
      ...
      print(ring_ref.power)  # Error, used invalidated group
```


That ring is in a collection (the `d.rings` `ArrayList`), so it's in a child group `r.rings.items[*]`, so the compiler shouldn't let us use our reference after the `damage` method.


Or using our more familiar terms: the `Ring` could be independently destroyed (such as via a `remove` or `append` call on the `ArrayList`), so it's in a child group, so the compiler shouldn't let us use our reference after the `damage` method.


So, as you can see, `hp` is in the `Entity`'s group, but a `Ring` is in a child group.

<slice />


### print(len(rings_list_ref))

Let's do a harder example. Consider the `rings_list_ref` that points to the whole `d.rings` list, rather than an individual `Ring`.


```rs
  ref rings_list_ref = d.rings  # Group r
  ...
      ...
      d.damage(10)  # Invalidates refs to r's child groups
      ...
      print(len(rings_list_ref))  # Okay
```


That `rings_list_ref` is actually pointing at group `r`, not a child group, because the `rings` `ArrayList` isn't _in_ a collection (it _is_ the collection). It's in group `r` (not a child group), which wasn't invalidated, so the compiler can let us use our reference after the `damage` method.


Or using our more familiar terms: the List itself can't be independently destroyed before or after the `Entity` (its memory is inside the `Entity` after all), so it's not in a child group, so the compiler can let us use our reference after the `damage` method.


That means `rings_list_ref` is still valid, and we can use it in that `print` call!


<slice />


### print(s.durability)

Consider `s`, which points into the `hand` variant's `Shield` value.


```rs
  match ref d.hand:
    case Shield as ref s:  # Group r.hand.Shield
      ...
      d.damage(10)  # Invalidates refs to r's child groups
                    # Group r.hand.Shield is invalidated
      ...
      print(s.durability)  # Error, used invalidated group
```


`damage` could have replaced that `Shield` with a `Sword`, thus destroying the `Shield`.


Because of that risk, the compiler invalidates all of group `r`'s child groups, and catches that `print(s.durability)` is invalid.


<slice />


### Child Groups, Summarized


To summarize all the above:

 * A Variant's element _or anything owned by a pointer_ will be in a child group.
    * In other words, a group's child group is the objects that that group owns which can be independently destroyed
    * For example, if an `ArrayList` is in a group, then its `contents` array is in its child group.
 * When someone modifies a parent group, we invalidate all references into any of its child groups.


If any of this doesn't make sense, please help us out by coming to the [Vale discord](https://discord.gg/SNB8yGH) and asking questions! I want to make this explanation as clear as possible, so more people understand it.


<slice />


## Where do groups come from?

So we know what a child group is, but how does one _make_ a group? Where do they come from?


Local variables! Each local variable has its own group. [# What about heap allocations? For example, if we had a `var x = Box[Entity](10, 10)`. In this case, the local variable `x` has a group. The Entity it's pointing to is a _child group._]


Let's look at `main`:


```rs
fn main():
    entities = List(Entity(10, 10), Entity(12, 7))
    attack(entities[0], entities[1])
```


The local variable `entities` introduces a group, containing only itself. As we've just discussed, this group contains several child groups (that are not created by local variables). When we invoke `attack`, we're passing the child group that represents the elements of the `entities` list.


Additionally, groups can be combined to form other groups. This would also work:


```rs
fn main():
    entity_a = Entity(10, 10)
    entity_b = Entity(12, 7)
    attack(entity_a, entity_b)
```


This time, when we invoke `attack`, we're passing a group that represents the "union" of the two local variables.


So, to summarize where groups come from:

 * Each local variable forms its own group.
 * Multiple groups can be combined into one.
 * Variants and collections inside a group can form child groups.



<slice />


## Isolation


There's a restriction I haven't yet mentioned: all items in a group must be mutually isolated, in other words, they can't indirectly own each other, and one can't have references into the other. In other words, in the above example, an `Entity` cannot contain a reference to another `Entity`.


With this restriction, we know that e.g. `d.damage(42)` can't possibly delete some other `Entity`, for example `a`. More generally, we know that if a function takes in a bunch of references into a group, it can't use those to delete any items in the group.


I won't go too deeply into this, but if you want an example of why this is needed, try mentally implementing an AVL tree with the system. AVL tree nodes have ownership of other nodes, so any function that has the ability to _modify_ a node suddenly has the ability to _destroy_ a node, and if nodes can be destroyed, we can't know if references to them are still valid. That would be bad. So instead, we have the mutual-isolation rule.


<slice />


## Functions' Group Annotations


Here's a smaller version of one of the above snippets.


```rs
fn attack[mut r: group Entity](
    ref[r] a: Entity,
    ref[r] d: Entity):
  ref contents_ref = a.armor_pieces[0] # Ref to contents

  d.damage(3)

  print(contents_ref) # Invalid
```


At long last, we can talk about the `[mut r: group Entity]`! These are *group annotations.* They help the compiler know that two references might be referring to the same thing. Note that the call site doesn't explicitly have to supply a group for `r`, the compiler will infer it.


The use of the group `r` in the signature of `attack`  informs the compiler that even though `d.damage(3)` is modifying `d`, this may change the value of `a`, and therefore we need to invalidate any references that exist to child groups of a.


Stated more accurately, `d.damage(3)` is modifying group `r`, so it invalidates all references that point into `r`'s child groups (like `contents_ref`).


These group annotations also help at the call site, like in this example:


```rs
fn main():
    entities = List(Entity(10, 10), Entity(12, 7))
    attack(
        entities[rand() % len(entities)],
        entities[rand() % len(entities)])
```


Specifically, this invocation of `attack` is valid, because `attack` has been declared in such a way that the arguments are allowed to alias. This information is explicit in the function signature (in `attack`), so it is visible to both the programmer and the compiler.


<slice />


### A More Complex Example


Let's see a more complex example, and introduce a new concept called a *path* which helps the compiler reason about memory safety when calling functions.


Here's our `main` function again:

```rs
fn main():
    entities = List(Entity(10, 10), Entity(12, 7))
    attack(
        entities[rand() % len(entities)],
        entities[rand() % len(entities)])
```


And here's something similar to our `attack` from before, but with a new call to a new `power_up_ring` function:

```rs
fn attack[mut r: group Entity](
    ref[r] a: Entity,
    ref[r] d: Entity):
  ref armor_ref = a.armor # Ref to a's armor

  # Modifies a.rings' contents
  power_up_ring(a, a.rings[0])

  # Valid, compiler knows we only modified a.rings' contents
  armor_ref.hardness += 2
```


As the comments say, `power_up_ring` is modifying one of `a`'s rings, and it doesn't invalidate our `armor_ref`.


To see how that's possible, let's see `power_up_ring` (note I'm taking some liberties with the syntax, a much shorter version is in a section below):

```rs
# Wielder Entity's energy will power up the ring.
# Changes the ring, but does not change the wielder Entity.
fn power_up_ring[e: group Entity, mut rr: group Ring = e.rings*](
    ref[e] entity: Entity,
    ref[rr] a_ring: Ring
):
    a_ring.power += entity.energy / 4
```


Let's unpack that `fn` line:

 * `e: group Entity` means: `e` is a group of `Entity`s. Note how there's no `mut` here.
 * `mut rr: group Ring` means: `rr` is a group of `Ring`s. This one is `mut`.
 * ` = e.rings*` means: `rr` points to `Ring`s in `e`'s `Entity`s' field `.rings`. We call this a *path.*


! Nick's original proposal doesn't design for this particular capability, where we can take an immutable parent group and a mutable child group, but we tossed around the idea offline and we think it'll work. Let us know if you see anything to improve!


With this, the caller (`attack`) has enough information to know exactly what was modified. [# Well not _exactly_. Technically, only the `.power` field is being modified, but `power_up_ring` is saying that _anything_ inside `Ring` might have changed.]


Specifically, `attack` knows that `Entity`s' `.rings` elements may have changed. Therefore, after the call to `power_up_ring`, `attack` should invalidate any references pointing into `Entity`s' `.rings` elements, but not invalidate anything else. Therefore, it should not invalidate that `armor_ref`.


Inside the function, we see a `a_ring.power += entity.energy / 4`. Note how it's:

 * Reading an `Entity`
 * Modifying a `Ring` inside the `Entity`.


The latter is also why we have `mut` in `mut rr: group Ring`; the compiler requires a function put `mut` on any group it might be modifying.


This is also something that distinguishes this approach from Rust's. Partial borrows can do some of that, but generally you can't have a `&Entity` while also having an `&mut Item` pointing to one of the `Entity`'s items.


<slice />


### Paths

I want to really emphasize something from the last section:

`mut rr: group Ring = e.rings*`


This is the key that makes this entire approach work across function calls. Whenever there's a callsite, like `attack`'s call to `power_up_ring(a, a.rings[0])`, it can assemble a full picture of whether that call is valid, and how it affects the code around it.


When compiling `attack`, the compiler thinks this:

 * The caller (`attack`) calling `power_up_ring(a, a.rings[0])`.
 * The callee's first argument's group `e`.
 * The callee's second argument's group is `e.rings*`, which corresponds to `a.rings[0]` in the caller.
 * The callee's second argument's group is `mut`, so the callee will modify things in it.
 * Therefore, the callee will modify the caller's `a.rings`'s elements' contents.


This path is how the caller knows what the callee might have modified. That's the vital information that helps it know exactly what other references it might need to invalidate.


<slice />


### Syntax

If you thought that syntax was verbose:

```rs
fn power_up_ring[e: group Entity, mut rr: group Ring = e.rings*](
    ref[e] entity: Entity,
    ref[rr] a_ring: Ring
):
    a_ring.power += entity.energy / 4
```

...that's my fault. I wanted to show what's really going on under the hood.


Nick actually has some better syntax in mind:

```rs
fn power_up_ring(
   entity: Entity,
   mut ref [entity.rings*] a_ring: Ring
):
    a_ring.power += entity.energy / 4
```

Way simpler!


<slice />


## The approach, summarized

With that, you now know all the pieces to Nick's approach. Summarizing:


*References to object vs its contents:* there's a distinction between an object and its contents. We can have as many references to an object as we'd like. Mutations to the contents will invalidate references that point into the contents, but don't have to invalidate any references to the object itself.


*Child groups* let us think a little more precisely about what mutations will invalidate what references to what contents.


*Group annotations* on the function give the compiler enough information at the callsite to know which references in the caller to invalidate.


<slice />


## Does the approach really not have unique references?

When I was learning about the approach, I was kind of surprised that it had no unique references. They seemed inevitable. [#experience] In his proposal, Nick even mentions this example:

```rs
fn foo[mut r: group String](names: List[ref[r] String]):
    p1 = names[0]
    p2 = names[1]
    p1[] = p2[]     # Error: cannot copy p2[]; it might be uninitialized.
```


The final line of the function first destroys `p1`'s pointee (implicitly, just before assigning it a new value), and then copies data from `p2`'s pointee. (By the way, postfix `[]` is Mojo-speak for dereference, so `p1[]` is like C's `*p1`)


The challenge here, as he explains, is that `p1` and `p2` might be pointing to the same object. If so, one or both of these objects might end up with uninitialized data.


His solution mentions using escape hatches in this case, like this:

```rs
fn swap[T: Movable, mut r: group T](ref[r] x: T, ref[r] y: T):
    if __address_of(x) == __address_of(y):
        return
        
    # Now that we know the pointers don't alias, we can use unsafe
    # operations to swap the targets. The exact code isn't important.
    unsafe_x = UnsafePointer.address_of(x)
    unsafe_y = UnsafePointer.address_of(y)
    
    # ...use unsafe_x and unsafe_y here to swap the contents...
```


...but this can theoretically be built into the language, like this:

```rs
fn swap[T: Movable, mut r: group T](ref[r] x: T, ref[r] y: T):
    if not distinct(x, y):
        return
    
    # ...use x and y...
```


At first, I saw this and thought, "Aha! `distinct` hints to the compiler that these are unique references!"


But... maybe not. Instead of thinking of these as unique references, you could think of this as "splitting" group `r` into two temporary distinct groups.


<slice>
#experience: Throughout the entire proposal, I was expecting the next section to talk about how we inevitably add unique references back in. And as I was thinking ahead, I kept on adding unique references in, in my tentative understanding of his model. This is the problem with being accustomed to conventional borrow checking... it makes it harder to think of any other approach.

Luckily, Nick consistently tried to understand what operations can cause pointers to dangle, and impose as few restrictions as possible while ensuring that dangling pointers are always invalidated. With that in mind, the AxM constraint never arose. It's the same mindset I used to come up with Vale's generational references + regions blend. It must be like art: design constraints lead to inspiration!
</slice>


# Comparison to Borrow Checking

Group Borrowing could be much better than borrow checking.

 * It should be much more permissive than borrow checking, and prove a lot more programs correct.
 * It should lead to better error messages. Whereas rustc gives errors about abstract borrowing violations, this model should be able to give errors that point out the actual real risks: "you can't use this pointer down here, because it might not be valid anymore because of this modification up here".
    * This is particularly important to a language like Mojo, which is designed for Python programmers. The ramp from dynamic typing to static typing to single ownership to borrowing is steep, and this could help.
 * Allowing safe mutable aliasing could lead to less people reaching for workarounds like unsafe pointers. In other words, by making safe references more expressive,  we can make systems languages more memory-safe in practice.


Though, it might also result in programs that are architecturally similar to borrow checking.

 * The mutual isolation restriction might influence our programs' data to look like trees, similar to how Rust's borrow checker does. [#tree] However, the approach has much more relaxed rules around how we access those trees from the outside (via references in local variables and arguments), which is a nice improvement.
 * A line of code that deletes something will invalidate any references to any of its child groups. A similar constraint appears in borrow checking, though it's more relaxed here.


<ignore>
A function cannot read items from a group and its parent group, unless they're both immutable. And if you want to modify anything, nobody can have access to any parent groups.
</ignore>


It might be faster than borrow checking in some cases.

 * For example, the `attack` example doesn't need to do repeated hash lookups like in Rust.
 * More generally, his approach means we can have more references, and less hashing, cloning, and bounds checking.


But it might be slower in some cases. Not having unique references means it could be challenging for the compiler to compile references to faster `noalias` [# noalias is an annotation given to LLVM to tell it that no other pointer will be observing the pointed-at data while the pointer is in scope. It helps the compiler skip some loads and stores.] pointers. Nick showed me [this article](https://developer.nvidia.com/blog/cuda-pro-tip-optimize-pointer-aliasing/) to highlight the possible speed differences, and we discussed a few promising options. Perhaps a compiler could:

 * Emit `noalias` for an argument when the argument is the only argument into a certain group.
 * Have a special kind of group where all references are guaranteed distinct.
 * Notice when the code checks (via `if`, `assert`, etc.) that all pointers into a group are distinct, and emit `noalias` then.


And this model might have downsides:

 * Since there's no such thing as a unique reference, it could lead to awkward situations, where we have to convince the compiler that two references don't alias.
    * Then again, Rust kind of has this too, in the form of .split_at_mut. It might be easier here?
 * This model hasn't been implemented yet and therefore hasn't been proven to work, of course.


So, will this be revolutionary? Perhaps! Or maybe it'll be just a surface-level improvement on borrow checking in practice. Or, it could be the key that unlocks borrowing and makes it more palatable to the mainstream.


<ignore>
## Is This Like Polonius?

Nope. Polonius is an experimental implementation of Rust's existing "aliasing xor mutability" model, that aims to fix a few bugs in today's implementation (NLL). It doesn't make Rust more expressive. (Unless you equate bug fixes with expressiveness.) More precisely: today's NLL checker suffers from a few false positives (examples are given here), where programs that don't violate the aliasing xor mutability rule are rejected by the borrow checker.

Quote from Niko Matsakis (lead designer of Rust's borrow checker):
> The first thing to note is that Polonius makes no difference from the point of view of an end-user of Rust. That is, the borrow checker ought to work the same as it would have under the NLL proposal, more or less. However, there are some subtle shifts in this proposal in terms of how the compiler thinks about your program, and that could potentially affect future language features. (Original Polonius blog post, 2018)
</ignore>


<slice>
#tree: It might not actually influence our systems into trees. I suspect that in a multi-layered architecture, upper layers can have many aliasing mutable references to objects in layers below, while still allowing people to modify those lower layers' contents.

If that turns out to be true (Nick and I are still exploring it) then he's found a way to make borrowing work for some DAG-shaped program architectures, rather than just strictly tree-shaped architectures.

On top of that, if we compose this approach with linear types, I think we can get at least halfway towards compile-time-memory-safe back-references, which would unlock a lot of things like doubly-linked lists, back-references, and delegates. TBD whether that works, but that would be pretty exciting.

I'm being a bit vague, but drop by the [Vale discord server](https://discord.gg/SNB8yGH) and I can explain my crazy thoughts a bit more.
</slice>


# Where we go from here


Where does the idea go from here? Not sure!


This idea is still new, and could evolve in a lot of different directions.

 * Maybe we'll discover some ways we can decompose it into multiple orthogonal mechanisms, like how implementation inheritance (Java's `extends`) is really just `implements`+delegation+composition. [# By this I mean, you can accomplish anything with `extends`, if you turn the base class into an interface and a struct (like Dart does), and your "subclass" would instead `implements` the interface, contain the struct in a field, and forward any calls from that interface into that struct.]
 * Maybe we'll discover that this pairs _perfectly_ with another mechanism, like reference counting (or generational references? Who knows!).
 * Maybe we'll find a different way to communicate across inter-function boundaries, so that child group invalidation can be more precisely expressed and controlled.
 * Maybe someone will find a way to make these groups (mutably) alias each other! [# This would have to be opt-in of course. Non-aliasability is a good default, because it allows the compiler to perform optimizations (e.g. keep values in registers for longer) that can actually have a dramatic impact on performance.]


In [the grimoire](https://verdagon.dev/grimoire/grimoire), I hinted about a hypothetical blend of reference counting and borrowing that we don't yet know how to make. I mention that one possible path to it will be to combine various memory safety techniques together. _This could be one of them._ 


So regardless of how well this model does on its own, it could be an amazing starting point for hybrid memory safety models. I wouldn't be surprised if one of you reads this, reads the [grimoire](LINK), and discovers a clever way to blend this with existing mechanisms and techniques. Let me know if you do, and I can write an article like this for you too!

<slice />


# Conclusion


Once you understand it, the concept is pretty simple in hindsight.


Of course, it pains me to say that "it's simple", because it makes it seem like it was easy to discover. I know from personal experience just how hard it is to come up with something like this... it takes a _lot_ of thinking, trial and error, and bumping into dead ends. [#alot]


And we must remember that Nick's model is a draft, and is still being iterated upon. As with any new model, there will be holes, and there will likely be fixes. Vale's region borrowing design fell apart and was fixed a few times yet is still standing, and Nick's model feels even cleaner than regions, so I have hope.


If there's one big thing to take away from this post, it's that we aren't done yet. There is more to find out there!


That's all! I hope you enjoyed this post. If you have any questions for Nick, he hangs out in the [Mojo server](https://discord.com/invite/modular) (username nick.sm), or feel free to ask questions in the [r/vale subreddit](https://www.reddit.com/r/vale/) or [Vale discord server](https://discord.gg/SNB8yGH).


And most importantly, if you enjoy this kind of exploration, [sponsor Nick!](https://github.com/sponsors/nmsmith)


Cheers,

- Evan Ovadia


<slice>
#alot: Designing [region borrowing for generational references](link) took me _years_. And before that, I was almost broken by 32 iterations of a (now abandoned) Vale feature called "hybrid-generational memory". Near the end there, I was so burned out on the highs and lows of breaking and repairing and improving that feature, that I almost gave up on language design entirely.

Nick told me he's gone through a similarly grueling experience trying to nail down a design for his "groups". I'm glad he stuck with it!
</slice>


---

## 🤖 Claude

Read it in full. Key pieces I'm holding in context for the discussion:

- **Groups are formed at local variables** (not annotated up front like Vale's older regions). Combining groups + child groups happen inline.
- **The core rule**: modifying a group invalidates refs into its **child groups** (Variant payloads, collection elements, Box/pointer contents — anything independently destructible), but NOT refs to the group itself. That's what makes multiple readwrite aliases to an `Entity` sound.
- **Mutual isolation** among items in a group (no cycles / cross-item refs) is the load-bearing restriction that keeps mutation from being able to delete a sibling.
- **Function signatures use paths** (`e.rings*`) with `mut` markers to declare which child groups a callee touches; the caller assembles the "what got invalidated" picture at the call site.
- **No unique references.** The `distinct(x, y)` idiom (or `if not distinct(x, y): return`) is the analog of `split_at_mut` — expressed as splitting a group into two distinct sub-groups, not as a separate unique-ref type.
- **Simpler surface syntax**: `mut ref [entity.rings*] a_ring: Ring`.

Also holding: your framing in the doc + convo-2 — group-borrowing is the locked direction but completely unimplemented (ground-breaking in the coming months); §11.1–§11.9 are provisional region-framing; §11.10/§11.11 are the two subsections we've committed conditionally.

Ready when you want to bring up the alternative.

---

## 👤 User

**ALTERNATIVE**: let's talk about the alternative now.

i'll say the big pieces first, and we can walk through the smaller pieces together after.

 * when a group has no mut effects mentioned in a func, it is truly *not* changed by anything in that function. that function sees that group as truly immutable.
 * there's no RefCell or Cell escape hatch for that fact.
 * when a group has mut effects mentioned in a func, we know it's only visible to a single thread.
 * when a group has no mut effects mentioned in a func, it can be shared among multiple threads

that's the foundation. concisely, thoughts so far?

---

## 🤖 Claude

Concise thoughts:

**What this buys you (real wins):**

- **Sync collapses at the boundary.** Refs from no-mut-effects groups can go to Rust honestly as `&T + Send + Sync` — Vale's "truly immutable" is *stronger* than Rust's Sync. The α/β open question in the doc mostly resolves in β's direction with a much stronger justification (Vale's guarantee is real, not just per-type field-walk auto-derive).
- **ValeImm wrapper simplifies or disappears.** Bare `&T` from a no-mut-effects group already carries the seamless-fearless claim; you only need the wrapper for lifetime-bounding, not for Send/Sync.
- **Cell / RefCell / Mutex-as-RefCell disappear as stdlib patterns.** With-mut-effects groups + direct mutation already give you zero-cost single-thread interior mutability (compiler tracks it via group effects). Nothing extra needed.

**What it costs (real losses):**

- **Cross-thread shared mutation (Arc<Mutex<T>>) isn't expressible** without a special dispensation. If `.lock()` counts as a mut effect on the Mutex, then Mutex must live in a with-mut-effects group → single-thread → defeats the purpose. If Mutex gets a compiler-blessed dispensation (its `.lock()` is a coordinated ownership transfer, not a group-effect mutation), then it works but at the cost of a special case. Otherwise you're actor/channel-only for cross-thread mutable state — defensible ("stronger safety than Rust") but a real ecosystem shift.

**What needs pinning:**

- **Rust `&T` imports.** Under Nick's model I said universal-lift-to-read-group. Under this alternative, Rust's `&T` allows Cell/atomic/Mutex — Rust doesn't guarantee "truly immutable." So the universal lift breaks. Options: (A) lift to no-mut-effects only if T is Rust-Sync-and-no-interior-mut; else lift to with-mut-effects (single-thread); (B) always lift to with-mut-effects (losing cross-thread from incoming refs). Resurrects a boundary Sync-check but simpler than Nick's per-method version.

**Nick-vs-alternative in one line:** you're trading "read groups permit synchronized-mutation escape hatches" for "no-mut-effects means truly immutable, period." The gain is a clean cross-thread story and a stronger boundary; the cost is losing shared-mutable-state as a first-class expressible pattern.

The Mutex/atomic question is the load-bearing thing to resolve next.

---

## 👤 User

**ALTERNATIVE**: let's talk about the alternative now.

i'll say the big pieces first, and we can walk through the smaller pieces together after.

 * when a group has no mut effects mentioned in a func, it is truly *not* changed by anything in that function. that function sees that group as truly immutable.
 * there's no RefCell or Cell escape hatch for that fact.
 * when a group has mut effects mentioned in a func, we know it's only visible to a single thread.
 * when a group has no mut effects mentioned in a func, it can be shared among multiple threads

one big exception to the above:

 * Mutex, Channels, atomics (basically anything rust regards as Sync) is actually an exception to the above immutability. even if a vale reference is into a group with no mut effects, it can still use mutex/channels/atomics/etc.


some more details:

 * when rust gives a &mut as an argument to a vale function, the vale function receives it as an anonymous group with a mut effect on that whole object.
 * when rust gives a & as an argument to a vale function, the vale function receives it as an anonymous group with no effects. the vale function will truly not modify it.
 * when vale has a reference into a group with no mut effects, and gives it to a rust function, the rust function should receive it as a &.
 * when vale has a reference into a group with mut effects, and gives it to a rust function, valec enforces that it's temporarily the *only* reference to that object, and the rust function receives it as a &mut.
 * I think we don't need anything like ValeImmRef.

i think this approach does the same thing the other one did with Send.

i'm not sure what we'll do about Sync in this approach. maybe itll be easier? don't know.

that's the foundation. concisely, thoughts so far?

---

## 🤖 Claude

The model reads as Rust's `&`/`&mut` semantics **applied at the group level with effect tracking as the discriminator**, plus one stronger property: no Cell/RefCell escape hatch. That's a real simplification over the current doc, which introduced a separate "read group" annotation on top of effect tracking — this collapses both into one concept.

**What I see as the wins:**

- **One concept instead of two.** No read/normal annotation vocabulary; the mut effect IS the classifier. Closer to what Nick's actual article describes than the current doc's "read group" surface language.
- **ValeImmRef genuinely retires.** Because Vale's no-mut claim is TRULY immutable (not "Vale won't unsync-mutate but Cell is fine"), it maps honestly to Rust's `&T` semantics. No wrapper needed for the immutability projection; Vale's promise and Rust's promise line up.
- **Rust → Vale imports get honest.** Under the current doc, incoming Rust `&T` lifts to a "Vale won't mutate" read group but Rust might mutate globally via interior mutability. Under the alternative, "no effects" is symmetric with what Rust promises for its own `&T`.
- **Vale → Rust boundary is symmetric.** `&T` for no-mut, `&mut T` for mut. No wrapper. Standard Rust patterns work.

**Three questions before I have a clean model:**

1. **Cell/RefCell status.** If there's no escape hatch through no-mut groups, do Cell/RefCell exist in Vale stdlib at all? If they exist, they're only usable in mut-effect scopes — so what's their advantage over just... mutating directly through the mut group? Are they there purely for `unsafe { Cell::from_mut }`-style precision optimization? Or do we drop them entirely (and rely on Mutex + elision-to-`get_mut` per §12.7 for interior mutability)?

2. **Multi-mut intra-Vale.** Current §11.5 allows multiple `&G mut T` intra-Vale with typechecker tracking visibility. You said the Rust boundary requires "temporarily the only reference to that object." Does that mean multi-mut intra-Vale survives (with boundary-time tightening as today), or does the alternative go further and say a mut ref is universally single-visible (intra-Vale AND at boundary)? The latter is Rust's rule verbatim; the former keeps Vale's expressiveness advantage.

3. **Sync at the Rust boundary.** The alternative doesn't automatically resolve α/β. Even though Vale's no-mut claim is stronger than Rust's Sync (no Cell surface), Rust still expects `&T: Send` requires `T: Sync` and doesn't know about Vale's stronger claim. Vale still has to decide whether to emit `unsafe impl Sync for T` (β) or leave everything `!Sync` and force wrapper detours (α). Unless — the fact that Vale never Cell.set-through-`&`s means auto-derive Sync (β) is now airtight from Vale's side, making β strictly better than under the previous model?

Worth reading through — but concisely, the architectural direction feels tighter than what's currently in §12.

---

## 👤 User

1. Cell is actually mostly obviated by group borrowing which handles aliasing much better. Mutex should fill the role of RefCell, as an escape hatch. uncontended mutex locks are pretty cheap. it's not quite as fast as refcell. but im also hoping that refcell would also be partially obviated by group borrowing being generally better at these things. dont get me wrong, it is a drawback. but im hoping its one that's largely mitigated.
2. intra-vale, a ref into a group that has mut effects doesnt necessarily be the only ref into that group or the only ref to that object. group borrowing is great at having multiple references to the same object and it still being mutable. intra-vale, one never really needs to have a unique reference; if something is the only accessible reference into a group then it's effectively unique and can be lowered to a restrict. but i might misunderstand the question so lmk if that doesnt clear things up.
3. defer this question for now

---

## 🤖 Claude

Answers make the model coherent. Adjusted picture:

- **Cell/RefCell deprecated-not-blocked.** Vale stdlib probably still has them (for occasional use), but group borrowing handles the aliasing cases they'd normally be reached for; Mutex + `get_mut` elision (§12.7) covers the single-thread interior-mutability case at the same runtime cost RefCell would've had (elided in single-mut-provable scopes; cheap uncontended CAS otherwise). "Not quite RefCell speed" is a real cost only when group analysis can't prove exclusive access AND there's actual contention — narrow window.

- **Multi-mut intra-Vale preserved; boundary tightens.** Same shape as current §11.5. `mut effect = single-thread-visible for this function's execution` is the invariant, but WITHIN the function, group borrowing allows multiple aliasing mut refs to the same object. At the Rust boundary, valec picks a moment where the ref-in-question is the only live one and projects `&mut T`. "Effectively unique → lowered to `restrict`" is already §11.6's story. So the alternative doesn't disturb multi-mut expressiveness intra-Vale.

**One observation from the answers:** the alternative is basically "current doc minus three pieces of over-engineering, aligned closer to Nick's actual article":
1. Drop the explicit "read group" annotation surface — replace with emergent-from-effect-tracking classification (mut vs no-mut on the group per function)
2. Drop the Cell/RefCell access-level rule (§12.6's "blocked through read-group refs") — Cell/RefCell aren't the primary interior-mutability path anyway; Mutex-with-elision fills the role; the "no escape hatch" property falls out from "Vale users don't reach for these often"
3. Drop ValeImmRef wrappers — Vale no-mut projects honestly to Rust `&T` because Vale's promise now matches Rust's semantics symmetrically

§12.7 (Mutex elision) survives essentially unchanged and actually becomes more central under the alternative. §11.11 (Rust → Vale imports) survives with the caveat softened ("Vale-side truly won't mutate" instead of "Vale-side won't unsync-mutate but Rust might via Cell").

Standing by for wherever you want to take this next — deferring Sync per your call, but flagging that whichever way we go on the alternative, the α/β question still sits at the Rust boundary independent of the intra-Vale reframing.

---

## 👤 User

1. Cell is actually mostly obviated by group borrowing which handles aliasing much better. Mutex should fill the role of RefCell, as an escape hatch. uncontended mutex locks are pretty cheap. it's not quite as fast as refcell. but im also hoping that refcell would also be partially obviated by group borrowing being generally better at these things. dont get me wrong, it is a drawback. but im hoping its one that's largely mitigated. To be clear: vale has no Cell and no RefCell. TBD what happens when vale code tries to import them from rust.
2. intra-vale, a ref into a group that has mut effects doesnt necessarily be the only ref into that group or the only ref to that object. group borrowing is great at having multiple references to the same object and it still being mutable. intra-vale, one never really needs to have a unique reference; if something is the only accessible reference into a group then it's effectively unique and can be lowered to a restrict. but i might misunderstand the question so lmk if that doesnt clear things up.
3. defer this question for now

---

## 🤖 Claude

That's coherent. Three follow-up observations:

**1. Cell/RefCell obviation is more defensible than it first sounds.** The main Rust idioms where Cell/RefCell shine:
- Cell for cheap mutation through shared refs → obviated by group-borrowing's multi-mut-in-normal-group (you just mutate; no wrapper needed).
- RefCell for single-thread mutable state behind an aliased ref → covered by Mutex with the elision-to-`get_mut` pattern (§12.7). Uncontended cost is a branch on Result plus a deref; RefCell's runtime borrow check is ~similar. Real drawback is the Mutex allocation footprint per instance (word larger than Cell), but that's small.

The remaining rough edge is `Cell<T>` in the sense of "self-referential struct where you need to swap two fields" — but that's rare and can be handled by explicit `swap`-style stdlib helpers.

**2. Intra-Vale multi-mut cleared up.** Yes, §11.5 semantics survive as-is under the alternative — multi-mut refs into the same group are allowed intra-Vale; single-visible-mut cases lower to `restrict*`. The "single reference at boundary" clause only kicks in when projecting `&mut T` to Rust (matching Rust's rule at the boundary is unavoidable). So the alternative doesn't force any change to §11.5.

**3. Rust Cell/RefCell import remains a real question worth pinning eventually.** The interesting sub-cases:
- `import rust.std.cell.Cell<T>` → Vale would need to model `Cell::set(&self, T)` as having a mut effect on the enclosing group even though it's called through `&`. That's a per-Rust-API semantic hint (similar to how Vale needs to know Mutex/channels/atomics are "synchronized primitives").
- Alternative: Vale rejects the import outright, treating Rust's Cell as inexpressible in Vale semantics. Forces users to write Vale-native equivalents (or use the `#[cfg(rust_interop)]`-gated escape hatch).
- Or: Vale accepts the import but flags any use of Cell.set as breaking the no-mut invariant if reachable from a no-mut group — this is a per-method annotation on the imported type.

None of these need answers now, but the annotation-file surface (§24) is the natural place for the metadata if Vale goes with option 3.

**Structural implication for the doc**, if you decide to lock this: the "read group" vocabulary in the current §11/§12 framing is a spitball artifact — it came out of convo-2 before the effect-tracking framing crystallized. The alternative is arguably closer to what Nick's article actually describes than what's currently written. If you commit, §11.11 / §12.5 (ValeImm) / §12.6 (read-groups-as-Sync-alt) all need rewrites; §12.7 (Mutex elision) stays; §29.9 (Sync α/β) can be deferred as you noted.

Standing by for the next thread.

---

## 👤 User

1. Cell is actually mostly obviated by group borrowing which handles aliasing much better. Mutex should fill the role of RefCell, as an escape hatch. uncontended mutex locks are pretty cheap. it's not quite as fast as refcell. but im also hoping that refcell would also be partially obviated by group borrowing being generally better at these things. dont get me wrong, it is a drawback. but im hoping its one that's largely mitigated. To be clear: vale has no Cell and no RefCell. the vale user can still import rust's Cell and RefCell, but they can't be mutated when theyre inside a mut-effect-less group, so their usefulness will be little if any in vale.
2. intra-vale, a ref into a group that has mut effects doesnt necessarily be the only ref into that group or the only ref to that object. group borrowing is great at having multiple references to the same object and it still being mutable. intra-vale, one never really needs to have a unique reference; if something is the only accessible reference into a group then it's effectively unique and can be lowered to a restrict. but i might misunderstand the question so lmk if that doesnt clear things up.
3. defer this question for now

---

## 🤖 Claude

Answers land. Consolidated picture:

**The model is Rust's `&`/`&mut` semantics lifted to the group level via effect tracking**, with intra-Vale keeping its expressiveness (multi-mut allowed, boundary tightens). Cell/RefCell get dropped from Vale stdlib in exchange for Mutex-with-elision doing the interior-mutability work. This aligns much more closely with Nick's actual article than the current doc's "read group as separate annotation" language does — the annotation collapses back into what effect tracking already computes.

**Two follow-on observations:**

1. **§12.7 (Mutex elision via `get_mut`) becomes load-bearing, not merely convenient.** Under the current locked model, if elision-to-`get_mut` doesn't fire on some pattern, users can reach for Cell/RefCell as an escape hatch. Under the alternative, there IS no escape hatch below Mutex — so elision has to work reliably wherever group analysis can prove single-mut, or perf suffers. That elevates the priority of §12.7's codegen path being airtight. The Rust-imported-Cell case is genuine but presumably rare (imported Cell is useless through no-mut groups; users would seldom reach for it).

2. **There may be a subtle gap when Vale types wrap Rust interior-mutability fields.** If a Vale struct has a `rust.std.cell.Cell<i32>` field and Vale projects that struct as `&T` to Rust (based on no-mut group status), Rust callers can call `.set()` on the Cell field through the exposed API. That violates Vale's "truly immutable during scope" claim.

   The mitigation is Vale's typechecker walking fields to determine "does this type contain interior-mutability-through-`&T` fields that Rust can reach?" — which is basically **auto-derive Sync in another vocabulary**. Under the alternative, this analysis becomes the gate for whether a Vale type CAN be projected as `&T` cross-thread-shareable to Rust. It's the same field-walk as (β), just framed as "boundary projection eligibility" instead of "emit Sync claim."

   That's why (3) — Sync at boundary — is probably the harder-than-it-looks question even under the alternative. Deferring is fine; flagging that it doesn't quietly disappear.

Where do you want to take this next — keep exploring the model, or start thinking about what a doc rewrite of §11+§12 would look like?

---

## 👤 User

1. Cell is actually mostly obviated by group borrowing which handles aliasing much better. Mutex should fill the role of RefCell, as an escape hatch. uncontended mutex locks are pretty cheap. it's not quite as fast as refcell. but im also hoping that refcell would also be partially obviated by group borrowing being generally better at these things. dont get me wrong, it is a drawback. but im hoping its one that's largely mitigated. To be clear: vale has no Cell and no RefCell. the vale user can still import rust's Cell and RefCell, but they can't be mutated when theyre inside a mut-effect-less group, so their usefulness will be little if any in vale.
2. intra-vale, a ref into a group that has mut effects doesnt necessarily be the only ref into that group or the only ref to that object. group borrowing is great at having multiple references to the same object and it still being mutable. intra-vale, one never really needs to have a unique reference; if something is the only accessible reference into a group then it's effectively unique and can be lowered to a restrict. but i might misunderstand the question so lmk if that doesnt clear things up.
3. defer this question for now

more details:

 * Rust skips the mutex lock when it does get_mut, we never skip the mutex lock. theoretically, when it's in a group with mut effects, we *could* reduce it to a simple single-threaded boolean thing like rust's RefCell, but lets not go that direction quite yet.

---

## 🤖 Claude

Understood on all three. One thing I want to flag:

**§12.7 correction needed regardless of the alternative.** As I landed it, §12.7 says Vale elides Mutex.lock to `get_mut()` when group analysis proves single-accessible. Your clarification "we never skip the mutex lock" reverses that — Vale always takes the real lock, deferring the theoretical single-thread-boolean optimization. This isn't specific to the alternative model; it's a Vale-general position I mis-inferred from the convo (you never actually confirmed the elision landed before "kill the watcher please"). Want me to fix §12.7 now, or hold until we settle on the alternative and revise §12 more holistically?

**One consequence worth naming for the alternative:** the combination of "no Cell/RefCell + always-lock Mutex" means Vale under this model gives up on zero-cost interior mutability entirely. Rust has Cell (zero cost, restricted API) + RefCell (runtime check, no lock) + Mutex (real lock). Vale under the alternative has only Mutex, always locked. Uncontended mutex is single-CAS-cheap but not free — over inner-loop iterations it adds up. You already said you're OK with the trade because group borrowing mitigates most cases where you'd reach for Cell/RefCell, and I agree that's plausible. Just wanted to name the cost explicitly so it's not sneaking up on us later — the eventual "reduce to boolean flag in mut-effect groups" optimization is what buys the cost back if it becomes a pain point.

Cell/RefCell being importable-from-Rust-but-mostly-useless is a nice affordance for interop — users can still hold Rust types that contain them; they just can't do anything mutating through no-mut-group refs. Doesn't fight the model.

Ready for the next piece.

---

## 👤 User

now ask me #3 again

---

## 🤖 Claude

Reraising #3 with the alternative in mind:

**Under the current model**, α/β were symmetric in expressiveness — (α) forced the ValeImmRef wrapper detour but the wrapper was there as an escape, so cross-thread Vale-type share from Rust works either way.

**Under the alternative, ValeImmRef is retired.** That reshapes (α):

- **(α) + alternative**: No Sync claims from Vale + no wrapper = Rust literally cannot cross-thread-share Vale types via `&T`. `tokio::spawn(move || { use(&vale_value); })` fails at Rust's compile-time trait check. No workaround at the reference level. Users would have to route through a Vale-owned mechanism (migratory async, channel, Vale-side parallel block that owns the sharing).

- **(β) + alternative**: Vale auto-derives Sync per type from fields; stub_gen emits `unsafe impl Sync for T`. Arguably cleaner under the alternative than under the current model — Vale-defined types genuinely have no ambient Cell/RefCell (imported Rust ones show up as walkable fields, so field-derive sees them and answers `!Sync` correctly), so auto-derive is airtight.

So under the alternative, (α) becomes materially more restrictive than it was — Rust cannot share Vale refs across threads at the language level, period; they'd always have to go through a Vale-owned cross-thread mechanism. Is that acceptable — i.e., are you comfortable saying "Rust should route through migratory-async / channels for cross-thread Vale value handoff, not through bare `&T` share"? Or does the alternative essentially force (β)?

---

## 👤 User

β sounds good, any downsides?

---

## 🤖 Claude

A few, most minor:

**Real downsides:**

1. **Sync surface leaks back into Vale source for edge cases.** The auto-derive covers 99% of cases without user visibility, but Rust's escape hatches (`unsafe impl Sync for MyType`, `unsafe impl !Sync for MyType`, `Box<dyn IFoo + Sync>` bounds) need Vale-source analogs for the rare override / dyn cases. So the "no Sync in Vale source" property isn't quite absolute — it's "no Sync in Vale source for the common case." Narrow but present.

2. **Discoverability**: Vale users never write Sync but Rust callers see it emitted. A Vale dev debugging "why is my type showing as Sync to Rust callers?" has to know the auto-derive is happening at stub_gen. Doc-level mitigation — one line in §12.1 saying "Sync is emitted at boundary based on field walk; not user-visible in Vale source except for overrides." Not a fundamental issue.

3. **Vale's derive must mirror Rust's auto-trait rules exactly.** If Vale's field-walk concludes Sync but Rust's auto-derive on an equivalently-shaped type would conclude !Sync (or vice versa), Vale is either lying (unsound) or being unnecessarily conservative (ergonomic loss). Requires a CI fence that spot-checks Vale-derived Sync against Rust's auto-trait rules for a corpus of type shapes. Same fence discipline you already have for Send.

4. **Locks out (α) as future option.** If later you decide you want strict "no claims from Vale, ever" honesty (e.g., some sound reason emerges that field-walk Sync isn't trustworthy enough), the reversal is a real rewrite. But under the alternative model, (α) is impractical anyway — no wrapper, no way to cross-thread-share bare `&T` — so this is largely theoretical. Worth noting only for the record.

**Not-really-downsides but worth naming:**

5. **Slight typing-pass complexity increase.** One more per-type derive pass (Sync) parallel to Send. Modest — the field-walking machinery already exists for Send.

6. **Nested Rust-type field lookups.** Vale's Sync derive on `struct Foo { r: rust.some_crate.Thing }` has to query rustc's trait system for `rust.some_crate.Thing: Sync`. Standard interop query but adds one Rust-trait-consult per field of that shape. Same shape as the corresponding Send derive.

**Non-downsides I want to flag as strengths:**

- Under the alternative, the derive is arguably **more airtight** than it would have been under the current model — Vale's own type system has no Cell/RefCell, so a walk over Vale-native fields alone always concludes Sync. The only !Sync outcomes come from imported Rust Cell/RefCell (or other Rust !Sync types) showing up as fields, which is exactly the correct answer.
- HBAB stays clean: Vale's Sync claim is genuinely Vale's real analysis, not a phantom claim. Rust receives what Vale believes to be true, and Vale believes it because it walked fields.

Nothing that should stop the lock. Want me to update the doc to lock (β) + the alternative model?

---

## 👤 User

β sounds good

---

## 🤖 Claude

β locked. Two follow-ups before I touch the doc:

**1. β-specific edits under the current model.** §12.1, §12.4, §12.5, §12.6, §26.20, §26 summary table, §29.9 all currently read "OPEN α/β." Do you want me to convert them to "β locked" now, or hold until the bigger question below resolves?

**2. Lock the alternative model in the doc too?** The alternative reshapes multiple §11-§12 sections. Rough scope of what would change:

- **§11.11** (Rust → Vale imports): switch from "any `&T` → read group / `&mut T` → normal group + mut effect" to "no-mut anonymous group / anonymous group + mut effect" — effect-based, no read/normal annotation vocabulary.
- **§12.1**: reframe the interior-mutability model — Vale has no Cell/RefCell; imported Rust Cell/RefCell are dead through no-mut groups; Mutex is the sole interior-mutability primitive.
- **§12.5 (ValeImm wrappers)**: retire the section entirely. Vale's honest Send + β-derived Sync + no Cell/RefCell means bare `&T` projection covers the boundary cleanly; no wrapper needed. This is a real architectural win — one whole mechanism gone.
- **§12.6 (Read groups as alt to Sync)**: rewrite. The current text frames "read group" as an explicit access-level annotation with a Cell/RefCell blocking rule and a Mutex-atomics escape hatch. Under the alternative, "no mut effects on the group in this function" replaces the annotation, and there's no Cell/RefCell to block (they're not in Vale stdlib).
- **§12.7 (Mutex elision)**: revert — "Vale always takes the real lock; the theoretical single-thread-boolean-flag optimization is deferred." Per your correction.
- **§11 intro / §11.5 aliasing**: probably tighten under the effect-based model but §11 is already flagged as provisional pending group-borrowing landing, so most of the region-framing can stay pending as-is.

That's a substantive rewrite, but the payoff is real (ValeImm retired outright; one interior-mutability primitive instead of three; one concept per group instead of read/normal split). Land it now, or hold until you're more confident in the alternative direction?

---

## 👤 User

yeah im pretty confident this alternative is better. please nuke the old approach and replace it with this one

---

## 🤖 Claude

Executing the alternative rewrite. Batching all the substantive edits — §11.11, §12 opener, §12.1 heading + Sync body, §12.4, §12.5, §12.6, §12.7, §26 summary table + §26.20, plus §29.9 retirement and Glossary cleanup.

---

## 🤖 Claude

Some content ended up misplaced from the earlier edit sequence. Let me clean up: remove the floating Sync paragraph after §12.4 (redundant with the §12.4 bullet), then rewrite §12.6 and §12.7.

---

## 🤖 Claude

Now three more edits: add Sync mention to §12.1 body, fix §12.5 leftover paragraph, and rewrite §12.6 with the correct exact text.

---

## 🤖 Claude

Now §12.6 rewrite (with the correct "itself" word this time) and §12.7 rewrite:

---

## 🤖 Claude

Now the tail edits — §26 summary table row, §26.20 body, §29.9 retirement, and Glossary cleanup, in parallel:

---

## 🤖 Claude

Let me verify no stragglers of retired concepts remain — grep for ValeImm, α/β references, elision claims, read-group annotation, and §29.9 references:

---

## 🤖 Claude

Rewrite complete. Doc went from 3,118 to 3,064 lines (net −54 after retiring §29.9's α/β subsection and the Glossary ValeImm entry).

**What now reads as locked:**

- **§11.11 — Rust → Vale imports**: `&T` → anonymous group with no mut effect on x (universal, no Sync check); `&mut T` → anonymous group with mut effect on the whole object. "Vale won't mutate" is a genuine promise because there's no ambient interior-mutability escape hatch to weaken it.
- **§12 opener + §12.1**: Send AND Sync both auto-derived at typing pass; no more α/β language; Sync is extra-airtight under the effect model because Vale-defined types can't hold ambient Cell/RefCell.
- **§12.4 (Why honest)**: Send and Sync both honest; escape-hatch overrides via `unsafe impl` for both.
- **§12.5 — No boundary wrapper**: ValeImm formally retired. Section explains why: effect-tracking + auto-derived Sync + no Cell/RefCell = bare `&T` projection is already honest. Vale mut refs project to `&mut T` with valec enforcing single-visible-ref at the projection call site.
- **§12.6 — Effect tracking replaces per-type Sync Vale-internally**: rebuilt around effect-based framing; no read/normal annotation vocabulary; Cell/RefCell absent from Vale stdlib; Mutex/atomics/channels as the sole interior-mutability primitives (usable through no-mut-effect groups). Notes the retirement of the read-group annotation with rationale.
- **§12.7 — Mutex always locks**: elision-to-`get_mut` explicitly noted as available in principle but NOT built in v1 per your call. Cost consequence spelled out (single CAS uncontended, matters in hot loops; deferred single-thread-flag optimization is the escape valve if needed).
- **§26.20 HBAB body + §26 summary table row**: Send and Sync both honest; no α/β; effect-tracking-airtightness noted.
- **§29.9**: retired outright.
- **Glossary ValeImm entry**: removed.

Remaining mentions of "ValeImm" or "read-group annotation" are all in explicit retirement notes — no dangling doctrine.

---

## 👤 User

re-arm watcher please, and wait for Harmonious's message

---

## 🤖 Claude

Watcher armed (bg id `b4bpw7ebs`). Will fire when a `from-*-to-Vale4-tl-*.md` lands. Standing by.

---

## 👤 User

<task-notification>
<task-id>b4bpw7ebs</task-id>
<tool-use-id>toolu_01L63KrVDzTDCNpDiQvuoHvh</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/b4bpw7ebs.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm watcher in parallel.

---

## 🤖 Claude

Surfacing before drafting a response — Harmonious's message touches on two things I need your input on.

**Harmonious asks:**
1. What's changed between the public group-borrowing write-up and what Vale's locking in?
2. Is Vale committing to Nick's *successor* iteration, or iterating independently on top of the public write-up?
3. How does the newer approach interact with `dangle`, HBAB, per-view refs?
4. Anything else about Vale's memory-safety story not in the arch doc?

**What I can answer directly from the current doc + our conversation:**
- **Interactions changed materially** — HBAB now covers Send AND Sync (β auto-derive locked), per-view refs / ValeImm retired entirely, `dangle` shape unchanged.
- **The effect-based model** we just locked is the substantive delta from what §11 currently attributes to Nick's public write-up: Vale-defined stdlib has no Cell/RefCell (closing the "read group with ambient Cell.set" gap), effect tracking on function sigs replaces read/normal annotation vocabulary, cross-thread safety semantics (no-mut-effect ⇒ shareable / mut-effect ⇒ single-thread), Mutex always locks (no `get_mut` elision in v1).
- **Rust-interop consequences** — the §11.11 / §12.5–12.7 rewrites captured these.

**What I need from you before responding:**

1. **Nick's successor iteration.** Do you know it and are we following it, or are we iterating independently on top of the public write-up? My default framing would be "iterating independently — Vale's picking specific interpretations (no Cell/RefCell in stdlib, cross-thread semantics from effect tracking) that aren't in the public write-up and aren't necessarily where Nick's successor lands." That accurate?

2. **Should I update §11's intro** while I'm at it to reflect that Vale's actual commitment is "group-borrowing-style, with specific interpretations layered on top (see §11.11, §12.5–12.7)" rather than the current "Nick's model" wording that reads as if Vale is following the public write-up literally? Small edit, and it'd make the answer to Harmonious's Q1/Q2 consistent with what the doc actually says.

Once I hear back on those two, I'll draft the reply. Watcher armed.

---

## 👤 User

nick hasnt made the successor iteration. i think Harmonious is asking about our new approach

---

## 🤖 Claude

Draft response + a small §11 intro tweak for you to eyeball before I send.

**Draft to Harmonious:**

> Setting Q5 aside noted.
>
> **Q1/Q2 (successor iteration).** Nick's "successor iteration" the article mentions hasn't been made — it's referenced as in-progress but doesn't exist yet. What Vale's actually locking is a specific interpretation *on top of* the public write-up, iterated independently. Nick may converge with something similar later or not; we're not gated on his cadence.
>
> **Q3 (what's changed).** Four substantive modifications on top of the public group-borrowing model:
>
> 1. **Cross-thread safety semantics from effect tracking.** The public write-up focuses on aliasing correctness in a single-thread context; Vale extends it: **a function that declares no mut effect on a group truly does not mutate that group during the function's execution**, so refs in that group are shareable across threads for that duration. Mut-effect access means the group is single-thread-visible for the window. Effectively Rust's `&`/`&mut` semantics applied at the group level with the effect annotation as the discriminator.
>
> 2. **No `Cell`/`RefCell` in Vale stdlib.** The public write-up doesn't rule these out; Vale explicitly does. Closes the "read group but ambient Cell.set can still fire" gap. Rust `Cell`/`RefCell` are importable but their mutating methods aren't callable through refs in a group with no mut effect on the ref. Trade-off: gives up zero-cost single-thread interior mutability; group borrowing's more permissive aliasing rules mitigate most cases at compile time.
>
> 3. **`Mutex`, atomics, channels as the sole synchronized-mutation escape hatch.** Callable through no-mut-effect refs (their mutations are properly synchronized).
>
> 4. **`Mutex` always takes the real lock in v1.** No group-analysis-driven `get_mut` elision. The single-thread-flag optimization is available in principle but not built for v1. Uncontended CAS cost accepted as small.
>
> **Q4 (interactions with prior discussion).**
>
> - **`dangle` (§11.10) — shape unchanged.** Still user-annotated, checked-not-asserted, group-borrowing's analysis validates before `#[may_dangle]` projection. DRAFD invariant unaffected.
> - **HBAB (§26.20) — Send AND Sync both auto-derived at typing pass, β locked.** The Sync α/β question is resolved. Vale's Sync claims are *extra-airtight* under this model — Vale-defined types can't hold ambient Cell/RefCell (per the no-Cell rule above), so the field-walk answer matches what a Rust auto-trait walker would compute if it could see through Vale's opacity.
> - **Per-view refs / ValeImm wrapper — retired entirely.** Under the effect-based model + no Cell/RefCell, Vale's "no mut effect on this group" is a genuinely-immutable claim that projects honestly to Rust as bare `&T`. No "Vale's proof is stronger than Rust's Sync" gap to bridge with a wrapper. Rust callers get standard `&T` / `&mut T`; the boundary is symmetric.
>
> **Q5 (anything else in Vale's memory-safety story).**
>
> Current doc reflects all of this — §11.11 pins Rust → Vale reference imports (universal lift for `&T`, mut effect for `&mut T`), §12.5 documents the wrapper retirement, §12.6 has the effect-tracking-replaces-Sync-Vale-internally story, §12.7 has the always-locks Mutex position. Cross-refs to §11.10 (dangle), §26.20 (HBAB).
>
> Cross-thread ownership *transfer* (as distinct from reference sharing) still uses the same specific stdlib mechanisms — migratory async futures, channels, `Arc<T>` — each with its own per-feature safety analysis. Send auto-derive is what those check against.
>
> Vale's memory-safety substrate is still completely unimplemented; the group-borrowing analyzer + the modifications above are the coming-months plan, not existing code. §11 intro flags this.
>
> — Vale4-tl

**Proposed §11 intro tweak** — current wording reads as if Vale is following Nick's public write-up literally. Small edit to fix:

- Current: `"Locked direction: **group borrowing** (Nick Smith's model — write-up at ...)."`
- New: `"Locked direction: **group borrowing** (built on Nick Smith's public write-up at ...), with Vale-specific interpretations layered on top — see §11.11, §12.5–12.7."`

And add one sentence at the end of that paragraph listing the interpretations concisely: cross-thread semantics from mut-effect tracking; no Cell/RefCell in stdlib; standard `&T`/`&mut T` projection with no boundary wrapper. Makes §11 intro consistent with what the rest of the doc actually says.

Ship both, adjust, or hold?

---

## 👤 User

looks good

---

## 🤖 Claude

Landing both. §11 intro edit + send to Harmonious in parallel.

---

## 🤖 Claude

Sent. §11 intro tweak landed, response delivered. Watcher (`bysne2jo1`) still armed. Standing by.

---

## 👤 User

--to-path . isnt correct, theyre at /Volumes/V/Harmonious

---

## 🤖 Claude

Right — cross-worktree needs the recipient's dir. That file landed in my worktree, not theirs. Let me clean up + resend correctly.

---

## 🤖 Claude

Delivered to `/Volumes/V/Harmonious/`. Watcher (`bysne2jo1`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>bysne2jo1</task-id>
<tool-use-id>toolu_01Nn7Bq5o6hsQ8PqZ1kiEFVH</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bysne2jo1.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm watcher.

---

## 🤖 Claude

Surfacing — Harmonious's follow-up is a sharp probing of the enforcement mechanism. Their two options ((a) hardcoded, (b) annotation) miss what I think is the actual answer, so I want to check with you before locking it in a response.

**Their concern (real):** `Cell::set(&self, val)` and `AtomicUsize::store(&self, val, ordering)` have identical Rust signatures (both `&self`, both mutate) but need to be treated differently under Vale's rule — Cell blocked, AtomicUsize allowed. Where does the distinction live?

**My analysis:** Rust's `Sync` trait already carries exactly this distinction.
- `Cell<T>` is `!Sync` (unsynchronized interior mutation)
- `AtomicUsize` is `Sync` (synchronized interior mutation)
- `Mutex<T>` is `Sync`, `RefCell<T>` is `!Sync`, `Rc<T>` is `!Sync`, `Arc<T>` is `Sync`
- Custom user types propagate via Rust's auto-derive

Rust's Sync trait means "safe to share via `&T` across threads." That's operationally equivalent to what Vale's no-mut-effect group requires: any `&T` method is either read-only or synchronized. If T:Sync, `&T` methods are safe under Vale's rule; if T:!Sync, they aren't.

So my proposed answer is a **third option (c)** that Harmonious didn't list: **enforcement is type-level via Rust's existing Sync trait**. Not hardcoded, not Vale-side annotation — Vale consults Rust's Sync trait as the source of truth.

**Consequences under this rule:**
- Cell in no-mut-effect group → all methods blocked (including `.get()`, which is technically pure-read — over-restriction, acceptable since Cell's only value proposition is `.set()` which is blocked anyway)
- Rc in no-mut-effect group → all methods blocked (Rc.clone mutates refcount unsynchronized; correct rejection; Rc requires mut-effect group)
- AtomicUsize in no-mut-effect group → all methods available
- Mutex in no-mut-effect group → `.lock()` available (returns guard with mut-effect child group per §12.6)
- Vec<T> works if T:Sync — auto-derive propagates

**Generic propagation** (Harmonious asked): `impl<T: Cache> Cache for MyCacheWrapper<T>` falls out from Rust's auto-derive — MyCacheWrapper<T> is Sync iff T:Sync via Rust's trait system. No Vale annotation infrastructure needed for this.

**Send/Sync auto-derive for Vale types** (§12.1): walks fields, consults Rust's Sync trait for each Rust-imported field type. Same source of truth. AtomicUsize field → propagates Sync; Cell field → propagates !Sync.

**Where §24 annotation files still matter:** cases where Rust's Sync claim doesn't fully capture Vale-specific needs — HRTB structure, `drops_args` markers, custom group-effect propagation on `&mut self` methods. NOT the primary Cell/RefCell block — that's Rust's Sync trait doing the work.

**Sanity checks:**

1. **Does the Sync-based rule actually hold?** T:Sync = safe cross-thread `&T` access = all `&T` methods are read or synchronized = safe under Vale's no-mut-effect rule. I think yes but want you to eyeball.

2. **Is the "Cell::get blocked too" over-restriction acceptable?** Cell used only for `.get()` gains nothing over a direct value; blocked-in-full is a fine simplification. Confirm?

3. **What about `unsafe impl Sync` on a type that actually races?** Vale trusts Rust's claim (garbage-in, garbage-out). Consistent with Rust's own trust model.

If (c) is right, this is a real architectural insight worth locking. Should I:
- (i) Send (c) as the answer to Harmonious?
- (ii) Also update §12.6 to explicitly document the Sync-based enforcement rule?

---

## 👤 User

no, i dont want Sync to be visible in valec.

i think it would be enforced like this:

cell:
fn set<g' mut, T>(self: &g'Cell<T>, val: T) { ... }
note how self is inside the g' group which is mut. this means set cannot be called from a read-only group.

AtomicUsize:
fn store<g'>(self: &g'AtomicUsize, val: usize, ordering: Ordering) {
   unsafe {
      ...
   }
}

thoughts?

---

## 👤 User

no, i dont want Sync to be visible in valec.

i think it would be enforced like this:

cell:
fn set<g' mut, T>(self: &g'Cell<T>, val: T) { ... }
note how self is inside the g' group which is mut. this means set cannot be called from a read-only group.

AtomicUsize:
fn store<g'>(self: &g'AtomicUsize, val: usize, ordering: Ordering) {
   unsafe {
      ... // mutate it in here
   }
}
note how g' doesn't have any mut effects. that makes this a read-only group. but the unsafe is used under the hood to store things anyway.

thoughts?

---

## 🤖 Claude

Elegant. Two points:

**§24 as delivery:** §24 already spec'd "Group effects of Rust methods" as an annotation shape, so this fits — Vale ships stdlib annotations for Cell/RefCell/Vec/HashMap/Arc/Mutex/atomics/channels; ecosystem builds annotations for third-party crates. No new infrastructure needed.

**Sync emission at boundary (§12.1):** current model is field walk consulting Rust's Sync trait for imported field types — matches what Rust would compute if it could see through opacity. Under your group-effect model there's a theoretically stronger claim available — all Vale-defined types Sync by construction, because group-effect enforcement rules out unsync mutation through any no-mut-effect `&T` method regardless of field content. But that path complicates boundary exposure — Vale would have to ensure it never exposes raw !Sync Rust type refs (like `&Cell<u32>`) at no-mut-effect boundaries, else Rust could bypass Vale's enforcement by calling Cell::set on the leaked ref. I lean keep the field walk for architectural simplicity — matches Rust convention, users can `unsafe impl Sync for MyType` per-type for the stronger claim. Confirm?

Nice side effect of your mechanism: **Cell::get stays callable in no-mut-effect groups** — my earlier Sync-trait rule would have blocked Cell::get as collateral, over-restricting. Per-method group-effect signatures give the right precision.

Once you confirm, I'll:
- Send Harmonious the answer (mechanism + §24 delivery + boundary field-walk unchanged)
- No §12.6 update needed (framing already matches this)
- Small §24 mention that group-effect annotations for Rust APIs are the mechanism behind §12.6's "no Cell.set through no-mut-effect groups" rule

---

## 👤 User

> Under your group-effect model there's a theoretically stronger claim available — all Vale-defined types Sync by construction, because group-effect enforcement rules out unsync mutation through any no-mut-effect &T method regardless of field content.

tell me more about this? im interested in anything that simplifies things. maybe we can later figure out how to make the boundary work.

---

## 🤖 Claude

Walking through the stronger claim:

**Core idea.** Vale's group-effect enforcement guarantees any no-mut-effect `&T` method is safe to call concurrently — no unsync mutation possible by construction. Stub_gen exposes only no-mut-effect methods through `&T`. So `&T` is safely shareable cross-thread regardless of T's field structure. Emit `unsafe impl Sync for T` universally for Vale-defined types.

**Concrete win vs field walk:**

Vale type T with a `Cell<u32>` field. Vale exposes `read_cell(&self) -> u32` — internally calls `Cell::get` (no-mut-effect per your model).
- Field walk: `Cell:!Sync` → T:!Sync. Users can't share `&T` cross-thread. Over-restriction.
- Group-effect enforcement: T:Sync. Concurrent Rust `&T` calls run concurrent `Cell::get` — same-memory reads, no concurrent writes possible (`Cell::set` is blocked), safe. Vale's analysis backs the claim.

**The one boundary complication.** Methods returning refs to Rust !Sync types leak the enforcement gap. If Vale exposes `get_cell_ref(&self) -> &Cell<u32>`, Rust receives the ref and can call `Cell::set`. Bypasses Vale's rules.

**Fix:** Vale's frontend forbids no-mut-effect methods from returning references to Rust !Sync types. Vale-defined return types are always fine (recursively Sync by construction). Rust-imported return types get consulted against Rust's Sync trait. Rule is one predicate on method signatures.

Parameter leaks aren't a concern under §11.11's rule ("Vale's promise is Vale-side won't mutate; global concurrency is Rust's problem"). Owned return values also fine — they're separate values, no shared state. Cell::clone is no-mut-effect and safe to expose; Rc::clone is mut-effect and gets rejected from no-mut methods automatically (which matches Rust's Rc:!Send anyway).

**What this actually simplifies for users:**

Sync as a type-level concept becomes fully invisible in Vale source. Users never write it, never think about it, never see error messages about it. It exists only as a stub_gen emission at the boundary, and it's always-yes for Vale-defined types.

Field walk keeps Sync as a conceptual weight — users occasionally learn what Sync means when their type unexpectedly becomes !Sync via a field. Group-effect enforcement removes that weight entirely; errors that happen at boundary crossings are framed in Vale's mut-effect terms (which users already know) rather than in a Rust type-trait concept.

**Interaction with Send.** Send stays field-walk based (unchanged). Rc field → containing Vale type is !Send. But Vale still emits Sync for it. That's a legit combination: Rust can share `&T` cross-thread (Sync suffices) but can't move T itself cross-thread (needs Send). Matches Rust semantics.

**Cost:**
- Return-type filter at Vale frontend — small implementation.
- Vale's Sync claim is stronger than Rust's auto-derive would give; Rust callers might see Sync for types they wouldn't expect from field structure. HBAB-legal though (Vale's real analysis backs it).
- If a user explicitly wants `!Sync` (some intentional single-thread design), they'd override with `unsafe impl !Sync for MyType`.

**Where the concept still lives — honestly:**
- Rust's Sync trait is consulted for the return-type filter (per method)
- Rust's Send trait is consulted for the field walk (per type, for Send emission only)
- Neither surfaces to Vale users

Feels like a real simplification. Want to lock this shape? If yes, §12.1 shifts to "Sync always-emitted for Vale-defined types + return-type filter"; §12.6 tightens on how the filter interacts with exposure; §24 unchanged.

---

## 👤 User

could vale's opacity help here?

---

## 🤖 Claude

Yes, and specifically for the input case, opacity fully handles it.

**Opacity closes the internal-field leak automatically.** Vale type T with a Cell field, Rust getting `&T`: Rust can't reach the Cell field because T is opaque per §10. Rust only calls what stub_gen exposed. Vale exposes no-mut-effect methods only, and per your model those can't internally call Cell::set (would need mut effect propagation). Cell is unreachable to Rust. Sync claim on T is honest by construction — opacity + group-effect enforcement together do the work.

**Opacity DOESN'T handle the output case by default.** Vale method returning a raw `&Cell<u32>` — Rust receives a Rust type (not opaque), calls Cell::set through it. Leak.

**Extension: apply opacity to Rust type projections at return positions.**

Instead of returning raw `&Cell<u32>`, stub_gen wraps in an opaque Vale wrapper — `ValeRef_Cell_u32<HASH>`. Rust receives the wrapper; can only call stub_gen-exposed methods; stub_gen exposes only Cell methods with no-mut-effect signatures (Cell::get, not Cell::set). Same opacity discipline §10 applies to Vale types, generalized to Rust type projections.

Discriminator for what gets wrapped: **Rust's Sync trait.** Sync types (`&str`, `&i32`, `&Vec<T: Sync>`) flow transparently, no wrapper. !Sync types (`&Cell`, `&RefCell`, `&Rc`) get wrapped.

Underlying representation: wrapper is a pointer to the original; stub_gen-emitted methods deref internally. Zero-cost projection at runtime.

**Parameters:** stay unwrapped. Rust hands Vale a raw `&Cell<u32>` in; Vale lifts to anonymous group with no mut effect per §11.11; Vale respects group effects Vale-side. Rust's global concurrency is Rust's story per §11.11's contract. Only returns need this wrapping treatment — because on return Vale is handing Rust something Vale could safely have controlled.

**Two v1 paths:**

- **Block-return** (simpler): Vale frontend rejects methods returning Rust !Sync refs from no-mut contexts. Small implementation, small user-facing restriction.
- **Wrap-return** (extends opacity to Rust type projections): stub_gen wraps at boundary; per-Rust-type wrapper generation + method exposure declarations. Aligns with §10 mental model.

Wrap-return is philosophically cleaner — same opacity discipline everywhere. Block-return ships faster.

**Either way the core simplification stands:** Sync as a type-level concept fully hidden from Vale source; all Vale-defined types Sync by construction at boundary; opacity + group-effect enforcement do the work jointly.

**One nice property of wrap-return worth naming:** the per-Rust-type wrappers are exactly the shape ValeImm wrappers had, but for Rust types instead of Vale types and only when returned from no-mut Vale methods. The mechanism we retired for Vale types (per-Vale-type opaque wrappers with controlled method exposure) reappears in a different role. Not a resurrection of ValeImm — the reasoning is inverted (Vale used to need it to project stronger claims about Vale types to Rust; now it'd be needed to project weaker access to Rust types the other way).

Lean wrap-return for the eventual model, block-return for v1 with wrap-return catalogued as v2? Or lock wrap-return now given it's philosophically the right shape?

---

## 👤 User

hold please

---

## 🤖 Claude

Standing by. Watcher (`bn7uuhejj`) armed.

---

## 👤 User

<task-notification>
<task-id>bn7uuhejj</task-id>
<tool-use-id>toolu_01SC1Kq6PgCLLhf1A2mvCKat</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bn7uuhejj.output</output-file>
<status>killed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" was stopped</summary>
</task-notification>

---

## 🤖 Claude

No response requested.

---

## 👤 User

sanity check me: a rust & now map to a vale ref-into-a-read-group? and when vale has a ref-into-a-read-group, it _cant_ call set. so if a vale object contains a cell, we cant use rust to bypass the "no modifying Cell inside read group" thing, right?

---

## 🤖 Claude

Yes — with one caveat Vale's frontend needs to enforce.

**The mostly-closed leaks:**
- Direct field access: blocked by Vale opacity (§10). Rust can't reach `t.cell` from `&T`.
- `Cell::set` via Vale-exposed method: blocked. No no-mut-effect method can internally call `Cell::set` (which needs mut effect per your signature model), so no such method exists to expose.

**The one remaining leak vector:** Vale method returning `&Cell<u32>` directly. Rust receives the ref, calls `Cell::set` on it. Bypasses Vale's enforcement.

**Fix:** Vale's frontend forbids no-mut-effect methods from returning references to Rust `!Sync` types.

Concrete cases:
- `fn read_cell(&self) -> u32` — OK (owned value out)
- `fn get_cell(&self) -> Cell<u32>` — OK (owned via `Cell::clone`; separate Cell, no shared state with Vale's)
- `fn get_cell_ref(&self) -> &Cell<u32>` — **REJECTED** at Vale compile time (returns ref to Rust `!Sync` type from no-mut context)

With that filter: your sanity check holds. Rust cannot modify the Cell inside a Vale object seen through a no-mut-effect group.

Without it: even single-thread Rust could call `.set()` on the returned Cell ref, bypassing Vale's protection. And this holds under either boundary Sync model (always-Sync-for-Vale-types OR field-walk) — the filter is a general "don't let Rust reach into interior mutability Vale is protecting" rule, not specific to the always-Sync claim.

Sanity check confirmed conditional on locking that filter.

---

## 👤 User

that filter sounds good.

explain to me why we're still walking for Sync, even though vale exposes everything to rust as Sync?

---

## 🤖 Claude

Honestly — with the filter locked, there's no strong reason to keep field walk. I originally leaned that way for "matches Rust's auto-derive convention," but since Vale users never see Sync in Vale source, that convention only matters to Rust callers reading Vale's boundary claims — and there, "Vale types are Sync by construction because Vale's group-effect enforcement guarantees safety of `&T` sharing" is a coherent statement Rust can accept.

**Under always-Sync + filter:**
- Vale users never encounter Sync (matches your stance)
- Vale-defined types with `Cell`/`RefCell`/`Rc` fields stay Sync-shareable at boundary — more permissive than field walk
- Rust callers get honest Sync claims backed by Vale's real analysis (HBAB satisfied)
- User opt-out via `unsafe impl !Sync for MyType` for intentional single-thread designs

**The only thing field walk catches that always-Sync doesn't** is implicit !Sync via a !Sync field. But under always-Sync + filter, that field can safely be there — group-effect enforcement + the return-type filter makes it OK. So field walk's "catch" would be a false positive under this model. Users who genuinely want !Sync should just say so explicitly rather than relying on field structure.

**Generics case worth naming:** `Wrapper<T>` where T might be Cell. Under always-Sync, `Wrapper<Cell<u32>>` is Sync at boundary. If Wrapper has a method `fn get_ref(&self) -> &T`, at monomorphization that becomes `&Cell<u32>` — the filter fires at instantiation time and rejects that specific method's exposure for that instantiation. Localized error: "this method can't be exposed for this T because return type would be a ref to a Rust !Sync type." More actionable than field walk's "T is not Sync, whole type unusable cross-thread."

**Send is still field walk** — different problem (ownership transfer's safety depends on field content in a way group-effect enforcement can't help with; Rc → refcount decrement race across threads, etc.). No connection to the Sync question.

So: no field walk for Sync. Always-Sync for all Vale-defined types by construction, filter closes the boundary leak.

Want me to lock always-Sync + filter, update §12.1 and §12.6 accordingly, and send Harmonious the full picture?

---

## 👤 User

yep, lock it

---

## 👤 User

yep, lock it and send

---

## 👤 User

yep, lock it and tell them

---

## 🤖 Claude

Locking always-Sync + filter. Doing the doc edits, then sending Harmonious.

---

## 🤖 Claude

Now the substantive §12.6 addition — the enforcement mechanism + boundary Sync emission with filter:

---

## 🤖 Claude

All 8 doc edits landed. Now the response to Harmonious:

---

## 🤖 Claude

Doc updates landed (8 edits across §12 and §26). Response delivered to Harmonious. Re-arming watcher.

---

## 🤖 Claude

Watcher armed (`bqdui4rbk`). Standing by.

---

## 👤 User

<task-notification>
<task-id>bqdui4rbk</task-id>
<tool-use-id>toolu_01PEyGuWTYiE57JffE1eeWmy</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bqdui4rbk.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm in parallel.

---

## 🤖 Claude

Surfacing — Harmonious's follow-up sharpens on annotation completeness. Two threads:

**Coverage subtleties (their examples), my analysis:**

- **RefCell::borrow**: yes, must be mut-effect. Mutates BorrowFlag internally; concurrent calls race. Different from Cell::get (which is truly no-mut — reads UnsafeCell without touching any counter). Stdlib annotation needs to distinguish these carefully.
- **Rc::deref**: no-mut. Returns &T via pointer without touching refcount. Safe. Filter handles returned &T against Rust's Sync trait on T.
- **&Rc<T> as parameter**: incoming Rust `&Rc<T>` lifts to no-mut group per §11.11, lifetime-bounded, Vale can't extend the borrow. Cross-thread capture blocked by Rust's own !Sync propagation through Vale's concurrency primitives (which respect Send). Filter is on RETURN not parameter — parameters don't need it because Rust's own rules already gate them.
- **Trait methods with ambient effects**: impl bodies must conform to trait's declared group effects; Vale typechecker rejects impl bodies that internally call mut-effect methods when the signature promises no-mut.

The mut-effect annotation surface is bigger than I initially framed — not just "unsync mutation methods" but also "reads that happen to touch mutable metadata like BorrowFlag." Vale stdlib annotations need careful authoring.

**Missing-annotation policy — real call needed:**

- **(a)** Assume no mut → silent UB (dead)
- **(b)** Assume mut → fail-closed at call sites
- **(c)** Refuse imports without annotation → fail-closed at import sites

Both (b) and (c) are sound; difference is UX. I lean **(c) with easy per-project override annotations** — Vale ships stdlib annotations; user projects supply local annotations for third-party crates in a project-local annotation dir; refuse imports of unannotated crates by default, escape valve for users. Matches Vale's "no shortcuts / correctness over convenience" stance. Forces ecosystem coverage to build explicitly rather than silently.

Alternative: **(b) with linter warnings** — softer adoption, weaker forcing function. Users may not realize they've been accepting overly-conservative behavior.

Which policy? And do we want to spell this out in §24 (which currently doesn't address missing-annotation behavior at all)?

---

## 👤 User

we _might_ be able to use rust's Rc<T> internally in vale, if we never expose it across the FFI boundary, and keep it hidden in vale's opacity. because yeah, the fact that rust can clone(self: &Rc<T>) is sacreligious from rust's perspective. so vale wants rust to keep its hands off of vale's internal Rc<T>s.

since vale will never expose a vale-owned Rc to rust, the question of "can rust do Rc::deref" just becomes "if vale has a Rc<Spaceship>, and rust has a handle to it, what does that look like, and does it expose a deref()?". i think the right call here is for vale to _wrap_ rust's VC in some sort of vale struct, either internally throughout vale's internals, or just on the FFI boundary.

&Rc<T> as parameter: vale should not accept these, because vale sees these as truly immutable, and rust might be modifying them. in fact, this might be a more general problem: can vale ever accept anything thats !Sync from rust? vale sees all incoming rust & as truly immutable, and rust might be violating that.

not sure on the trait thing.

thoughts on all that?

---

## 🤖 Claude

Aligning on the three:

**1. Rc wrapping + Vale-owned Rust !Sync internals stay hidden.**

Vale-owned Rc/Cell/RefCell stay behind Vale opacity — either always Vale-internal or at least wrapped at FFI. Rust never sees a raw `&Rc<Spaceship>`; only sees `&VaTeSpaceshipHandle` with stub_gen-controlled surface. Rust can't Rc::clone through the wrapper. §12.6's "no ambient interior mutability at boundary" claim holds because Vale-owned Rust !Sync types stay hidden.

**2. Refuse `&T` where T:!Sync as parameter.**

Yes, closes a real hole. §11.11 currently says universal lift regardless of T. Under always-Sync + effect-tracking, that had a gap: Vale's "truly immutable" claim depends on no Rust-side mutation, but Rust !Sync types allow interior-mutability aliasing (even single-threaded — Cell::set could interleave with Vale's reads via a Rust alias on the same thread, breaking Vale's immutability assumption).

Concrete rule: **Vale rejects any Rust `&T` parameter where T:!Sync per Rust's Sync trait.** Rust callers wanting to pass Cell/RefCell/Rc/etc. to Vale must transfer ownership or copy/clone first. Vale queries Rust's Sync trait via TyCtxt — no new annotation infrastructure needed for the check, uses Rust's own type system as source of truth.

Generalizes the concern: any incoming Rust value that could be mutated through Rust-side aliases violates Vale's promise. Rust's Sync trait is the exact predicate for "this type is safe to share via `&` without cross-thread races" — which is stronger than "no cross-alias mutation Rust-side" but is the closest existing Rust-side property. Simpler to just use it as the gate.

**3. Traits.**

My take: trait signatures declare group effects; impl bodies conform per Vale typechecker. If trait signature is under-annotated (declares no mut effect where reality has one), impl bodies calling mut-effect methods via that signature get rejected by Vale — fail-closed at impl-body typecheck time. If over-annotated, impls are conservatively constrained (users can specialize by writing a different trait or wrapping).

**Still open from previous message: missing-annotation default policy — (b) assume-mut fail-closed at call sites, or (c) refuse-imports without annotation.** Real UX call. Which way?

Doc updates once you sign off:
- §11.11 tightens: refuse &T where T:!Sync as parameter
- §12.6 mentions the wrapping pattern for Vale-owned Rust !Sync types
- §24 spells out missing-annotation policy

---

## 👤 User

could we say that rust can still send &mut !Sync things to vale? after all, if it's giving vale exclusive access, that means nobody can modify it, even if it is !Sync.

lest not talk traits or missing-annotation default policy yet

---

## 🤖 Claude

Yes, that works cleanly. `&mut T` gives exclusive access — Rust's borrow checker guarantees no other Rust alias exists during the borrow, so no cross-alias mutation can happen regardless of T:Sync. Vale's mut-effect group on the parameter aligns with "Vale is doing the mutation, exclusively" — there's no "truly immutable" claim to violate.

**Updated parameter-import rule:**

- Rust `&T` (shared): accepted only if T:Sync per Rust's Sync trait — else refused, because Rust could have other `&T` aliases mutating via interior mutability (even single-threaded, via Cell/RefCell).
- Rust `&mut T` (exclusive): accepted for any T — Rust's borrow checker guarantees no concurrent Rust alias exists during the borrow.

**Cross-thread sanity check:** `&mut T` is Send iff T:Send. If Rust sends `&mut T` across threads, only the receiving thread has it (still exclusive). Vale receives it in a mut-effect group, which is single-thread-visible per §12.6, so Vale won't try to share across Vale threads.

**Ergonomic side-effect for Rust callers:** if they need to pass a Cell/RefCell/etc. to Vale, `&mut Cell<u32>` works while `&Cell<u32>` doesn't. Natural workaround — matches Rust's own convention that mutation goes through `&mut`.

**Summary shape:**

| Rust signature | Vale accepts? | Vale-side lift |
|---|---|---|
| `&T` where T:Sync | ✓ | anon no-mut group |
| `&T` where T:!Sync | ✗ REJECTED | — (Rust could mutate via aliases) |
| `&mut T` (any T) | ✓ | anon mut-effect group |

§11.11 tightens to reflect this. Ready to apply, or want to iterate?

---

## 👤 User

are there any ways that a rust user who has a & !Sync can somehow temporarily make them & Sync? or otherwise do something that will help them here if they want to call into vale?

---

## 🤖 Claude

Not directly (Rust doesn't have a safe "promote to Sync" operation on refs), but several standard patterns give equivalent results:

**Convert to `&mut T` if they can prove exclusivity:**
- Own the outer container → use its `get_mut()` method
- `&RefCell<T>` → `let mut b = refcell.borrow_mut(); vale_fn(&mut *b);` — runtime-checks exclusivity, gives Vale `&mut T` which Vale accepts for any T. Cleanest common workaround for RefCell users, and it's runtime-checked exclusivity so no static ownership needed.

**Pass owned value instead of ref:**
- `Cell<u32>` where `u32:Copy` → `cell.get()` gives owned u32
- T:Clone → clone and pass owned T
- Any T → transfer ownership if the API takes owned

**Wrap in a Sync-safe primitive:**
- Move value into `Mutex<T>` → `&Mutex<T>` is Sync (Mutex:Sync), Vale accepts
- Or `Arc<Mutex<T>>` if shared ownership across threads is wanted

**Per-type common paths:**

| Situation | Workaround |
|---|---|
| `&Cell<T>`, T:Copy | `cell.get()` → owned T |
| `&Cell<T>`, T:Clone | `cell.clone()` → owned Cell (if Vale API accepts owned) |
| `&RefCell<T>` | `borrow_mut()` pattern → `&mut T` (runtime-checked exclusivity) |
| `&Rc<T>`, T:Sync | `&*rc` → `&T` directly (Rc::deref is safe) |
| `&Rc<T>`, T:!Sync | Recursively — get T out via Rc::deref, apply above |
| user-defined !Sync | User wraps in `Mutex` or provides `&mut` via exclusive access |

The `&mut`-via-`borrow_mut` escape hatch is the most convenient because it's runtime-checked exclusivity — Rust users don't need static ownership guarantees, RefCell's runtime borrow check gives them the exclusivity proof at runtime and produces a `&mut T` Vale will happily accept.

Want me to add a note about these workarounds to §11.11 so Rust callers hitting the T:!Sync refusal have obvious paths forward documented?

---

## 👤 User

so the only answer is to move (which requires ownership) or clone or pass &mut instead?

---

## 🤖 Claude

Yes — those three are the only paths on the Rust caller side, at least in safe Rust.

Fundamentally:
- **Move** — transfer ownership to Vale (Vale takes owned T)
- **Clone** — Rust makes a copy (via Cell::get on Copy, via Clone, whatever), Vale takes owned T
- **`&mut` instead of `&`** — exclusive access, Rust proves via ownership or runtime check (borrow_mut)

Anything else would require unsafe (transmute to a Sync wrapper, unsafe impl Sync newtype, etc.) — off the table for safe interop.

**One other angle** that lives on the Vale-API-author side rather than the Rust-caller side: **take a closure instead of `&T`**. If Vale API takes `impl FnOnce(&T) -> R` or similar, Rust caller writes `|_| use(&non_sync_t)` and passes the closure. Vale calls the closure synchronously; Rust-side access to the !Sync ref happens inside the closure body (which is Rust code, respecting Rust's rules). Vale never receives the raw `&T` directly.

This is a real pattern for cases where Vale API authors know their consumers will often have !Sync data. But it's not a workaround the Rust caller can do themselves — it requires the Vale API to accept a closure.

So: from the Rust caller's side, it's move/clone/`&mut`. From the API-design side, closure-taking APIs are an additional shape.

Want me to add this to §11.11 as a workarounds note?

---

## 👤 User

does that kind of lambda usage present any unsoundness for us? can we really call rust closures like that to get around this? same questions for traits, since accepting a closure is really just accepting a trait

---

## 🤖 Claude

**Closures — no unsoundness, safe as an escape hatch.**

Vale calling a Rust closure is like Vale calling any Rust function. The closure's captures (including `&Cell<u32>` or other !Sync refs) are Rust's business. Rust-owned state isn't in any Vale group; Vale's group-effect model applies to Vale-side data only.

Concrete trace: Rust caller writes `|| non_sync_ref.get()` and passes to Vale. Rust holds `&Cell<u32>` loaned to the closure. Closure lifetime bounds the ref's validity. Vale receives owned closure. Vale calls it; internally the closure reads through the ref. Values may change between calls (Rust caller can Cell::set between Vale calls) — Vale just sees different return values, which is normal function behavior. Vale's model doesn't assume anything about Rust-side state.

Send/Sync of the closure itself: Rust's own auto-derive. Closure capturing `&Cell<u32>` is !Send + !Sync. Vale can't spawn it cross-thread (async/parallel primitives require Send captures per Rust's rules). Vale can call it synchronously in the current function scope. Safe.

**Traits — same story for Vale CALLING trait methods; wrinkle when Vale IMPLEMENTS one.**

Vale calling a Rust trait method: same as calling any function. Trait method signature declares group effects; Vale calls per signature. If the impl body internally uses !Sync state via interior mutability, that's Rust's business.

The wrinkle: **Vale implementing a Rust trait whose method takes `&T where T:!Sync` as parameter.** E.g., `trait Foo { fn method(&self, x: &Cell<u32>) }`. Vale user writes `impl Foo for MyVaTeType`. The Vale impl body would receive `&Cell<u32>` as parameter — and our rule refuses that. So Vale can't implement such traits.

Not unsoundness — just an impl-side limitation. Users hit it with a clear compile error at the `impl` block. Traits with `&mut T` or `&T where T:Sync` parameters (the vast majority) impl fine. Generic traits like `trait Foo<T> { fn method(&self, x: &T) }` can be Vale-impl'd for T:Sync instantiations but not T:!Sync — checked at monomorphization.

**Summary:**

| Direction | Vale accepts? |
|---|---|
| Vale CALLS Rust closure/trait method | ✓ — captures/receiver state is Rust's business, no Vale-model violation |
| Vale IMPLEMENTS Rust trait with `&T where T:!Sync` param | ✗ — blocked by the parameter rule at impl-body typecheck |

Closure-passing is a legitimate escape hatch for Rust callers with !Sync data: encapsulate the !Sync access inside a closure body, pass the closure to Vale, Vale calls it without ever handling the !Sync ref directly. Genuinely useful workaround pattern for Vale API designers to know about.

---

## 👤 User

yeah vale has no way to express !Sync so vale can't implement that kind of trait...

hmm. interesting. so, if vale can't take a reference to something inside a Cell, then it should never care if something inside a Cell can change. it only ever receives copies of it. interesting. to vale, it's as if rust is magically pulling something out of thin air, and doesnt even know that its coming from inside a cell. from vale's perspective, cell could even be an index into a global magical collection somewhere else, for all vale cares. i guess this applies to refcell too? it can call into rust, and rust can lock a refcell and return to vale the contents of the refcell?

---

## 🤖 Claude

Yes, exactly — and RefCell works the same way with runtime borrow-check mechanism.

RefCell has `borrow()` / `borrow_mut()` that acquire runtime-checked borrow guards. Rust caller does:

```rust
let guard = refcell.borrow_mut();
vale_fn(&mut *guard);
// guard dropped after vale_fn returns, releasing the borrow
```

During Vale's execution, RefCell is locked — Rust code can't concurrently borrow the RefCell. Vale has coherent exclusive access to the interior T. When Vale returns, guard drops, borrow released.

Same shape for shared read: `refcell.borrow()` → shared borrow guard → Vale receives `&T` (T:Sync check applies).

**Your mental model generalized:** Rust's !Sync interior-mutability wrappers are opaque black boxes from Vale's perspective. Rust has "unwrap" operations (Cell::get, Cell::get_mut, RefCell::borrow, RefCell::borrow_mut, Rc::deref, etc.) that produce plain values or refs Vale can accept. Vale receives copies or refs; doesn't know or care that they came from inside a Cell/RefCell/Rc. Cell could genuinely be an index into a global collection, or a database handle — Vale treats the contents identically. The wrapper machinery is Rust-side implementation detail Vale never sees.

**Escape-hatch pattern this creates.** Rust API authors exposing Cell/RefCell/Rc-owned data to Vale have three shapes available:

1. **Value-returning accessors** (Cell::get for Copy; Cell::clone → owned Cell)
2. **&mut-returning accessors** (get_mut where exclusive; borrow_mut runtime-checks exclusivity)
3. **Callback-based APIs** where Rust encapsulates the wrapper access in a closure

All three keep the wrapper Rust-side and hand Vale only plain interior.

**Interesting side effect:** this makes RefCell's runtime borrow check an *interop primitive*. Rust caller uses `borrow_mut` to acquire exclusivity at runtime, then passes `&mut T` to Vale — Vale gets `&mut` with no static exclusivity proof required from the Rust side. Well-precedented pattern, plays cleanly with Vale's model.

**Atomics/Mutex don't need this dance** because they're Sync — Vale accepts `&AtomicUsize` and `&Mutex<T>` directly via the T:Sync rule. No unwrap step needed.

---

## 👤 User

got it. please add this to the doc, but put a bit note saying that this warrants further investigation, both to find soundness holes it exposes, and also to look for other opportunities this reveals about vale/rust interop and opportunities this reveals about vale as a whole.

---

## 🤖 Claude

Executing. §11.11 update with the T:Sync gate, universal &mut, escape-hatch patterns, and investigation note. Two edits.

---

## 🤖 Claude

Now the escape-hatch patterns + investigation note:

---

## 🤖 Claude

Both edits landed. §11.11 now has:

- Table with three rows: `&T where T:Sync` accepted, `&T where T:!Sync` rejected, `&mut T` (any T) accepted
- First two property bullets rewritten to reflect the T:Sync gate + universal `&mut T`
- New **Escape-hatch patterns** section with the workaround table, callback-API note, mental model, trait-impl limitation
- New **[OPEN] Further investigation warranted** call-out block covering soundness holes / interop opportunities / Vale-language opportunities

Standing by. Watcher (`bi1vxka66`) still armed.

---

## 👤 User

now, what was the stuff about traits and annotations?

---

## 🤖 Claude

Two deferred threads. Recapping and giving my current read:

**Traits — what's open beyond what §11.11 already documents.**

Already covered: Vale can't implement Rust traits whose method parameters include `&T where T:!Sync`. Users hit this at impl-body typecheck.

Still open on traits:
- **Under-annotated Rust trait signatures.** If a Rust trait is imported without Vale-side group-effect annotations on its methods, what does Vale assume? Same fundamental question as missing method annotations — see below.
- **Impl-conformance mechanics.** Trait signature declares group effects; impl body must conform. If trait signature is under-annotated in a way that mismatches semantic reality (declares no-mut but the trait's semantic contract mutates), Vale's typechecker either rejects mutating impl bodies (fail-closed) or silently accepts them (unsafe). Same fail-closed vs unsafe question.
- **Trait methods with group parameters.** `trait Modify<G> { fn modify(&self, target: &G T); }` — trait signature must declare group effects on `G` for impls to conform. Same annotation-completeness question, now applied to trait-level rather than method-level annotations.

All three trait concerns collapse to the same underlying question: **what's the default when Vale can't find a group-effect annotation for an imported Rust API surface?**

**Missing-annotation policy — real policy call.**

Harmonious's three options:
- **(a) Assume no mut effect** → silent UB when annotation is wrong-by-omission (dead option, unsafe)
- **(b) Assume mut effect** on missing annotation → fail-closed at call sites and impl-body typecheck. Softer adoption; some safe calls get blocked until users add annotations.
- **(c) Refuse imports without annotation** → fail-closed at import sites. Stricter; user can't even reference an unannotated method/trait until annotations exist. Forces per-crate coverage explicitly.

Both (b) and (c) are sound; differ in UX and ecosystem-forcing behavior.

My earlier lean was **(c) with easy per-project override annotations** — Vale ships stdlib annotations; user projects supply local annotations for third-party crates in a project-local annotation dir; refuse imports of unannotated crates by default with escape valve. Matches Vale's "no shortcuts / correctness over convenience" stance and forces the ecosystem to build annotation coverage explicitly rather than silently rely on defaults.

Alternative: **(b) with linter warnings** during import — accept unannotated APIs with mut-effect assumption, warn loudly. Softer adoption path, weaker forcing function; users may fail to notice they've been accepting overly conservative behavior.

**One concrete ask:** which policy? Same choice applies uniformly to (i) missing method annotations, (ii) missing trait signature annotations, (iii) missing group-effect annotations on trait group parameters. Once locked, I'll add a paragraph to §24 spelling out the default (currently §24 doesn't address missing-annotation behavior at all) and cross-refs from §11.11 / §12.6 where relevant.

---

## 👤 User

give me an example of the missing-annotation stuff?

---

## 🤖 Claude

Concrete example — `RefCell::borrow`:

```rust
impl<T> RefCell<T> {
    pub fn borrow(&self) -> Ref<'_, T>;  // Rust signature: takes &self
}
```

The signature says `&self` — looks like a pure read. But semantically, `borrow` mutates the RefCell's internal `BorrowFlag` (incrementing the shared-borrow count, checking no mut borrow is active). Two concurrent threads calling it race on that flag.

Vale needs an annotation to distinguish this from `Cell::get`, which also takes `&self` but is truly no-mut (reads UnsafeCell without touching any counter). The correct annotations:

- `Cell::get<g', T: Copy>(self: &g' Cell<T>) -> T` — no mut effect
- `RefCell::borrow<g' mut, T>(self: &g' RefCell<T>) -> Ref<'_, T>` — **mut effect on g'**

**Scenario when `RefCell::borrow`'s annotation is missing:**

```vale
struct Widget {
    counter: RefCell<u32>,
}

impl Widget {
    // Vale user thinks this is a pure read — but internally it mutates BorrowFlag
    func read_count(&self) -> u32 {
        self.counter.borrow().clone()
    }
}
```

Under **(a) assume no mut effect on missing annotations**: Vale accepts `read_count` as no-mut. Vale emits `unsafe impl Sync for Widget` (always-Sync by construction, §12.6). Rust shares `&Widget` across threads. Concurrent Rust threads call `widget.read_count()` — each internally invokes `RefCell::borrow`, each mutates `BorrowFlag`. **Silent race, silent UB.**

Under **(b) assume mut effect on missing annotations**: Vale rejects `read_count` as no-mut because `RefCell::borrow` is treated as mut-effect on `self.counter`'s group. User gets a compile error: "method `read_count` calls a mut-effect operation but is declared no-mut." User either restructures to make `read_count` a mut method, supplies a proper annotation for `RefCell::borrow`, or switches to `Cell`. Fail-closed at typecheck.

Under **(c) refuse imports without annotation**: Vale refuses to import `RefCell` until annotations exist. Stdlib annotations ship with Vale toolchain so RefCell wouldn't fail here — but a third-party crate whose types Vale hasn't seen would fail at the import site with "cannot import — no group-effect annotations for this API." User writes local annotations in their project's annotation dir before the import compiles.

**Real-world impact for third-party crates (tokio, hyper, serde, sled, etc.) where Vale doesn't ship annotations:**

- **(b)**: user runs into "this method needs mut-effect" errors during development; each is a signal that either the annotation's missing or the code needs restructuring. User annotates or restructures reactively.
- **(c)**: user hits "cannot import this crate — no annotations" at first `import`. Forces annotation acquisition before writing any Vale code that touches the crate. Higher up-front cost, lower per-error cost during development.
- **(a)**: same code compiles and races silently. Dead per Vale's stance.

The RefCell example specifically shows why (a) is unacceptable — the failure mode is genuine cross-thread data race on stdlib types that seem "innocent enough" to leave unannotated.

---

## 👤 User

yeah i think if vale receives a !Sync, it should assume it's coming in inside a `mut` group. thatll keep it from escaping the current thread.

if we accepted it as no mut effect, vale might accidentally farm it out to a bunch of child vale threads, and they would all be able to lock refcells on various threads. is that the right thinking?

---

## 🤖 Claude

Right thinking, and it's exactly what our current model does — worth walking through so it's clear where each guarantee lives:

**`&mut T` where T:!Sync** (§11.11): lifts to anon mut-effect group. Mut-effect groups are single-thread-visible per §12.6. Vale can't farm the ref across Vale threads.

**`&T` where T:!Sync** (§11.11): rejected outright at import. Never enters Vale as a shared ref, so can't be farmed period.

**Owned T where T:!Sync inside a Vale struct field**: the field can only be MUTATED (via RefCell::borrow, Cell::set, etc.) from methods with mut effect on the containing group. No-mut-effect methods on `&Widget` can't touch the RefCell in any meaningful way (all its useful methods are mut-effect). Vale's group-effect enforcement prevents accidental cross-thread access.

**Where the farming bug you describe actually fires:** under **(a) assume-no-mut default for missing annotations**. Concretely with the RefCell example:

1. Vale imports RefCell but has no annotation for `RefCell::borrow`
2. Under (a), Vale assumes `borrow` is no-mut-effect
3. `Widget::read_count(&self) { self.refcell.borrow().clone() }` accepted as no-mut method
4. Vale always-Sync emits `unsafe impl Sync for Widget`
5. Rust farms `&Widget` across N threads
6. Each thread calls `read_count`, each internally does `refcell.borrow()`, each mutates BorrowFlag
7. Race on BorrowFlag → UB

**Under (b) assume-mut default**: `RefCell::borrow` (missing annotation) treated as mut-effect. `read_count` can't be no-mut. Compile error → user restructures or supplies annotation. Race prevented at typecheck.

**Under (c) refuse-imports default**: user must annotate `RefCell::borrow` before importing RefCell at all. Vale stdlib ships correct RefCell annotations, so no friction there. Third-party crate imports blocked until annotated. Race prevented at import site.

Your instinct generalizes to the correct rule: **assume mut effect on ambiguous cases** — whether the ambiguity is at the reference-import boundary (T:!Sync incoming shared ref) or at missing method annotations. Fail-closed consistently in both places.

That's the shape (b) and (c) both implement, just at different friction points. Confirms (a) is dead; still open whether you want (b) or (c) for the missing-annotation default.

---

## 👤 User

b

---

## 🤖 Claude

Locking (b). Doc updates: §24.7 addition, §12.6 cross-ref, §26 invariant (MAMFC), and send Harmonious the resolution.

---

## 🤖 Claude

Now the §26 invariant additions — summary table row + §26.21 body:

---

## 🤖 Claude

Now send Harmonious the resolution:

---

## 🤖 Claude

Doc updates landed (4 edits) and response sent. Watcher (`bi1vxka66`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>bi1vxka66</task-id>
<tool-use-id>toolu_01NzeWNHehwsx4PyCd6DQ3r5</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bi1vxka66.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Surfacing — Harmonious's Q7 is a substantive architectural question about §5.1's borrowed-mode FFI + concurrent CGU semantics.

**Their question:** How does `consumer_fill_modules` interact with C++ Backend for multiple Vale CGUs?
- (a) One FFI call per CGU, sequential in the hook window
- (b) Single call, one big Vale CGU
- (c) Something else

Plus two state-model concerns: (i) GlobalState (LLVM Type* cache) — must be per-context, not cross-context; (ii) MetalCache — process-global vs per-call.

**My recommendation: (a) with Vale-controlled CGU partitioning.**

Rationale:
- Matches rustc's per-CGU model → enables post-hook rayon parallelism for LLVM optimization of Vale bodies (the hot path in codegen).
- Sky's `llvm_gen.rs` already does this shape (state-per-call, one FFI per ModuleLlvm) — empirically validated.
- (b) loses rustc's parallelism for potentially substantial Vale IR; not worth the engineering saving.

**Proposed state model under (a):**
- **GlobalState** (LLVM Type* cache, DataLayout, ptrSize): **per-FFI-call**. Fresh cache each `backend_compile_program_into`. Cross-context Type* is silent LLVM UB, so this is load-bearing.
- **MetalCache** (Vale-source-level type/method resolution): **shared across FFI calls within one valec-rs invocation**. Doesn't depend on LLVMContext. FFI calls are sequential within the sync hook window → no concurrent-access race.
- **Vale CGU partitioning**: Vale controls its own partition. v1 can use a simple strategy (one CGU per Vale library or top-level module); refine later based on measured LLVM optimization times.

**Doc additions to §5.1** (main location) + reference from §5.7:
- Sequential-call semantics: multiple `backend_compile_program_into` within one hook window; fresh (LLVMContext, LLVMModule) each.
- Explicit "no cross-call Type* aliasing" invariant.
- MetalCache lifecycle: shared across calls within one valec-rs invocation, not per-CGU.
- The **existing single-invocation GlobalState model in Vale's C++ Backend gets refactored to instantiate-per-call**. This is a real Phase 0 engineering task, not a small tweak.

**Two things I want you to eyeball before I answer Harmonious + update §5.1:**

1. **Recommendation (a) — does that match your intent for the C++ Backend?** The refactor to per-call GlobalState is real work (~days of engineering on the C++ side); (b) avoids it. Trade-off is post-hook parallelism.

2. **The C++ Backend refactor scope.** I'm treating GlobalState per-call as mandatory under (a). Sky's `llvm_gen.rs` shows the pattern. But Vale's C++ Backend is Vale-native code, not toylangc's Rust code. The refactor is real. Want to flag this to the Phase 0 task list, or lump it under §5.7's C++ port line?

Standing by for your take, then I'll send Harmonious and update §5.1.

---

## 👤 User

1. a sg
2. yes. i want GlobalState to become less global long-term so this is a step in the right direction

---

## 🤖 Claude

Landing. §5.1 updates + Phase 0 mention + Harmonious response.

---

## 🤖 Claude

Adding the concurrent-CGU semantics + state model to §5.1, and Phase 0 task for GlobalState refactor.

---

## 🤖 Claude

Now send Harmonious:

---

## 🤖 Claude

§5.1 concurrent-CGU semantics + state model landed, Phase 0 GlobalState refactor task added, Harmonious answered. Watcher (`bah5badmp`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>bah5badmp</task-id>
<tool-use-id>toolu_011GiR9UFje5Qs9bCyKq9qMG</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bah5badmp.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Surfacing — Q8 (sealed interfaces × Rust orphan rule) has a fourth option I want to recommend that Harmonious didn't list.

Their three: (a) accept leak, (b) prevent sealed `exported(rust)`, (c) runtime discriminant check.

**Option (d): emit the Rust sealed-trait pattern in stub_gen.**

Rust has a well-established idiom for sealed traits: private supertrait that only the trait author can impl. Vale's stub_gen can emit this shape:

```rust
mod __vale_sealed {
    pub trait Sealed_AstNode {}
}

pub trait AstNode: __vale_sealed::Sealed_AstNode {
    // ... interface methods ...
}

impl __vale_sealed::Sealed_AstNode for ExprNode {}  // Vale-emitted
impl AstNode for ExprNode { ... }                    // Vale-emitted
```

Rust downstream trying to add `impl AstNode for RustType` fails at Rust compile time — can't impl `Sealed_AstNode` because the `__vale_sealed` module is private and unreachable from downstream. Rustc rejects the impl at the downstream `impl` site with a clear error.

Preserves Vale sealed semantics AND Rust interop simultaneously. Zero runtime cost. Well-precedented (Rust stdlib uses this for `Iterator::size_hint` extensions, serde uses it for `Deserializer`, etc.).

**Beats Harmonious's three:**
- (a) silently unsound — not Vale's stance
- (b) unnecessarily restrictive — users lose Vale-side sealed semantics if they want Rust interop
- (c) runtime cost + stub bloat; also needs decision about what to do when foreign impl detected

**Doesn't affect the file-vs-project sealed-closure-scope decision** — that's Vale-source-side discipline (where can impls be added Vale-side?). (d) handles the Rust-boundary side; both file-scope and project-scope work with it.

Confirm (d), and I'll answer Harmonious + add a paragraph to §6.6 spelling out the sealed-trait-pattern emission?

---

## 👤 User

i think an exported(rust) sealed trait would export to an enum. and possibly also a trait... yeah, also a trait. the enum would impl the trait. so yeah option d i think?

---

## 🤖 Claude

Perfect — enum + trait gives Rust callers both pattern-matching and dyn-dispatch ergonomics. Executing: §6.6 update + Harmonious answer.

---

## 🤖 Claude

Now Harmonious:

---

## 🤖 Claude

§6.6 updated with the enum + sealed-trait pattern, Harmonious answered. Watcher (`bh495dtcz`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>bh495dtcz</task-id>
<tool-use-id>toolu_01VUFSo8gZSM29JeHSTG94gK</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bh495dtcz.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Surfacing both — a small Q8 clarification and a substantive Q9 ask.

**Q8 clarification** (small): When Vale receives a sealed interface value from Rust, is it the enum form, the trait-object form, or both?

My read: **enum form is primary** (Vale can pattern-match on the discriminant); trait-object is an alternate projection for the rare case where Rust wants dyn dispatch. Vale-source pattern match works uniformly on the enum. Vale API design should prefer the enum for sealed interface parameters. Small addition to §6.6.

**Q9 (substantive):** §22.4's cross-crate perf-parity claim ("Vale's cost equals Rust's at every opt level") came from Sky's Bench 1/4b measurements — on Sky's Rust Inkwell emitter. Vale's C++ Backend has different IR emission patterns. The claim doesn't automatically transfer.

Harmonious's cited precedent: Sky §F1 — `#[inline(never)]` on Sky's stub fn shells silently killed cross-language inlining for a long stretch until an emitter-audit caught it. Emitter details matter a lot for perf; inheritance from Sky's empirical numbers is not the same as validation on Vale's actual emitter.

Their ask: is Vale planning bench-parity measurement for the C++ Backend, or is Vale accepting "find out when we ship" as posture?

**My recommendation:**
- **§22.4 wording correction**: distinguish "inherited claim from Sky's Rust Inkwell emitter, un-measured on Vale's C++ Backend" from "Vale-measured." Current wording reads as validated for Vale; it isn't.
- **Phase 5 gets a bench-parity milestone**: before shipping, run Vale's C++ Backend through analog of Sky Bench 1 (basic function-call cross-language inline) and Bench 4b (drop-chain LTO cascade). Verify inner-loop disassembly-byte-parity for the canonical scenarios. Emitter-detail bugs (missing intrinsic annotations, wrong attribute stamps, IR shape hitting different inliner heuristics) surface as parity failures at that gate.
- **If parity fails**: emitter-detail bug in Vale's C++ Backend. Same investigation shape as Sky §F1 — audit inliner-hitting attributes on emitted symbols, compare to rustc-emitted equivalents, fix the discrepancy. Not a §22.4 architectural failure; an implementation-level bug the gate catches.

This is more work in Phase 5 but honest about what we know vs what we've validated. Aligns with §25.3.6's calibration discipline ("budget empirical-fixture work as load-bearing for catching premise errors").

**Alternative** (weaker): accept the inherited claim, discover discrepancies later. Cheaper up front, riskier at ship time.

Confirm the "measure at Phase 5" posture + the wording correction, and I'll answer Harmonious with the clarification + the Q9 measurement plan + land the doc changes?

---

## 👤 User

Q8: your read is right yeah
Q9: lets plan bench-parity measurements. we need to uphold that performance

---

## 🤖 Claude

Landing all four: §6.6 clarification, §22.4 wording, Phase 5 milestone, and Harmonious answer.

---

## 🤖 Claude

Now Harmonious:

---

## 🤖 Claude

All four landed: §22.4 wording, §28 Phase 5 bench-parity milestone, §6.6 direction clarification, Harmonious response delivered. Watcher (`bxviqs3w3`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>bxviqs3w3</task-id>
<tool-use-id>toolu_01FfS3dS7CYUNrQgfQxmoEGF</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bxviqs3w3.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Surfacing Q10 (libLLVM coexistence). Mostly mechanical, but one UX decision worth checking with you.

Harmonious's three asks:
1. Runtime libLLVM lookup (which one loads?)
2. Failure mode when versions drift
3. vale-toolchain.toml alignment check

**My proposed answers:**

- **(1) RPATH per binary** — `valec`'s RPATH → its bundled libLLVM directory; `valec-rs`'s RPATH → rustc sysroot's libLLVM. Standard mechanism, no cross-contamination. Version-suffixed library filenames (`libLLVM-21.1.8.dylib`) add a defensive layer so different versions can coexist on disk without collision.
- **(2) Hard error at build init** with clear diagnostic pointing at correct install command. Fail-closed. Sample:
  ```
  error: vale-toolchain.toml pins channel "vale-nightly-2026-06-29"
         but valec-rs is at "vale-nightly-2026-06-15"
    hint: run `rustup update vale-rs-nightly-2026-06-29`
  ```
- **(3) Vale's CLI verifies at every `valec build`** — read pinned channel from vale-toolchain.toml, detect which binaries the project needs, compare installed versions, hard error on mismatch. Fast check, runs before any real work.

**UX decision worth your take** — when versions drift, should Vale's CLI:

- **(α) Error with instructions** ("run `rustup update vale-rs-nightly-XXX`") — user takes action
- **(β) Auto-invoke valeup / rustup** to install the correct version — mirrors how rust-toolchain.toml + rustup auto-install missing toolchains
- **(γ) Hybrid**: error by default (Vale's "no shortcuts" stance), opt-in auto-install via `[tool.valec] auto_install = true` in vale-toolchain.toml — users who want the Rust-style experience flip the flag

I lean **(γ)** — errors by default match Vale's correctness-over-convenience stance; auto-install opt-in for users who want Rust-style ergonomics. (α) alone is fine too if you'd rather keep tooling simple.

Doc updates once you sign off: §3.5 gets a "cross-installer alignment mechanism" paragraph spelling out RPATH + version check + failure mode. Small addition. Which UX (α/β/γ)?

---

## 👤 User

2 and 3. hard errors always. we can soften later. while we're developing, we want to be error-heavy and assertion-heavy

---

## 🤖 Claude

Landing. §3.5 addition + Harmonious answer.

---

## 🤖 Claude

Now Harmonious:

---

## 🤖 Claude

§3.5 alignment mechanism landed, Harmonious answered. Watcher (`b61oo0dn6`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>b61oo0dn6</task-id>
<tool-use-id>toolu_01PdhVSoeUAB7MLNHmzBgMyF</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/b61oo0dn6.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Q11 — mostly mechanical, but the current §5.6 language is ambiguous. Landing the clarification + Harmonious answer.

---

## 🤖 Claude

Now Harmonious:

---

## 🤖 Claude

§5.6 clarification landed, Harmonious answered. Watcher (`bmeqsjws4`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>bmeqsjws4</task-id>
<tool-use-id>toolu_01LZWJCCGd663ZxVbwoUAo68</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bmeqsjws4.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Surfacing Q12 — comptime evaluator concurrency. Substantive locking decision.

**Context:** §13.11 locks single-threaded evaluator v1; §13.7 integrates partial evaluation with per_instance_mir which fires on rustc's rayon workers. Enforcement needed.

**Harmonious's four options + my analysis:**

- **(a) Global lock** across per_instance_mir queries. Simple, safe. @GCMLZ risk manageable if Vale provider avoids re-entrant locks (matches existing discipline in §26.2). Vale provider work is bounded — partial-eval body construction, not the full mono walk — so serializing it doesn't kill much throughput. Rustc's other work stays parallel.
- **(b) Per-thread slab.** Real parallelism; memoization per-thread (wasted evaluation on collisions but not wrong); content-hash discipline (§13.3, from §29.A.content-hash-const-args) already ensures slab addresses never leak into hashes, so no correctness concern there. More engineering (per-thread lifecycle, arena management).
- **(c) Force `-Z threads=1`**: kills rustc's mono parallelism entirely. Too costly.
- **(d) Work-queue with dedicated evaluator worker**: same effective serialization as (a) with more overhead. Skip.

**My recommendation: (a) for v1, with (b) documented as future direction.**

Rationale: v1 posture is fail-closed / correctness-heavy; global lock is the safest single-threaded-invariant enforcement. Vale provider work is a small fraction of mono walk time. If Phase 5 bench-parity or later profiling shows Vale's provider on the critical path, upgrade to (b).

**Slab lifecycle (Harmonious's related concern):** bake happens during per-Instance body construction — slab holds intermediate values during evaluation; results baked into typed AST are converted to slab-independent form (per §13.4's "resolved form, not slab references"). Under (a) sequential access, slab state is consistent per query; between queries the slab can be reset or kept per implementation preference. Under (b) per-thread slabs, each query is independent.

**Confirm (a) for v1 + (b) as future direction**, and I'll:
- Update §13.11 to spell out the concurrency approach + slab lifecycle
- Add a bullet to §13.7 about serialization at per_instance_mir provider
- Answer Harmonious with the locked posture

Or want (b) directly for v1 — more engineering up front but real parallelism? Your call.

---

## 👤 User

confirmed

---

## 🤖 Claude

Landing.

---

## 🤖 Claude

Now Harmonious:

---

## 🤖 Claude

§13.7 + §13.11 updated with (a) locked and (b) as v2 direction, Harmonious answered. Watcher (`b7n21nwcy`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>b7n21nwcy</task-id>
<tool-use-id>toolu_01GaV4YzPRsoxyhLmNGapgfk</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/b7n21nwcy.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Surfacing Q13 — substantive language design on `comptime if` at type positions.

Harmonious's two threads:

**(1) Position enumeration** — where can `comptime if { T1 } else { T2 }` appear?

My recommendations for v1:
- **(i)** struct field type: allowed (LOCKED per String example)
- **(ii)** function return type: allowed (natural extension)
- **(iii)** function parameter type: allowed (symmetric with return)
- **(iv)** generic argument position: allowed (`Vec<comptime if ... , ...>`)
- **(v)** let-binding type annotation: allowed (falls out from "type slot")
- **(vi)** trait impl selector: **DEFERRED** — rustc's coherence rules interact badly with comptime-selected traits; complex boundary work; v2 candidate
- **(vii)** match arm scrutinee: N/A — comptime selection doesn't help pattern matching

Uniform: allowed everywhere a type slot appears in Vale source, except trait impl position (deferred to v2).

**(2) Comptime functions returning Type — construction or selection?**

Recommendation: **allowed with selection restriction.** Users can factor `comptime if` chains into named helpers:

```
comptime fn choose_type<A>() -> Type {
    comptime if A == GlobalAlloc { X } else { Y }
}
struct Foo<A> {
    inner: choose_type<A>()
}
```

Mechanically identical to inline `comptime if`, just factored via a function. §13.4's "no type construction" still holds — users can't write `comptime fn make_struct(...) -> Type { struct { a: Int, b: Bool } }` (constructing new type declarations at comptime remains forbidden).

**Instantiations of pre-existing generics count as pre-existing** (Box<T> is Box-with-arg-T, not construction). So:

```
comptime fn wrap<T>() -> Type { Box<T> }   // allowed — Box exists, just instantiated
```

is fine.

**Impact**: cleaner stdlib factoring, more expressive user code, mechanism already exists (same evaluator path as inline `comptime if`), small design cost.

**Confirm** positions (i)-(v) allowed + (vi) deferred + comptime-fn-returns-Type allowed with selection restriction, and I'll update §13.4 to spell out the position enumeration + factoring rule?

---

## 👤 User

comptime if isn't really for type-expressions. it's meant to be a statement inside a function body, i think similar to constexpr if in c++.
for all those other use cases, the mechanism would be different to accomplish those goals, we can talk about those if you want

---

## 🤖 Claude

Got it — walking `comptime if` back to body-only (constexpr if analog). That has doc implications I want to surface:

**§12.1's String example** currently uses `comptime if` at struct field position (per Q2 from convo-2):
```
struct String<A> {
  inner: comptime if A == GlobalAlloc { rust.String } else { Vec<u8, A> }
}
```

If `comptime if` is body-only, that shape isn't correct. Same for the trait-signature example I added — anywhere I wrote `comptime if` in type position needs rework.

**§13.4** says "comptime can select among pre-existing types; a struct field's type can be a `comptime if` expression over existing types." Also now inaccurate.

Yes, let's talk about the mechanisms for those use cases. My guesses on what they could be:

- **Multiple `#[cfg]`/generic-conditional struct definitions**: separate definitions for `String<GlobalAlloc>` vs `String<A>` for other A; Vale resolves based on concrete arg at instantiation. Like Rust's specialization.
- **Associated-type trait mapping**: a trait `BackingFor<A>` with `type Backing`; impls for each specific allocator; `String<A>` field type is `<A as BackingFor>::Backing`. Analog of Rust's associated type family pattern.
- **Comptime function returning Type at instantiation time** (not `comptime if`): `fn String<A>` gets a per-instantiation "compute inner type" via reflection or type-family machinery, but through a different construct than `comptime if`.
- **Sugar layer over one of the above**: users write something that reads like `comptime if` but desugars to a per-instantiation dispatch mechanism.

Which of these matches your intent? Once I know the mechanism for type-level selection at struct-field / return / param positions, I can:
1. Rework §12.1's String example to the correct shape
2. Update §13.4 to describe the actual type-selection mechanism instead of "comptime if at types"
3. Answer Harmonious with the accurate design space

Standing by for your direction.

---

## 👤 User

for the string example, it would actually look like this:
struct String<A: Allocator> {
   comptime let inner_type: AnyType = CalculateInnerType(A); // AnyType is a trait btw
   inner: inner_type
}
// this looks like it's using run-time reflection, since it's run at comptime (in the comptime above) it's actually doing compile-time type inspection
func CalculateInnerType<A: Allocator>(a_type: A) AnyType {
   if a_type == GlobalAlloc { return rust.String; }
   else { return Vec<u8, A> }
}

---

## 🤖 Claude

Got it — `comptime let` binds a compile-time type value, `field: inner_type` uses it. More general than `comptime if` at type positions because arbitrary logic goes in the helper function. Elegant.

**Verifying my understanding:**

- `AnyType` is a trait that Type values satisfy (like Rust's `Any` but at comptime); `rust.String`, `Vec<u8, A>`, etc. impl it.
- `comptime let` at struct definition scope is evaluated per-instantiation; then normal type-slot references (`inner: inner_type`) resolve at that instantiation's compile.
- The helper function is regular Vale — no `comptime if`; regular `if` works because the whole function runs at comptime.

**Three follow-ups for the doc rework:**

1. **Same mechanism for function return types?** E.g. `func foo<A>() -> ???` where the return type depends on A. Top-level `comptime let ret_ty: AnyType = ...` bound to the function's return type, or something else?
2. **Function parameter types?** Same shape needed in practice, or out of scope?
3. **Generic arg positions** (`Vec<comptime-computed-type, GlobalAlloc>`) — falls out from being able to reference the comptime let, or a separate mechanism?

For Harmonious's remaining positions:
- **(vi) trait impl selector**: still deferred/rejected? Comptime-selected trait resolution seems architecturally hard.
- **(vii) match arm scrutinee**: N/A regardless?

Once I know the mechanism's supported surface, I'll rework §12.1 String example (retiring the `comptime if` shape) + rewrite §13.4's type-selection language + answer Harmonious.

---

## 👤 User

1. yes
2. yes
3. yeah a generic arg is basically a comptime let thats fed into the receiver

---

## 🤖 Claude

Landing. Big rework needed for §12.1's String example, §13.4's mechanism description, and §29.5's summary. First let me grep for any other `comptime if` mentions in type-position that I need to catch.

---

## 🤖 Claude

Now Harmonious:

---

## 🤖 Claude

All doc updates landed (§12.1 String example, allocator_api reference, §13.4 rewrite, §29.5 mention), and Harmonious answered with the corrected mechanism. Watcher still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>bc1x5azdk</task-id>
<tool-use-id>toolu_017WmWPkdHviU9mvaBuo9SHq</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bc1x5azdk.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Blast — earlier command had a typo (`target/search` instead of the send binary path) that consumed the heredoc body. Resending Q13 answer properly.

---

## 🤖 Claude

Q13 answer resent with full body. Watcher (`bffuxakms`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>bffuxakms</task-id>
<tool-use-id>toolu_019vS5C1qxts1hPqz4zs8jRG</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bffuxakms.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Now Harmonious:

---

## 🤖 Claude

§13.7 mode-independence + §13.11 dual-mode enforcement landed, Harmonious answered. Watcher (`b70uynfzu`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>b70uynfzu</task-id>
<tool-use-id>toolu_01SFgkNgf2LHk38ysvSuAvq5</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/b70uynfzu.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Now Harmonious:

---

## 🤖 Claude

§14.5 HBAB-consistency clarification landed, Harmonious answered. Watcher (`b45mgh9fb`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>b45mgh9fb</task-id>
<tool-use-id>toolu_015zgi29SWh6xf8sCfRhL4Tw</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/b45mgh9fb.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Surfacing Q16 — Phase 3 arena ownership migration, the TBD lifetime tower decision. Genuine open question I don't have enough context to answer without you.

**Harmonious's four questions + meta:**

- **(a) The tradeoff.** Ouroboros/yoke preserves finer-grained lifetime control but complicates every arena-referring type. Collapse simplifies at correctness cost. Which correctness guarantees actually matter to Vale's frontend? Is there a concrete scenario multi-lifetime catches that collapse would miss?
- **(b) What's blocking.** Waiting on group-borrowing implementation experience? Or an architectural clarification about frontend lifetime relationships?
- **(c) Cost of picking wrong.** Collapse → later revert to ouroboros = every arena-referring type re-annotated with lifetimes. Ouroboros → later collapse = carrying over-engineering. Which failure mode is cheaper to unwind?
- **(d) Timing.** Phase 3 is ~2-3 months per §28.1. When does the decision need to land — before Phase 3 starts, during, or can it be deferred further?
- **Meta:** biggest unresolved gate in phasing, or are there others of similar shape (Phase-N-blocked-on-unresolved-decision)?

**What I can bring to this:** §8 says arena-allocated `&'t` refs with MustIntern seal; HinputsT is in-memory during compilation; Session-scoped means "for the entire valec build session." What I don't know: the specific transient-vs-persistent arena boundaries in Vale's frontend, what group-borrowing's implementation will demand of the frontend arena model, or how the ouroboros/yoke vs collapse tradeoff plays under Vale's actual usage patterns.

**Two ways to answer:**

- **Honest defer**: "This is a real TBD; here's why we're not resolving it yet." List specific blockers + expected resolution timing. Doc's already TBD-flagged in Phase 3 so this is consistent with existing framing.
- **Substantive answer**: you supply framing on (a)-(d) and the meta, I write it up.

Which shape do you want? And if it's the substantive one — do you have a lean on ouroboros vs collapse, or is that genuinely still TBD?

---

## 👤 User

lets figure out and do the arena ownership migration before all this rustc interop stuff

---

## 🤖 Claude

Landing. Moving arena migration from Phase 3 TBD to Phase 0 groundwork.

---

## 🤖 Claude

Now Harmonious:

---

## 🤖 Claude

§28.1 phasing updated (Phase 0 gains arena migration; Phase 3 loses it), Harmonious answered. Watcher (`bxus6jwkt`) still armed. Standing by.

---

## 👤 User

<task-notification>
<task-id>bxus6jwkt</task-id>
<tool-use-id>toolu_01BLFekxcdGjxrfEqMrttsqF</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bxus6jwkt.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Wrapping the review — 7-item batch from Harmonious. Surfacing 3 that need your input; will bulk-handle the mechanical 4 after your sign-off.

**Need your input:**

**(c) §18.1 `[[lib]]` array notation.** Was intentional (multiple libs per Vale project) or typo (should be `[lib]` singular like Cargo)? Cargo does `[[bin]]` array + `[lib]` singular. If Vale mirrors Cargo, this is a typo; if Vale supports multiple libs per project, it's intentional. Which?

**(d) Q-reference opacity.** Harmonious flags that "Q11", "Q45 β", "Q66 E", etc. throughout the doc point at your original design conversation transcript — future readers without that transcript can't cross-reference. Three options:
- **(i)** Inline the Q-decision one-liner at each cite ("Q45 β locked allocator-generic types as..." rather than bare "per Q45 β"). More verbose but self-contained.
- **(ii)** Appendix table mapping every Q-ref to a one-line decision summary.
- **(iii)** Scrub Q-refs pre-1.0 in favor of prose-only justifications — Q-refs are internal editorial scaffolding, remove before ship.

I lean **(ii)** — appendix table is cheapest to build and readers who care can cross-ref; readers who don't skip it. Editorial call.

**(f) §26.18 CIDD grep-based fence.** "Any `unsafe {` not preceded by `comptime if __deterministic()` in a comptime-callable function fails the build." Grep can't determine "comptime-callable" — that requires reachability analysis (transitive callee closure from any comptime block/const). Fence is either over-guards (every stdlib `unsafe {` requires the gate — ergonomic cost) or under-guards (only marked-comptime functions checked — misses transitively-reachable-but-unmarked). Which? What catches the miss under under-guards? Real correctness concern; the current text is aspirational.

**Mechanical items I'll handle after your call on the three above** (want to bulk them for efficiency):

- **(a) §3.2 binary sizes ~2GB.** Add parenthetical: "estimate; includes forked rustc + libLLVM + pre-compiled Vale stdlib per target + valec-rs binary." Flag as aspirational until measured.
- **(b) §1.6 valec-rustc coupling.** Add clarification: "valec doesn't depend on rustc's internals, but its release cadence is coupled to rustc-nightly's LLVM via the shared libLLVM invariant (§3.6)."
- **(e) §21 publishing implication callout.** Add: "Vale libraries are consumer-toolchain-forcing — any Rust crate that transitively depends on a Vale library makes ALL its own consumers require the Vale toolchain. Library authors should factor this into publishing decisions."
- **(g) §28 Phase 0 sizing recount.** Update from "~9-18 months single engineer, ~6-9 months small team" to reflect additions (GlobalState refactor, arena migration, bench-parity fixtures). Rough new estimate: 12-24 months single engineer, 8-12 months small team.

Answer your calls on (c), (d), (f), and I'll batch-land all seven + close-out message to Harmonious.

---

## 👤 User

c. vale mirror cargo
d. its okay to reference the convos, as long as we say where the convos are. i think those exist somewhere around here, maybe docs/historical?
f. no grep-based fences in vale.

---

## 🤖 Claude

Landing all seven. First checking whether other grep-based fences need the same treatment as (f).

---

## 🤖 Claude

Executing all edits in parallel:

---

## 🤖 Claude

All 10 edits landed. Now the close-out message to Harmonious:

---

## 🤖 Claude

All 10 doc edits landed (Q-ref note, valec/rustc coupling clarification, binary size aspirational note, `[lib]` fix in two places, publishing-implications callout, three CI-fence retrofits from grep to proper analysis, Phase 0 sizing recount). Batch answer to Harmonious sent. Watcher (`bql8m7zh2`) still armed. Standing by for close-out ping.

---

## 👤 User

<task-notification>
<task-id>bql8m7zh2</task-id>
<tool-use-id>toolu_01PcbhNKSwACLxeuDEg2BPxg</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bql8m7zh2.output</output-file>
<status>completed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" completed (exit code 0)</summary>
</task-notification>

---

## 🤖 Claude

Mail arrived. Recv + re-arm.

---

## 🤖 Claude

Review closed. Harmonious wrapped it out cleanly. Reporting to you now.

**Review outcome:** 16 substantive threads (Q1-Q16) + 7 minor items over the arch review. Doc's materially stronger for the round-trip.

**Substantive items with material doc impact:**

- **Q1** retired `__mode_is_rustc()` — collapsed mode-gating to single `#[cfg(rust_interop)]` mechanism.
- **Q2 → Q13 chain**: initial `comptime if` at type positions retracted in favor of `comptime let AnyType` + helper function. My earlier framing was wrong; corrected via §12.1 String example + §13.4 rewrite.
- **Q3**: `dangle` + group borrowing framing; §11 intro flags Nick's model + Vale's independent iterations.
- **Q4**: valec → valec-rs graduation model, cache thrashes on binary swap.
- **Q5 → alternative substrate**: retired ValeImm wrapper entirely in favor of effect-based model + no Cell/RefCell + always-Sync boundary emission + return-type filter. Substantial §11/§12 rewrite.
- **Q7**: concurrent CGU semantics + per-call GlobalState; Phase 0 GlobalState refactor task added.
- **Q8**: sealed interface × orphan rule → enum + sealed trait pattern.
- **Q9**: Phase 5 bench-parity validation gate added; §22.4 wording corrected to distinguish "inherited from Sky" from "Vale-measured."
- **Q10**: cross-installer alignment via RPATH + hard-error on drift.
- **Q11**: binary entry shim per mode (C++ Backend `main` shim in valec mode; Rust shim in valec-rs mode).
- **Q12**: comptime concurrency global lock in valec-rs; single-threaded by construction in valec.
- **Q14**: mode-independence of instantiator + partial-eval; per_instance_mir as valec-rs driver.
- **Q15**: async substrate contained — only §14.5 HBAB-consistency clarification needed.
- **Q16**: **arena migration moved to Phase 0** (your call). Was TBD blocking gate; now foundational work.

**Minor items landed:** binary size aspirational note, valec-rustc coupling clarification, `[[lib]]`→`[lib]` (Vale mirrors Cargo), Q-refs pointer to `docs/historical/`, publishing-implications callout, **all three grep-based CI fences retrofitted to AST/structural/typechecker analysis** (your "no grep fences in Vale" call), Phase 0 sizing recount to 12-24 months.

**Doc state:** structural additions across §5, §6, §11, §12, §13, §14, §21, §24, §26, §28. New invariants: MAMFC (§26.21). Retired concepts: ValeImm wrapper, `comptime if` at type positions, α/β Sync openness, three grep-based fences. Cross-refs consistent throughout.

Harmonious's closer: *"The doc is materially stronger for the round-trip and Vale's arch is in a solid state as a pre-implementation lock."*

Watcher (`bu9ndf76h`) still armed if anything else comes up. Otherwise the review's done and we're at a clean pre-implementation architecture lock.

---

## 👤 User

<task-notification>
<task-id>bu9ndf76h</task-id>
<tool-use-id>toolu_01AncpjPLjVQxKT4nLQRXnyK</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-V-Vale4/1fef7402-0d47-4325-8680-9bacb1f4d5a2/tasks/bu9ndf76h.output</output-file>
<status>killed</status>
<summary>Background command "Re-arm mailbox watcher as Vale4-tl" was stopped</summary>
</task-notification>

---

