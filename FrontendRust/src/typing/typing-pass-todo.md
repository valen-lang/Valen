# Typing-Pass Migration TODO

Open items in the typing-pass Rust migration — deferrals, cleanups, and known gaps not yet closed. Moved out of `TL.md` (which now holds current status only).

*Audited against the live code; stale items (work already done) removed. Remaining:*

- **IRegionNameT** retains `_Phantom` — 0 Scala implementors found, deferred.
- **Remove the `// Rust adaptation (SPDMX-B): ...` comments** — ~65 of them across `FrontendRust/src/` (heaviest in `env/environment.rs`). The convention is retired: it's advisory only, not shield-mandated (SPDMX exception B sanctions interner/arena params without annotation). Strip them soon; no guidance recommends adding them anymore.
- **Slice-pipeline orphan sweep ~done.** Inventory at Phase-1 checkpoint: 77 `^fn .*<'s, 't>` matches in `src/typing/`; 64 are Pattern 3 (genuine free fns — `test/traverse.rs` `visit_*` family + `compiler_error_humanizer.rs` `humanize_*`/`printable_*` from `object` companions), 7 are non-pub `expect_*` helpers in `templata/templata.rs` matching the file's established `pub fn expect_*` free-fn convention (leave as-is), 5 are extractor `*_unapply` stubs awaiting Pattern-5 ambiguity ruling (`ast/ast.rs:376,999`; `ast/expressions.rs:2958,2970`; `templata/templata.rs:592`), and 1 (`compiler_solver.rs:2956 literal_to_templata`) was wrapped this session as `impl Compiler` method. Batch-sweep plan at `/Users/verdagon/.claude/plans/lets-do-proactive-please-proud-feather.md` is stale (its ~32-orphan estimate predated the reactive sweeps of Phase 1).
- **Revisit `/// Arena-allocated` vs `/// Temporary state` classifications.** Scaffolding may have over-eagerly marked types Arena-allocated when they're really transient solver outputs or function-return wrappers (`CompleteResolveSolve`/`CompleteDefineSolve`, `HinputsT`). Walk every `/// Arena-allocated` type and re-classify ones that don't need arena identity. Architect-level decision per type.
- **`RUST_MIN_STACK=16777216` set in this repo's `.cargo/config.toml`** — debug-mode workaround for postparser `scout_expression`'s giant per-arm-locals frame bloat (~21KB/frame; 96 frames of normal recursion exhausts the default 2MB stack on `typing_pass_on_roguelike`). Release builds pass unmodified. **Post-migration TODO: revert the config change** once `scout_expression` is split per-arm into helper fns (or its `(StackFrame, &IExpressionSE, VariableUses, VariableUses)`-tuple locals are Boxed) so default stack suffices in debug too.
- **Eliminate sources of nondeterminism.** Ptr-hashing on `@IEOIBZ` identity types, `IdT`, and `PtrKey<T>` is nondeterministic across runs and leaks into output via `HashMap`/`HashSet` iteration. `ArenaIndexMap` (insertion-ordered) is unaffected — the AASSNCMCX sweep accidentally fixed most cases. Remaining at-risk: `HashMap<PtrKey<IdT>, V>` in `CompilerOutputs` and `HashMap<IdT, V>` in `HinputsT` (both Temporary state). **Short-term:** extend ArenaIndexMap to ptr-hashed-key maps regardless of containing-struct class. **Long-term:** content-based hashing on `@IEOIBZ` types + `IdT` (touches the @IEOIBZ pattern). Bit-reproducible output requires both. Defer until the instantiator gets attention or a test flakes.
- **Production-CLI seam — items (3)+(4) open.** Items (1)+(2) landed: `FullCompilation::new` wired (was a `PhantomData` stub), 9 delegating methods on FullCompilation, 3 HammerCompilation orphan-wraps (`get_code_map`/`get_vpst_map`/`get_astrouts`). `frontend_rust` bin runs the live parser end-to-end on ASCII Vale (hello.vale parses clean). **Open:** (3) `pass_manager::build_and_output` Scout→Astrouts→CompilerOutputs→Hamuts→VAST-write pipeline body (~80 lines mirroring `PassManager.scala:286-341`; currently panics at `pass_manager.rs:878` "Scout phase not yet implemented"); (4) lifetime-glue question — pass_manager owns arenas on its stack (unlike the test harness which receives them), so FullCompilation may need a different ownership shape than the test path. Optional polish: `--region_override`, `-o <out>`, `--vale_ruster_path` CLI flags from Scala valec.
- **UTF-8 lexer position-tracking bug.** Multi-byte UTF-8 characters in Vale source drift the lexer cursor by 1 byte per occurrence. Repro: `parseChevronNoteAnchors` at `VmdParse/src/parse.vale:794-797` uses `"«"`/`"»"` (U+00AB/U+00BB, 2 bytes each); replacing them with ASCII `[`/`]` makes the file parse. `LexingIterator.position` is documented as a byte position and `advance()` correctly increments by `c.len_utf8()`, but Scala's counterpart does `position + 1` (UTF-16 chars). Candidate sites: `LexingIterator::peek_n` (`n` interpreted as byte count vs Scala's char count) or `try_skip_str` (`&str`'s byte length vs char length). ASCII Vale unaffected; blocks Vmd and any non-ASCII source.
- **`after_regions_*` typing-test tail — closed.** After Phase 4/4b/4e, the 40 originally-ignored tests are down to 20 across all `after_regions_*` files: 13 truly-permanent Scala-mirrors, 1 Scala-side `ignore()`'d (`upcasting_in_a_generic_function`), 4 Rust-side-only deferrals (`panic_in_expr`/`reports_when_ownership_doesnt_match`/`cant_make_weak_ref_to_non_weakable`/`end_to_end_test`), and 2 newly-`[~]` parser logic bugs from Phase 4e — see `migration-drive-todo.md` Phase 4e. Suite at 1122/0/20. Authoritative pass/deferred split (stale): `docs/historical/after-regions-test-fixing-quest.md`.

- **Two Phase 4e parser `[~]`s open.** Real logic bugs in `LexingPass`/`ParsingPass`: `report_leaving_out_semicolon_or_ending_body_after_expression_for_paren` (parser produces `Consecutor(...)` for `set x = 7 )` instead of `BadStartOfStatementError`), and `func_with_func_bound_with_missing_where` (parser produces `TopLevelFunction` for `func sum<T>() func moo(&T)void {3}` instead of `FuncBoundWithoutWhere`). Both `#[ignore]`'d with "blocked - …" rationales; tracked in `migration-drive-todo.md` Phase 4e.
- *(Done — reference note, not a TODO.)* The IEnvironmentT family is Polyvalue: held by value with derived eq/hash (@TFITCX / @PVECFPZ).
