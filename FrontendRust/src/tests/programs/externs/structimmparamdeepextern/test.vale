
exported struct Flamscrankle share {
  x int;
  b Bogglewoggle;
  y int;
}

exported struct Bogglewoggle share {
  s Spigglewigget;
  x int;
}

exported struct Spigglewigget share {
  x int;
  y int;
  z int;
}

extern func extFunc(flam Flamscrankle) int;

exported func main() int {
  return extFunc(Flamscrankle(7, Bogglewoggle(Spigglewigget(5, 7, 4), 5), 14));
}
