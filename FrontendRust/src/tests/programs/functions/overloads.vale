import v.builtins.arith.*;
import v.builtins.str.*;

// TSUGAR: func bork(a int, b int) int { return +(a, b); }
func bork(a &int, b &int) int { return a + b; }
func bork(a str, b str) str { return +(a, b); }
exported func main() int { return 3 bork 3; }
