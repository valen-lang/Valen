
weakable struct Muta { hp int; }
exported func main() int {
  ownMuta = Muta(73);
  weakMuta = &&ownMuta;
  maybeBorrowMuta = lock(weakMuta);
  drop(^ownMuta);
  // TSUGAR:   return maybeBorrowMuta.get().hp;
  return __copy_prim(maybeBorrowMuta.get().hp);
}