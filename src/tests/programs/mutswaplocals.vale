struct Ship { fuel int; }

exported func main() int {
  a = Ship(1);
  b = Ship(2);
  0; // lldb breakpoint: mutswaplocals-before
  [swapped] = (set b = (set a = (set b = Ship(3))));
  0; // lldb breakpoint: mutswaplocals-after
  [fa] = ^a;
  [fb] = ^b;
  return 42;
}
