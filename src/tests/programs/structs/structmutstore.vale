struct Carrier {
  hp int;
  interceptors int;
}

exported func main() int {
  c = Carrier(400, 8);
  set c.interceptors = 42;
  // TSUGAR:   return c.interceptors;
  return __copy_prim(c.interceptors);
}
