// Round-trip: Vale→C→Vale where C stashes a share ref and returns it.
// Proves the handle stays valid across a boundary hop.
exported struct Widget share { n int; }

extern func roundtrip(w Widget) Widget;

exported func main() int {
  w = Widget(42);
  returned = roundtrip(w);
  return returned.n;
}
