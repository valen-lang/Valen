// Isolates the auto-gen'd Arr_len + Arr_at for a runtime-sized array.
export []<imm>int as MyIntArray;

extern func testArrRead(arr []<imm>int) int;

exported func main() int {
  a = #[]int(6, {_ * 2});  // 0, 2, 4, 6, 8, 10 → sum 30
  return testArrRead(a) + 12;
}
