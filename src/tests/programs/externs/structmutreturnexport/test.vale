exported struct Thing {
  a str;
  b int;
}
exported func makeHelloThing(b int) Thing {
  Thing("hello", b)
}

extern func runExtCommand() Thing;

exported func main() int {
  res = runExtCommand();
  return len(res.a) + res.b;
}
