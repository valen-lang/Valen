
weakable struct Muta { hp int; }
exported func main() int {
  ownMuta = Muta(7);
  borrowMuta = &ownMuta;
  weakMuta = &&borrowMuta;
  maybeBorrowMuta = lock(weakMuta);
  // TSUGAR:   maybeBorrowMuta.get().hp
  __copy_prim(maybeBorrowMuta.get().hp)
}
