

import v.builtins.box.*;

interface MyInterface { }
struct MyStruct { value int; }
impl MyInterface for MyStruct;
exported func main() {
  x Box<dyn MyInterface> = Box<dyn MyInterface>(Box<MyStruct>(MyStruct(9)));
}
