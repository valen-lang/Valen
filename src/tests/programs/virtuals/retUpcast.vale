
import v.builtins.box.*;

interface XOpt { }
struct XSome { value int; }
impl XOpt for XSome;

func doIt() Box<dyn XOpt> {
  return Box<dyn XOpt>(Box<XSome>(XSome(9)));
}

exported func main() int {
  a = doIt();
  return 3;
}
