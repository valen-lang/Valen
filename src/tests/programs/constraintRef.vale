struct Carrier {
  hp int;
  interceptors int;
}

exported func main() int {
  carrier = Carrier(400, 8);
  ref = &carrier;
  // TSUGAR:   return ref.interceptors;
  return __copy_prim(ref.interceptors);
}
