
struct Spaceship {
  fuel int;
}

func getFuel(a &Spaceship) int {
  // TSUGAR:   return a.fuel;
  return __copy_prim(a.fuel);
}

exported func main() int {
  ship = Spaceship(42);
  return (&ship).getFuel();
}
