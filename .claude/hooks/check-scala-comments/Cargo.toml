[package]
name = "check-scala-comments"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
similar = "2"
regex = "1"
once_cell = "1"

[profile.release]
opt-level = "s"
strip = true
